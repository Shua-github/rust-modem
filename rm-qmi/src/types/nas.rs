//! Generated by `cargo xtask qmi-codegen`; do not edit.
//!
//! Source: `qmi-service-nas.json`.

#![allow(unused_imports)]
pub const SERVICE_ID: u16 = 3;
pub mod messages {
    #[doc = "Reset"]
    pub mod reset {
        use crate::{Endian, Error, TlvBuf, TlvReader, TlvWriter};
        use alloc::string::String;
        use alloc::vec::Vec;
        pub const MESSAGE_ID: u16 = 0;
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Request {}
        impl Request {
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                Ok(Vec::new())
            }
            pub fn from_tlvs(_tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Ok(Self::default())
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Response {}
        impl Response {
            pub const TLV_RESULT: u8 = 2;
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                Ok(Vec::new())
            }
            pub fn from_tlvs(_tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Ok(Self::default())
            }
        }
        impl crate::Response for Response {
            type Request = Request;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = crate::result::success_tlvs();
                tlvs.extend(self.to_tlvs()?);
                Ok(tlvs)
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                crate::result::check_result(tlvs)?;
                Response::from_tlvs(tlvs)
            }
        }
        impl crate::Request for Request {
            const SERVICE: u16 = super::super::SERVICE_ID;
            const MESSAGE_ID: u16 = 0;
            type Response = Response;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                self.to_tlvs()
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Self::from_tlvs(tlvs)
            }
        }
    }
    #[doc = "Abort"]
    pub mod abort {
        use crate::{Endian, Error, TlvBuf, TlvReader, TlvWriter};
        use alloc::string::String;
        use alloc::vec::Vec;
        pub const MESSAGE_ID: u16 = 1;
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Request {
            pub transaction_id: u16,
        }
        impl Request {
            pub const TLV_TRANSACTION_ID: u8 = 1;
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = Vec::new();
                {
                    let mut writer = TlvWriter::new();
                    writer.write_u16(*&self.transaction_id, Endian::Little);
                    tlvs.push(writer.into_tlv(1));
                }
                Ok(tlvs)
            }
            pub fn from_tlvs(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                let mut out = Self::default();
                let tlv = tlvs
                    .iter()
                    .find(|tlv| tlv.id == 1)
                    .ok_or(Error::MissingTlv { id: 1 })?;
                let mut reader = TlvReader::new(&tlv.value);
                out.transaction_id = reader.read_u16(Endian::Little)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Response {}
        impl Response {
            pub const TLV_RESULT: u8 = 2;
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                Ok(Vec::new())
            }
            pub fn from_tlvs(_tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Ok(Self::default())
            }
        }
        impl crate::Response for Response {
            type Request = Request;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = crate::result::success_tlvs();
                tlvs.extend(self.to_tlvs()?);
                Ok(tlvs)
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                crate::result::check_result(tlvs)?;
                Response::from_tlvs(tlvs)
            }
        }
        impl crate::Request for Request {
            const SERVICE: u16 = super::super::SERVICE_ID;
            const MESSAGE_ID: u16 = 1;
            type Response = Response;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                self.to_tlvs()
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Self::from_tlvs(tlvs)
            }
        }
    }
    #[doc = "Set Event Report"]
    pub mod set_event_report {
        use crate::{Endian, Error, TlvBuf, TlvReader, TlvWriter};
        use alloc::string::String;
        use alloc::vec::Vec;
        pub const MESSAGE_ID: u16 = 2;
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Request {
            pub signal_strength_indicator: Option<SignalStrengthIndicator>,
            pub rf_band_information: Option<bool>,
            pub registration_reject_reason: Option<bool>,
            pub rssi_indicator: Option<RssiIndicator>,
            pub ecio_indicator: Option<EcioIndicator>,
            pub io_indicator: Option<IoIndicator>,
            pub sinr_indicator: Option<SinrIndicator>,
            pub error_rate_indicator: Option<bool>,
            pub ecio_threshold: Option<EcioThreshold>,
            pub sinr_threshold: Option<SinrThreshold>,
            pub lte_snr_delta: Option<LteSnrDelta>,
            pub lte_rsrp_delta: Option<LteRsrpDelta>,
        }
        impl Request {
            pub const TLV_SIGNAL_STRENGTH_INDICATOR: u8 = 16;
            pub const TLV_RF_BAND_INFORMATION: u8 = 17;
            pub const TLV_REGISTRATION_REJECT_REASON: u8 = 18;
            pub const TLV_RSSI_INDICATOR: u8 = 19;
            pub const TLV_ECIO_INDICATOR: u8 = 20;
            pub const TLV_IO_INDICATOR: u8 = 21;
            pub const TLV_SINR_INDICATOR: u8 = 22;
            pub const TLV_ERROR_RATE_INDICATOR: u8 = 23;
            pub const TLV_ECIO_THRESHOLD: u8 = 25;
            pub const TLV_SINR_THRESHOLD: u8 = 26;
            pub const TLV_LTE_SNR_DELTA: u8 = 27;
            pub const TLV_LTE_RSRP_DELTA: u8 = 28;
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = Vec::new();
                if let Some(value) = &self.signal_strength_indicator {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(16));
                }
                if let Some(value) = &self.rf_band_information {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::from(*value));
                    tlvs.push(writer.into_tlv(17));
                }
                if let Some(value) = &self.registration_reject_reason {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::from(*value));
                    tlvs.push(writer.into_tlv(18));
                }
                if let Some(value) = &self.rssi_indicator {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(19));
                }
                if let Some(value) = &self.ecio_indicator {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(20));
                }
                if let Some(value) = &self.io_indicator {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(21));
                }
                if let Some(value) = &self.sinr_indicator {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(22));
                }
                if let Some(value) = &self.error_rate_indicator {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::from(*value));
                    tlvs.push(writer.into_tlv(23));
                }
                if let Some(value) = &self.ecio_threshold {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(25));
                }
                if let Some(value) = &self.sinr_threshold {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(26));
                }
                if let Some(value) = &self.lte_snr_delta {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(27));
                }
                if let Some(value) = &self.lte_rsrp_delta {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(28));
                }
                Ok(tlvs)
            }
            pub fn from_tlvs(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                let mut out = Self::default();
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 16) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = SignalStrengthIndicator::decode_from(&mut reader)?;
                    out.signal_strength_indicator = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 17) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u8()? != 0;
                    out.rf_band_information = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 18) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u8()? != 0;
                    out.registration_reject_reason = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 19) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = RssiIndicator::decode_from(&mut reader)?;
                    out.rssi_indicator = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 20) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = EcioIndicator::decode_from(&mut reader)?;
                    out.ecio_indicator = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 21) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = IoIndicator::decode_from(&mut reader)?;
                    out.io_indicator = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 22) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = SinrIndicator::decode_from(&mut reader)?;
                    out.sinr_indicator = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 23) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u8()? != 0;
                    out.error_rate_indicator = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 25) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = EcioThreshold::decode_from(&mut reader)?;
                    out.ecio_threshold = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 26) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = SinrThreshold::decode_from(&mut reader)?;
                    out.sinr_threshold = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 27) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = LteSnrDelta::decode_from(&mut reader)?;
                    out.lte_snr_delta = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 28) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = LteRsrpDelta::decode_from(&mut reader)?;
                    out.lte_rsrp_delta = Some(value);
                }
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Response {}
        impl Response {
            pub const TLV_RESULT: u8 = 2;
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                Ok(Vec::new())
            }
            pub fn from_tlvs(_tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Ok(Self::default())
            }
        }
        impl crate::Response for Response {
            type Request = Request;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = crate::result::success_tlvs();
                tlvs.extend(self.to_tlvs()?);
                Ok(tlvs)
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                crate::result::check_result(tlvs)?;
                Response::from_tlvs(tlvs)
            }
        }
        impl crate::Request for Request {
            const SERVICE: u16 = super::super::SERVICE_ID;
            const MESSAGE_ID: u16 = 2;
            type Response = Response;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                self.to_tlvs()
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Self::from_tlvs(tlvs)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct SignalStrengthIndicator {
            pub report: bool,
            pub thresholds: Vec<i8>,
        }
        impl SignalStrengthIndicator {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(u8::from(*&self.report));
                writer
                    .write_u8(u8::try_from((&self.thresholds).len()).map_err(|_| Error::TooLong)?);
                for item in &self.thresholds {
                    writer.write_i8(*item);
                }
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.report = reader.read_u8()? != 0;
                let count = reader.read_u8()? as usize;
                let mut items = Vec::with_capacity(count.min(256));
                for _ in 0..count {
                    items.push(reader.read_i8()?);
                }
                out.thresholds = items;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct RssiIndicator {
            pub report: bool,
            pub rssi_delta: u8,
        }
        impl RssiIndicator {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(u8::from(*&self.report));
                writer.write_u8(*&self.rssi_delta);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.report = reader.read_u8()? != 0;
                out.rssi_delta = reader.read_u8()?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct EcioIndicator {
            pub report: bool,
            pub ecio_delta: u8,
        }
        impl EcioIndicator {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(u8::from(*&self.report));
                writer.write_u8(*&self.ecio_delta);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.report = reader.read_u8()? != 0;
                out.ecio_delta = reader.read_u8()?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct IoIndicator {
            pub report: bool,
            pub io_delta: u8,
        }
        impl IoIndicator {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(u8::from(*&self.report));
                writer.write_u8(*&self.io_delta);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.report = reader.read_u8()? != 0;
                out.io_delta = reader.read_u8()?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct SinrIndicator {
            pub report: bool,
            pub sinr_delta: u8,
        }
        impl SinrIndicator {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(u8::from(*&self.report));
                writer.write_u8(*&self.sinr_delta);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.report = reader.read_u8()? != 0;
                out.sinr_delta = reader.read_u8()?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct EcioThreshold {
            pub report: bool,
            pub thresholds: Vec<i16>,
        }
        impl EcioThreshold {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(u8::from(*&self.report));
                writer
                    .write_u8(u8::try_from((&self.thresholds).len()).map_err(|_| Error::TooLong)?);
                for item in &self.thresholds {
                    writer.write_i16(*item, Endian::Little);
                }
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.report = reader.read_u8()? != 0;
                let count = reader.read_u8()? as usize;
                let mut items = Vec::with_capacity(count.min(256));
                for _ in 0..count {
                    items.push(reader.read_i16(Endian::Little)?);
                }
                out.thresholds = items;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct SinrThreshold {
            pub report: bool,
            pub thresholds: Vec<u8>,
        }
        impl SinrThreshold {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(u8::from(*&self.report));
                writer
                    .write_u8(u8::try_from((&self.thresholds).len()).map_err(|_| Error::TooLong)?);
                for item in &self.thresholds {
                    writer.write_u8(*item);
                }
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.report = reader.read_u8()? != 0;
                let count = reader.read_u8()? as usize;
                let mut items = Vec::with_capacity(count.min(256));
                for _ in 0..count {
                    items.push(reader.read_u8()?);
                }
                out.thresholds = items;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct LteSnrDelta {
            pub report: bool,
            pub snr_delta: u8,
        }
        impl LteSnrDelta {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(u8::from(*&self.report));
                writer.write_u8(*&self.snr_delta);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.report = reader.read_u8()? != 0;
                out.snr_delta = reader.read_u8()?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct LteRsrpDelta {
            pub report: bool,
            pub rsrp_delta: u8,
        }
        impl LteRsrpDelta {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(u8::from(*&self.report));
                writer.write_u8(*&self.rsrp_delta);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.report = reader.read_u8()? != 0;
                out.rsrp_delta = reader.read_u8()?;
                Ok(out)
            }
        }
    }
    #[doc = "Register Indications"]
    pub mod register_indications {
        use crate::{Endian, Error, TlvBuf, TlvReader, TlvWriter};
        use alloc::string::String;
        use alloc::vec::Vec;
        pub const MESSAGE_ID: u16 = 3;
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Request {
            pub system_selection_preference: Option<bool>,
            pub ddtm_events: Option<bool>,
            pub serving_system_events: Option<bool>,
            pub dual_standby_preference: Option<bool>,
            pub subscription_info: Option<bool>,
            pub network_time: Option<bool>,
            pub system_info: Option<bool>,
            pub signal_info: Option<bool>,
            pub error_rate: Option<bool>,
            pub hdr_new_uati_assigned: Option<bool>,
            pub hdr_session_closed: Option<bool>,
            pub managed_roaming: Option<bool>,
            pub current_plmn_name: Option<bool>,
            pub embms_status: Option<bool>,
            pub rf_band_information: Option<bool>,
            pub network_reject_information: Option<NetworkRejectInformation>,
        }
        impl Request {
            pub const TLV_SYSTEM_SELECTION_PREFERENCE: u8 = 16;
            pub const TLV_DDTM_EVENTS: u8 = 18;
            pub const TLV_SERVING_SYSTEM_EVENTS: u8 = 19;
            pub const TLV_DUAL_STANDBY_PREFERENCE: u8 = 20;
            pub const TLV_SUBSCRIPTION_INFO: u8 = 21;
            pub const TLV_NETWORK_TIME: u8 = 23;
            pub const TLV_SYSTEM_INFO: u8 = 24;
            pub const TLV_SIGNAL_INFO: u8 = 25;
            pub const TLV_ERROR_RATE: u8 = 26;
            pub const TLV_HDR_NEW_UATI_ASSIGNED: u8 = 27;
            pub const TLV_HDR_SESSION_CLOSED: u8 = 28;
            pub const TLV_MANAGED_ROAMING: u8 = 29;
            pub const TLV_CURRENT_PLMN_NAME: u8 = 30;
            pub const TLV_EMBMS_STATUS: u8 = 31;
            pub const TLV_RF_BAND_INFORMATION: u8 = 32;
            pub const TLV_NETWORK_REJECT_INFORMATION: u8 = 33;
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = Vec::new();
                if let Some(value) = &self.system_selection_preference {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::from(*value));
                    tlvs.push(writer.into_tlv(16));
                }
                if let Some(value) = &self.ddtm_events {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::from(*value));
                    tlvs.push(writer.into_tlv(18));
                }
                if let Some(value) = &self.serving_system_events {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::from(*value));
                    tlvs.push(writer.into_tlv(19));
                }
                if let Some(value) = &self.dual_standby_preference {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::from(*value));
                    tlvs.push(writer.into_tlv(20));
                }
                if let Some(value) = &self.subscription_info {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::from(*value));
                    tlvs.push(writer.into_tlv(21));
                }
                if let Some(value) = &self.network_time {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::from(*value));
                    tlvs.push(writer.into_tlv(23));
                }
                if let Some(value) = &self.system_info {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::from(*value));
                    tlvs.push(writer.into_tlv(24));
                }
                if let Some(value) = &self.signal_info {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::from(*value));
                    tlvs.push(writer.into_tlv(25));
                }
                if let Some(value) = &self.error_rate {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::from(*value));
                    tlvs.push(writer.into_tlv(26));
                }
                if let Some(value) = &self.hdr_new_uati_assigned {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::from(*value));
                    tlvs.push(writer.into_tlv(27));
                }
                if let Some(value) = &self.hdr_session_closed {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::from(*value));
                    tlvs.push(writer.into_tlv(28));
                }
                if let Some(value) = &self.managed_roaming {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::from(*value));
                    tlvs.push(writer.into_tlv(29));
                }
                if let Some(value) = &self.current_plmn_name {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::from(*value));
                    tlvs.push(writer.into_tlv(30));
                }
                if let Some(value) = &self.embms_status {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::from(*value));
                    tlvs.push(writer.into_tlv(31));
                }
                if let Some(value) = &self.rf_band_information {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::from(*value));
                    tlvs.push(writer.into_tlv(32));
                }
                if let Some(value) = &self.network_reject_information {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(33));
                }
                Ok(tlvs)
            }
            pub fn from_tlvs(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                let mut out = Self::default();
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 16) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u8()? != 0;
                    out.system_selection_preference = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 18) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u8()? != 0;
                    out.ddtm_events = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 19) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u8()? != 0;
                    out.serving_system_events = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 20) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u8()? != 0;
                    out.dual_standby_preference = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 21) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u8()? != 0;
                    out.subscription_info = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 23) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u8()? != 0;
                    out.network_time = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 24) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u8()? != 0;
                    out.system_info = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 25) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u8()? != 0;
                    out.signal_info = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 26) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u8()? != 0;
                    out.error_rate = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 27) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u8()? != 0;
                    out.hdr_new_uati_assigned = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 28) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u8()? != 0;
                    out.hdr_session_closed = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 29) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u8()? != 0;
                    out.managed_roaming = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 30) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u8()? != 0;
                    out.current_plmn_name = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 31) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u8()? != 0;
                    out.embms_status = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 32) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u8()? != 0;
                    out.rf_band_information = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 33) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = NetworkRejectInformation::decode_from(&mut reader)?;
                    out.network_reject_information = Some(value);
                }
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Response {}
        impl Response {
            pub const TLV_RESULT: u8 = 2;
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                Ok(Vec::new())
            }
            pub fn from_tlvs(_tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Ok(Self::default())
            }
        }
        impl crate::Response for Response {
            type Request = Request;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = crate::result::success_tlvs();
                tlvs.extend(self.to_tlvs()?);
                Ok(tlvs)
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                crate::result::check_result(tlvs)?;
                Response::from_tlvs(tlvs)
            }
        }
        impl crate::Request for Request {
            const SERVICE: u16 = super::super::SERVICE_ID;
            const MESSAGE_ID: u16 = 3;
            type Response = Response;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                self.to_tlvs()
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Self::from_tlvs(tlvs)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct NetworkRejectInformation {
            pub enable_network_reject_indications: bool,
            pub supress_system_info_indications: bool,
        }
        impl NetworkRejectInformation {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(u8::from(*&self.enable_network_reject_indications));
                writer.write_u8(u8::from(*&self.supress_system_info_indications));
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.enable_network_reject_indications = reader.read_u8()? != 0;
                out.supress_system_info_indications = reader.read_u8()? != 0;
                Ok(out)
            }
        }
    }
    #[doc = "Get Supported Messages"]
    pub mod get_supported_messages {
        use crate::{Endian, Error, TlvBuf, TlvReader, TlvWriter};
        use alloc::string::String;
        use alloc::vec::Vec;
        pub const MESSAGE_ID: u16 = 30;
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Request {}
        impl Request {
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                Ok(Vec::new())
            }
            pub fn from_tlvs(_tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Ok(Self::default())
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Response {
            pub list: Option<Vec<u8>>,
        }
        impl Response {
            pub const TLV_RESULT: u8 = 2;
            pub const TLV_LIST: u8 = 16;
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = Vec::new();
                if let Some(value) = &self.list {
                    let mut writer = TlvWriter::new();
                    writer.write_u16(
                        u16::try_from((value).len()).map_err(|_| Error::TooLong)?,
                        Endian::Little,
                    );
                    for item in value {
                        writer.write_u8(*item);
                    }
                    tlvs.push(writer.into_tlv(16));
                }
                Ok(tlvs)
            }
            pub fn from_tlvs(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                let mut out = Self::default();
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 16) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let count = reader.read_u16(Endian::Little)? as usize;
                    let mut items = Vec::with_capacity(count.min(256));
                    for _ in 0..count {
                        items.push(reader.read_u8()?);
                    }
                    let value = items;
                    out.list = Some(value);
                }
                Ok(out)
            }
        }
        impl crate::Response for Response {
            type Request = Request;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = crate::result::success_tlvs();
                tlvs.extend(self.to_tlvs()?);
                Ok(tlvs)
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                crate::result::check_result(tlvs)?;
                Response::from_tlvs(tlvs)
            }
        }
        impl crate::Request for Request {
            const SERVICE: u16 = super::super::SERVICE_ID;
            const MESSAGE_ID: u16 = 30;
            type Response = Response;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                self.to_tlvs()
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Self::from_tlvs(tlvs)
            }
        }
    }
    #[doc = "Get Signal Strength"]
    pub mod get_signal_strength {
        use crate::{Endian, Error, TlvBuf, TlvReader, TlvWriter};
        use alloc::string::String;
        use alloc::vec::Vec;
        pub const MESSAGE_ID: u16 = 32;
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Request {
            pub request_mask: Option<u16>,
        }
        impl Request {
            pub const TLV_REQUEST_MASK: u8 = 16;
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = Vec::new();
                if let Some(value) = &self.request_mask {
                    let mut writer = TlvWriter::new();
                    writer.write_u16(*value, Endian::Little);
                    tlvs.push(writer.into_tlv(16));
                }
                Ok(tlvs)
            }
            pub fn from_tlvs(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                let mut out = Self::default();
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 16) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u16(Endian::Little)?;
                    out.request_mask = Some(value);
                }
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Response {
            pub strength_list: Option<Vec<Element>>,
            pub rssi_list: Option<Vec<Element2>>,
            pub ecio_list: Option<Vec<Element3>>,
            pub io: Option<i32>,
            pub sinr: Option<u8>,
            pub error_rate_list: Option<Vec<Element4>>,
            pub rsrq: Option<Rsrq>,
            pub lte_snr: Option<i16>,
            pub lte_rsrp: Option<i16>,
            pub signal_strength: SignalStrength,
        }
        impl Response {
            pub const TLV_RESULT: u8 = 2;
            pub const TLV_STRENGTH_LIST: u8 = 16;
            pub const TLV_RSSI_LIST: u8 = 17;
            pub const TLV_ECIO_LIST: u8 = 18;
            pub const TLV_IO: u8 = 19;
            pub const TLV_SINR: u8 = 20;
            pub const TLV_ERROR_RATE_LIST: u8 = 21;
            pub const TLV_RSRQ: u8 = 22;
            pub const TLV_LTE_SNR: u8 = 23;
            pub const TLV_LTE_RSRP: u8 = 24;
            pub const TLV_SIGNAL_STRENGTH: u8 = 1;
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = Vec::new();
                if let Some(value) = &self.strength_list {
                    let mut writer = TlvWriter::new();
                    writer.write_u16(
                        u16::try_from((value).len()).map_err(|_| Error::TooLong)?,
                        Endian::Little,
                    );
                    for item in value {
                        (item).encode_to(&mut writer)?;
                    }
                    tlvs.push(writer.into_tlv(16));
                }
                if let Some(value) = &self.rssi_list {
                    let mut writer = TlvWriter::new();
                    writer.write_u16(
                        u16::try_from((value).len()).map_err(|_| Error::TooLong)?,
                        Endian::Little,
                    );
                    for item in value {
                        (item).encode_to(&mut writer)?;
                    }
                    tlvs.push(writer.into_tlv(17));
                }
                if let Some(value) = &self.ecio_list {
                    let mut writer = TlvWriter::new();
                    writer.write_u16(
                        u16::try_from((value).len()).map_err(|_| Error::TooLong)?,
                        Endian::Little,
                    );
                    for item in value {
                        (item).encode_to(&mut writer)?;
                    }
                    tlvs.push(writer.into_tlv(18));
                }
                if let Some(value) = &self.io {
                    let mut writer = TlvWriter::new();
                    writer.write_i32(*value, Endian::Little);
                    tlvs.push(writer.into_tlv(19));
                }
                if let Some(value) = &self.sinr {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(*value);
                    tlvs.push(writer.into_tlv(20));
                }
                if let Some(value) = &self.error_rate_list {
                    let mut writer = TlvWriter::new();
                    writer.write_u16(
                        u16::try_from((value).len()).map_err(|_| Error::TooLong)?,
                        Endian::Little,
                    );
                    for item in value {
                        (item).encode_to(&mut writer)?;
                    }
                    tlvs.push(writer.into_tlv(21));
                }
                if let Some(value) = &self.rsrq {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(22));
                }
                if let Some(value) = &self.lte_snr {
                    let mut writer = TlvWriter::new();
                    writer.write_i16(*value, Endian::Little);
                    tlvs.push(writer.into_tlv(23));
                }
                if let Some(value) = &self.lte_rsrp {
                    let mut writer = TlvWriter::new();
                    writer.write_i16(*value, Endian::Little);
                    tlvs.push(writer.into_tlv(24));
                }
                {
                    let mut writer = TlvWriter::new();
                    (&self.signal_strength).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(1));
                }
                Ok(tlvs)
            }
            pub fn from_tlvs(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                let mut out = Self::default();
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 16) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let count = reader.read_u16(Endian::Little)? as usize;
                    let mut items = Vec::with_capacity(count.min(256));
                    for _ in 0..count {
                        items.push(Element::decode_from(&mut reader)?);
                    }
                    let value = items;
                    out.strength_list = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 17) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let count = reader.read_u16(Endian::Little)? as usize;
                    let mut items = Vec::with_capacity(count.min(256));
                    for _ in 0..count {
                        items.push(Element2::decode_from(&mut reader)?);
                    }
                    let value = items;
                    out.rssi_list = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 18) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let count = reader.read_u16(Endian::Little)? as usize;
                    let mut items = Vec::with_capacity(count.min(256));
                    for _ in 0..count {
                        items.push(Element3::decode_from(&mut reader)?);
                    }
                    let value = items;
                    out.ecio_list = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 19) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_i32(Endian::Little)?;
                    out.io = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 20) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u8()?;
                    out.sinr = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 21) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let count = reader.read_u16(Endian::Little)? as usize;
                    let mut items = Vec::with_capacity(count.min(256));
                    for _ in 0..count {
                        items.push(Element4::decode_from(&mut reader)?);
                    }
                    let value = items;
                    out.error_rate_list = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 22) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = Rsrq::decode_from(&mut reader)?;
                    out.rsrq = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 23) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_i16(Endian::Little)?;
                    out.lte_snr = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 24) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_i16(Endian::Little)?;
                    out.lte_rsrp = Some(value);
                }
                let tlv = tlvs
                    .iter()
                    .find(|tlv| tlv.id == 1)
                    .ok_or(Error::MissingTlv { id: 1 })?;
                let mut reader = TlvReader::new(&tlv.value);
                out.signal_strength = SignalStrength::decode_from(&mut reader)?;
                Ok(out)
            }
        }
        impl crate::Response for Response {
            type Request = Request;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = crate::result::success_tlvs();
                tlvs.extend(self.to_tlvs()?);
                Ok(tlvs)
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                crate::result::check_result(tlvs)?;
                Response::from_tlvs(tlvs)
            }
        }
        impl crate::Request for Request {
            const SERVICE: u16 = super::super::SERVICE_ID;
            const MESSAGE_ID: u16 = 32;
            type Response = Response;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                self.to_tlvs()
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Self::from_tlvs(tlvs)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Element {
            pub strength: i8,
            pub radio_interface: i8,
        }
        impl Element {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_i8(*&self.strength);
                writer.write_i8(*&self.radio_interface);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.strength = reader.read_i8()?;
                out.radio_interface = reader.read_i8()?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Element2 {
            pub rssi: u8,
            pub radio_interface: i8,
        }
        impl Element2 {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(*&self.rssi);
                writer.write_i8(*&self.radio_interface);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.rssi = reader.read_u8()?;
                out.radio_interface = reader.read_i8()?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Element3 {
            pub ecio: u8,
            pub radio_interface: i8,
        }
        impl Element3 {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(*&self.ecio);
                writer.write_i8(*&self.radio_interface);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.ecio = reader.read_u8()?;
                out.radio_interface = reader.read_i8()?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Element4 {
            pub rate: u16,
            pub radio_interface: i8,
        }
        impl Element4 {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u16(*&self.rate, Endian::Little);
                writer.write_i8(*&self.radio_interface);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.rate = reader.read_u16(Endian::Little)?;
                out.radio_interface = reader.read_i8()?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Rsrq {
            pub rsrq: i8,
            pub radio_interface: i8,
        }
        impl Rsrq {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_i8(*&self.rsrq);
                writer.write_i8(*&self.radio_interface);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.rsrq = reader.read_i8()?;
                out.radio_interface = reader.read_i8()?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct SignalStrength {
            pub strength: i8,
            pub radio_interface: i8,
        }
        impl SignalStrength {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_i8(*&self.strength);
                writer.write_i8(*&self.radio_interface);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.strength = reader.read_i8()?;
                out.radio_interface = reader.read_i8()?;
                Ok(out)
            }
        }
    }
    #[doc = "Network Scan"]
    pub mod network_scan {
        use crate::{Endian, Error, TlvBuf, TlvReader, TlvWriter};
        use alloc::string::String;
        use alloc::vec::Vec;
        pub const MESSAGE_ID: u16 = 33;
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Request {
            pub network_type: Option<u8>,
        }
        impl Request {
            pub const TLV_NETWORK_TYPE: u8 = 16;
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = Vec::new();
                if let Some(value) = &self.network_type {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(*value);
                    tlvs.push(writer.into_tlv(16));
                }
                Ok(tlvs)
            }
            pub fn from_tlvs(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                let mut out = Self::default();
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 16) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u8()?;
                    out.network_type = Some(value);
                }
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Response {
            pub network_information: Option<Vec<Element>>,
            pub radio_access_technology: Option<Vec<Element2>>,
            pub mnc_pcs_digit_include_status: Option<Vec<Element3>>,
            pub network_scan_result: Option<u32>,
        }
        impl Response {
            pub const TLV_RESULT: u8 = 2;
            pub const TLV_NETWORK_INFORMATION: u8 = 16;
            pub const TLV_RADIO_ACCESS_TECHNOLOGY: u8 = 17;
            pub const TLV_MNC_PCS_DIGIT_INCLUDE_STATUS: u8 = 18;
            pub const TLV_NETWORK_SCAN_RESULT: u8 = 19;
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = Vec::new();
                if let Some(value) = &self.network_information {
                    let mut writer = TlvWriter::new();
                    writer.write_u16(
                        u16::try_from((value).len()).map_err(|_| Error::TooLong)?,
                        Endian::Little,
                    );
                    for item in value {
                        (item).encode_to(&mut writer)?;
                    }
                    tlvs.push(writer.into_tlv(16));
                }
                if let Some(value) = &self.radio_access_technology {
                    let mut writer = TlvWriter::new();
                    writer.write_u16(
                        u16::try_from((value).len()).map_err(|_| Error::TooLong)?,
                        Endian::Little,
                    );
                    for item in value {
                        (item).encode_to(&mut writer)?;
                    }
                    tlvs.push(writer.into_tlv(17));
                }
                if let Some(value) = &self.mnc_pcs_digit_include_status {
                    let mut writer = TlvWriter::new();
                    writer.write_u16(
                        u16::try_from((value).len()).map_err(|_| Error::TooLong)?,
                        Endian::Little,
                    );
                    for item in value {
                        (item).encode_to(&mut writer)?;
                    }
                    tlvs.push(writer.into_tlv(18));
                }
                if let Some(value) = &self.network_scan_result {
                    let mut writer = TlvWriter::new();
                    writer.write_u32(*value, Endian::Little);
                    tlvs.push(writer.into_tlv(19));
                }
                Ok(tlvs)
            }
            pub fn from_tlvs(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                let mut out = Self::default();
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 16) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let count = reader.read_u16(Endian::Little)? as usize;
                    let mut items = Vec::with_capacity(count.min(256));
                    for _ in 0..count {
                        items.push(Element::decode_from(&mut reader)?);
                    }
                    let value = items;
                    out.network_information = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 17) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let count = reader.read_u16(Endian::Little)? as usize;
                    let mut items = Vec::with_capacity(count.min(256));
                    for _ in 0..count {
                        items.push(Element2::decode_from(&mut reader)?);
                    }
                    let value = items;
                    out.radio_access_technology = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 18) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let count = reader.read_u16(Endian::Little)? as usize;
                    let mut items = Vec::with_capacity(count.min(256));
                    for _ in 0..count {
                        items.push(Element3::decode_from(&mut reader)?);
                    }
                    let value = items;
                    out.mnc_pcs_digit_include_status = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 19) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u32(Endian::Little)?;
                    out.network_scan_result = Some(value);
                }
                Ok(out)
            }
        }
        impl crate::Response for Response {
            type Request = Request;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = crate::result::success_tlvs();
                tlvs.extend(self.to_tlvs()?);
                Ok(tlvs)
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                crate::result::check_result(tlvs)?;
                Response::from_tlvs(tlvs)
            }
        }
        impl crate::Request for Request {
            const SERVICE: u16 = super::super::SERVICE_ID;
            const MESSAGE_ID: u16 = 33;
            type Response = Response;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                self.to_tlvs()
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Self::from_tlvs(tlvs)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Element {
            pub mcc: u16,
            pub mnc: u16,
            pub network_status: u8,
            pub description: String,
        }
        impl Element {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u16(*&self.mcc, Endian::Little);
                writer.write_u16(*&self.mnc, Endian::Little);
                writer.write_u8(*&self.network_status);
                writer.write_string(&self.description, 1, None)?;
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.mcc = reader.read_u16(Endian::Little)?;
                out.mnc = reader.read_u16(Endian::Little)?;
                out.network_status = reader.read_u8()?;
                out.description = reader.read_string(1, 0)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Element2 {
            pub mcc: u16,
            pub mnc: u16,
            pub radio_interface: i8,
        }
        impl Element2 {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u16(*&self.mcc, Endian::Little);
                writer.write_u16(*&self.mnc, Endian::Little);
                writer.write_i8(*&self.radio_interface);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.mcc = reader.read_u16(Endian::Little)?;
                out.mnc = reader.read_u16(Endian::Little)?;
                out.radio_interface = reader.read_i8()?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Element3 {
            pub mcc: u16,
            pub mnc: u16,
            pub includes_pcs_digit: bool,
        }
        impl Element3 {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u16(*&self.mcc, Endian::Little);
                writer.write_u16(*&self.mnc, Endian::Little);
                writer.write_u8(u8::from(*&self.includes_pcs_digit));
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.mcc = reader.read_u16(Endian::Little)?;
                out.mnc = reader.read_u16(Endian::Little)?;
                out.includes_pcs_digit = reader.read_u8()? != 0;
                Ok(out)
            }
        }
    }
    #[doc = "Initiate Network Register"]
    pub mod initiate_network_register {
        use crate::{Endian, Error, TlvBuf, TlvReader, TlvWriter};
        use alloc::string::String;
        use alloc::vec::Vec;
        pub const MESSAGE_ID: u16 = 34;
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Request {
            pub action: u8,
            pub manual_registration_info_3gpp: Option<ManualRegistrationInfo3gpp>,
            pub change_duration: Option<u8>,
            pub mnc_pcs_digit_include_status: Option<bool>,
        }
        impl Request {
            pub const TLV_ACTION: u8 = 1;
            pub const TLV_MANUAL_REGISTRATION_INFO_3GPP: u8 = 16;
            pub const TLV_CHANGE_DURATION: u8 = 17;
            pub const TLV_MNC_PCS_DIGIT_INCLUDE_STATUS: u8 = 18;
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = Vec::new();
                {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(*&self.action);
                    tlvs.push(writer.into_tlv(1));
                }
                if let Some(value) = &self.manual_registration_info_3gpp {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(16));
                }
                if let Some(value) = &self.change_duration {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(*value);
                    tlvs.push(writer.into_tlv(17));
                }
                if let Some(value) = &self.mnc_pcs_digit_include_status {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::from(*value));
                    tlvs.push(writer.into_tlv(18));
                }
                Ok(tlvs)
            }
            pub fn from_tlvs(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                let mut out = Self::default();
                let tlv = tlvs
                    .iter()
                    .find(|tlv| tlv.id == 1)
                    .ok_or(Error::MissingTlv { id: 1 })?;
                let mut reader = TlvReader::new(&tlv.value);
                out.action = reader.read_u8()?;
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 16) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = ManualRegistrationInfo3gpp::decode_from(&mut reader)?;
                    out.manual_registration_info_3gpp = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 17) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u8()?;
                    out.change_duration = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 18) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u8()? != 0;
                    out.mnc_pcs_digit_include_status = Some(value);
                }
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Response {}
        impl Response {
            pub const TLV_RESULT: u8 = 2;
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                Ok(Vec::new())
            }
            pub fn from_tlvs(_tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Ok(Self::default())
            }
        }
        impl crate::Response for Response {
            type Request = Request;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = crate::result::success_tlvs();
                tlvs.extend(self.to_tlvs()?);
                Ok(tlvs)
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                crate::result::check_result(tlvs)?;
                Response::from_tlvs(tlvs)
            }
        }
        impl crate::Request for Request {
            const SERVICE: u16 = super::super::SERVICE_ID;
            const MESSAGE_ID: u16 = 34;
            type Response = Response;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                self.to_tlvs()
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Self::from_tlvs(tlvs)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct ManualRegistrationInfo3gpp {
            pub mcc: u16,
            pub mnc: u16,
            pub radio_interface: i8,
        }
        impl ManualRegistrationInfo3gpp {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u16(*&self.mcc, Endian::Little);
                writer.write_u16(*&self.mnc, Endian::Little);
                writer.write_i8(*&self.radio_interface);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.mcc = reader.read_u16(Endian::Little)?;
                out.mnc = reader.read_u16(Endian::Little)?;
                out.radio_interface = reader.read_i8()?;
                Ok(out)
            }
        }
    }
    #[doc = "Attach Detach"]
    pub mod attach_detach {
        use crate::{Endian, Error, TlvBuf, TlvReader, TlvWriter};
        use alloc::string::String;
        use alloc::vec::Vec;
        pub const MESSAGE_ID: u16 = 35;
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Request {
            pub action: u8,
        }
        impl Request {
            pub const TLV_ACTION: u8 = 16;
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = Vec::new();
                {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(*&self.action);
                    tlvs.push(writer.into_tlv(16));
                }
                Ok(tlvs)
            }
            pub fn from_tlvs(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                let mut out = Self::default();
                let tlv = tlvs
                    .iter()
                    .find(|tlv| tlv.id == 16)
                    .ok_or(Error::MissingTlv { id: 16 })?;
                let mut reader = TlvReader::new(&tlv.value);
                out.action = reader.read_u8()?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Response {}
        impl Response {
            pub const TLV_RESULT: u8 = 2;
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                Ok(Vec::new())
            }
            pub fn from_tlvs(_tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Ok(Self::default())
            }
        }
        impl crate::Response for Response {
            type Request = Request;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = crate::result::success_tlvs();
                tlvs.extend(self.to_tlvs()?);
                Ok(tlvs)
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                crate::result::check_result(tlvs)?;
                Response::from_tlvs(tlvs)
            }
        }
        impl crate::Request for Request {
            const SERVICE: u16 = super::super::SERVICE_ID;
            const MESSAGE_ID: u16 = 35;
            type Response = Response;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                self.to_tlvs()
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Self::from_tlvs(tlvs)
            }
        }
    }
    #[doc = "Get Serving System"]
    pub mod get_serving_system {
        use crate::{Endian, Error, TlvBuf, TlvReader, TlvWriter};
        use alloc::string::String;
        use alloc::vec::Vec;
        pub const MESSAGE_ID: u16 = 36;
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Request {}
        impl Request {
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                Ok(Vec::new())
            }
            pub fn from_tlvs(_tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Ok(Self::default())
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Response {
            pub roaming_indicator: Option<u8>,
            pub data_service_capability: Option<Vec<u8>>,
            pub current_plmn: Option<CurrentPlmn>,
            pub cdma_system_id: Option<CdmaSystemId>,
            pub cdma_base_station_info: Option<CdmaBaseStationInfo>,
            pub roaming_indicator_list: Option<Vec<Element>>,
            pub default_roaming_indicator: Option<u8>,
            pub time_zone_3gpp2: Option<TimeZone3gpp2>,
            pub cdma_p_rev: Option<u8>,
            pub time_zone_3gpp: Option<i8>,
            pub daylight_saving_time_adjustment_3gpp: Option<u8>,
            pub lac_3gpp: Option<u16>,
            pub cid_3gpp: Option<u32>,
            pub concurrent_service_info_3gpp2: Option<bool>,
            pub prl_indicator_3gpp2: Option<bool>,
            pub dtm_support: Option<bool>,
            pub detailed_service_status: Option<DetailedServiceStatus>,
            pub cdma_system_info: Option<CdmaSystemInfo>,
            pub hdr_personality: Option<u8>,
            pub lte_tac: Option<u16>,
            pub call_barring_status: Option<CallBarringStatus>,
            pub umts_primary_scrambling_code: Option<u16>,
            pub mnc_pcs_digit_include_status: Option<MncPcsDigitIncludeStatus>,
            pub network_name_source: Option<u32>,
            pub serving_system: ServingSystem,
        }
        impl Response {
            pub const TLV_RESULT: u8 = 2;
            pub const TLV_ROAMING_INDICATOR: u8 = 16;
            pub const TLV_DATA_SERVICE_CAPABILITY: u8 = 17;
            pub const TLV_CURRENT_PLMN: u8 = 18;
            pub const TLV_CDMA_SYSTEM_ID: u8 = 19;
            pub const TLV_CDMA_BASE_STATION_INFO: u8 = 20;
            pub const TLV_ROAMING_INDICATOR_LIST: u8 = 21;
            pub const TLV_DEFAULT_ROAMING_INDICATOR: u8 = 22;
            pub const TLV_TIME_ZONE_3GPP2: u8 = 23;
            pub const TLV_CDMA_P_REV: u8 = 24;
            pub const TLV_TIME_ZONE_3GPP: u8 = 26;
            pub const TLV_DAYLIGHT_SAVING_TIME_ADJUSTMENT_3GPP: u8 = 27;
            pub const TLV_LAC_3GPP: u8 = 28;
            pub const TLV_CID_3GPP: u8 = 29;
            pub const TLV_CONCURRENT_SERVICE_INFO_3GPP2: u8 = 30;
            pub const TLV_PRL_INDICATOR_3GPP2: u8 = 31;
            pub const TLV_DTM_SUPPORT: u8 = 32;
            pub const TLV_DETAILED_SERVICE_STATUS: u8 = 33;
            pub const TLV_CDMA_SYSTEM_INFO: u8 = 34;
            pub const TLV_HDR_PERSONALITY: u8 = 35;
            pub const TLV_LTE_TAC: u8 = 36;
            pub const TLV_CALL_BARRING_STATUS: u8 = 37;
            pub const TLV_UMTS_PRIMARY_SCRAMBLING_CODE: u8 = 38;
            pub const TLV_MNC_PCS_DIGIT_INCLUDE_STATUS: u8 = 39;
            pub const TLV_NETWORK_NAME_SOURCE: u8 = 41;
            pub const TLV_SERVING_SYSTEM: u8 = 1;
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = Vec::new();
                if let Some(value) = &self.roaming_indicator {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(*value);
                    tlvs.push(writer.into_tlv(16));
                }
                if let Some(value) = &self.data_service_capability {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::try_from((value).len()).map_err(|_| Error::TooLong)?);
                    for item in value {
                        writer.write_u8(*item);
                    }
                    tlvs.push(writer.into_tlv(17));
                }
                if let Some(value) = &self.current_plmn {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(18));
                }
                if let Some(value) = &self.cdma_system_id {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(19));
                }
                if let Some(value) = &self.cdma_base_station_info {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(20));
                }
                if let Some(value) = &self.roaming_indicator_list {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::try_from((value).len()).map_err(|_| Error::TooLong)?);
                    for item in value {
                        (item).encode_to(&mut writer)?;
                    }
                    tlvs.push(writer.into_tlv(21));
                }
                if let Some(value) = &self.default_roaming_indicator {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(*value);
                    tlvs.push(writer.into_tlv(22));
                }
                if let Some(value) = &self.time_zone_3gpp2 {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(23));
                }
                if let Some(value) = &self.cdma_p_rev {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(*value);
                    tlvs.push(writer.into_tlv(24));
                }
                if let Some(value) = &self.time_zone_3gpp {
                    let mut writer = TlvWriter::new();
                    writer.write_i8(*value);
                    tlvs.push(writer.into_tlv(26));
                }
                if let Some(value) = &self.daylight_saving_time_adjustment_3gpp {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(*value);
                    tlvs.push(writer.into_tlv(27));
                }
                if let Some(value) = &self.lac_3gpp {
                    let mut writer = TlvWriter::new();
                    writer.write_u16(*value, Endian::Little);
                    tlvs.push(writer.into_tlv(28));
                }
                if let Some(value) = &self.cid_3gpp {
                    let mut writer = TlvWriter::new();
                    writer.write_u32(*value, Endian::Little);
                    tlvs.push(writer.into_tlv(29));
                }
                if let Some(value) = &self.concurrent_service_info_3gpp2 {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::from(*value));
                    tlvs.push(writer.into_tlv(30));
                }
                if let Some(value) = &self.prl_indicator_3gpp2 {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::from(*value));
                    tlvs.push(writer.into_tlv(31));
                }
                if let Some(value) = &self.dtm_support {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::from(*value));
                    tlvs.push(writer.into_tlv(32));
                }
                if let Some(value) = &self.detailed_service_status {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(33));
                }
                if let Some(value) = &self.cdma_system_info {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(34));
                }
                if let Some(value) = &self.hdr_personality {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(*value);
                    tlvs.push(writer.into_tlv(35));
                }
                if let Some(value) = &self.lte_tac {
                    let mut writer = TlvWriter::new();
                    writer.write_u16(*value, Endian::Little);
                    tlvs.push(writer.into_tlv(36));
                }
                if let Some(value) = &self.call_barring_status {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(37));
                }
                if let Some(value) = &self.umts_primary_scrambling_code {
                    let mut writer = TlvWriter::new();
                    writer.write_u16(*value, Endian::Little);
                    tlvs.push(writer.into_tlv(38));
                }
                if let Some(value) = &self.mnc_pcs_digit_include_status {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(39));
                }
                if let Some(value) = &self.network_name_source {
                    let mut writer = TlvWriter::new();
                    writer.write_u32(*value, Endian::Little);
                    tlvs.push(writer.into_tlv(41));
                }
                {
                    let mut writer = TlvWriter::new();
                    (&self.serving_system).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(1));
                }
                Ok(tlvs)
            }
            pub fn from_tlvs(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                let mut out = Self::default();
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 16) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u8()?;
                    out.roaming_indicator = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 17) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let count = reader.read_u8()? as usize;
                    let mut items = Vec::with_capacity(count.min(256));
                    for _ in 0..count {
                        items.push(reader.read_u8()?);
                    }
                    let value = items;
                    out.data_service_capability = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 18) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = CurrentPlmn::decode_from(&mut reader)?;
                    out.current_plmn = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 19) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = CdmaSystemId::decode_from(&mut reader)?;
                    out.cdma_system_id = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 20) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = CdmaBaseStationInfo::decode_from(&mut reader)?;
                    out.cdma_base_station_info = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 21) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let count = reader.read_u8()? as usize;
                    let mut items = Vec::with_capacity(count.min(256));
                    for _ in 0..count {
                        items.push(Element::decode_from(&mut reader)?);
                    }
                    let value = items;
                    out.roaming_indicator_list = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 22) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u8()?;
                    out.default_roaming_indicator = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 23) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = TimeZone3gpp2::decode_from(&mut reader)?;
                    out.time_zone_3gpp2 = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 24) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u8()?;
                    out.cdma_p_rev = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 26) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_i8()?;
                    out.time_zone_3gpp = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 27) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u8()?;
                    out.daylight_saving_time_adjustment_3gpp = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 28) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u16(Endian::Little)?;
                    out.lac_3gpp = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 29) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u32(Endian::Little)?;
                    out.cid_3gpp = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 30) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u8()? != 0;
                    out.concurrent_service_info_3gpp2 = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 31) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u8()? != 0;
                    out.prl_indicator_3gpp2 = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 32) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u8()? != 0;
                    out.dtm_support = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 33) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = DetailedServiceStatus::decode_from(&mut reader)?;
                    out.detailed_service_status = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 34) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = CdmaSystemInfo::decode_from(&mut reader)?;
                    out.cdma_system_info = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 35) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u8()?;
                    out.hdr_personality = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 36) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u16(Endian::Little)?;
                    out.lte_tac = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 37) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = CallBarringStatus::decode_from(&mut reader)?;
                    out.call_barring_status = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 38) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u16(Endian::Little)?;
                    out.umts_primary_scrambling_code = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 39) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = MncPcsDigitIncludeStatus::decode_from(&mut reader)?;
                    out.mnc_pcs_digit_include_status = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 41) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u32(Endian::Little)?;
                    out.network_name_source = Some(value);
                }
                let tlv = tlvs
                    .iter()
                    .find(|tlv| tlv.id == 1)
                    .ok_or(Error::MissingTlv { id: 1 })?;
                let mut reader = TlvReader::new(&tlv.value);
                out.serving_system = ServingSystem::decode_from(&mut reader)?;
                Ok(out)
            }
        }
        impl crate::Response for Response {
            type Request = Request;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = crate::result::success_tlvs();
                tlvs.extend(self.to_tlvs()?);
                Ok(tlvs)
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                crate::result::check_result(tlvs)?;
                Response::from_tlvs(tlvs)
            }
        }
        impl crate::Request for Request {
            const SERVICE: u16 = super::super::SERVICE_ID;
            const MESSAGE_ID: u16 = 36;
            type Response = Response;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                self.to_tlvs()
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Self::from_tlvs(tlvs)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct CurrentPlmn {
            pub mcc: u16,
            pub mnc: u16,
            pub description: String,
        }
        impl CurrentPlmn {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u16(*&self.mcc, Endian::Little);
                writer.write_u16(*&self.mnc, Endian::Little);
                writer.write_string(&self.description, 1, None)?;
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.mcc = reader.read_u16(Endian::Little)?;
                out.mnc = reader.read_u16(Endian::Little)?;
                out.description = reader.read_string(1, 0)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct CdmaSystemId {
            pub sid: u16,
            pub nid: u16,
        }
        impl CdmaSystemId {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u16(*&self.sid, Endian::Little);
                writer.write_u16(*&self.nid, Endian::Little);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.sid = reader.read_u16(Endian::Little)?;
                out.nid = reader.read_u16(Endian::Little)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct CdmaBaseStationInfo {
            pub base_station_id: u16,
            pub base_station_latitude: i32,
            pub base_station_longitude: i32,
        }
        impl CdmaBaseStationInfo {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u16(*&self.base_station_id, Endian::Little);
                writer.write_i32(*&self.base_station_latitude, Endian::Little);
                writer.write_i32(*&self.base_station_longitude, Endian::Little);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.base_station_id = reader.read_u16(Endian::Little)?;
                out.base_station_latitude = reader.read_i32(Endian::Little)?;
                out.base_station_longitude = reader.read_i32(Endian::Little)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Element {
            pub radio_interface: u8,
            pub roaming_indicator: u8,
        }
        impl Element {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(*&self.radio_interface);
                writer.write_u8(*&self.roaming_indicator);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.radio_interface = reader.read_u8()?;
                out.roaming_indicator = reader.read_u8()?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct TimeZone3gpp2 {
            pub leap_seconds: u8,
            pub local_time_offset: i8,
            pub daylight_saving_time: bool,
        }
        impl TimeZone3gpp2 {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(*&self.leap_seconds);
                writer.write_i8(*&self.local_time_offset);
                writer.write_u8(u8::from(*&self.daylight_saving_time));
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.leap_seconds = reader.read_u8()?;
                out.local_time_offset = reader.read_i8()?;
                out.daylight_saving_time = reader.read_u8()? != 0;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct DetailedServiceStatus {
            pub status: u8,
            pub capability: u8,
            pub hdr_status: u8,
            pub hdr_hybrid: bool,
            pub forbidden: bool,
        }
        impl DetailedServiceStatus {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(*&self.status);
                writer.write_u8(*&self.capability);
                writer.write_u8(*&self.hdr_status);
                writer.write_u8(u8::from(*&self.hdr_hybrid));
                writer.write_u8(u8::from(*&self.forbidden));
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.status = reader.read_u8()?;
                out.capability = reader.read_u8()?;
                out.hdr_status = reader.read_u8()?;
                out.hdr_hybrid = reader.read_u8()? != 0;
                out.forbidden = reader.read_u8()? != 0;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct CdmaSystemInfo {
            pub mcc: u16,
            pub imsi_11_12: u8,
        }
        impl CdmaSystemInfo {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u16(*&self.mcc, Endian::Little);
                writer.write_u8(*&self.imsi_11_12);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.mcc = reader.read_u16(Endian::Little)?;
                out.imsi_11_12 = reader.read_u8()?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct CallBarringStatus {
            pub cs_status: i32,
            pub ps_status: i32,
        }
        impl CallBarringStatus {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_i32(*&self.cs_status, Endian::Little);
                writer.write_i32(*&self.ps_status, Endian::Little);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.cs_status = reader.read_i32(Endian::Little)?;
                out.ps_status = reader.read_i32(Endian::Little)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct MncPcsDigitIncludeStatus {
            pub mcc: u16,
            pub mnc: u16,
            pub includes_pcs_digit: bool,
        }
        impl MncPcsDigitIncludeStatus {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u16(*&self.mcc, Endian::Little);
                writer.write_u16(*&self.mnc, Endian::Little);
                writer.write_u8(u8::from(*&self.includes_pcs_digit));
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.mcc = reader.read_u16(Endian::Little)?;
                out.mnc = reader.read_u16(Endian::Little)?;
                out.includes_pcs_digit = reader.read_u8()? != 0;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct ServingSystem {
            pub registration_state: u8,
            pub cs_attach_state: u8,
            pub ps_attach_state: u8,
            pub selected_network: u8,
            pub radio_interfaces: Vec<i8>,
        }
        impl ServingSystem {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(*&self.registration_state);
                writer.write_u8(*&self.cs_attach_state);
                writer.write_u8(*&self.ps_attach_state);
                writer.write_u8(*&self.selected_network);
                writer.write_u8(
                    u8::try_from((&self.radio_interfaces).len()).map_err(|_| Error::TooLong)?,
                );
                for item in &self.radio_interfaces {
                    writer.write_i8(*item);
                }
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.registration_state = reader.read_u8()?;
                out.cs_attach_state = reader.read_u8()?;
                out.ps_attach_state = reader.read_u8()?;
                out.selected_network = reader.read_u8()?;
                let count = reader.read_u8()? as usize;
                let mut items = Vec::with_capacity(count.min(256));
                for _ in 0..count {
                    items.push(reader.read_i8()?);
                }
                out.radio_interfaces = items;
                Ok(out)
            }
        }
    }
    #[doc = "Get Home Network"]
    pub mod get_home_network {
        use crate::{Endian, Error, TlvBuf, TlvReader, TlvWriter};
        use alloc::string::String;
        use alloc::vec::Vec;
        pub const MESSAGE_ID: u16 = 37;
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Request {}
        impl Request {
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                Ok(Vec::new())
            }
            pub fn from_tlvs(_tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Ok(Self::default())
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Response {
            pub home_system_id: Option<HomeSystemId>,
            pub home_network_3gpp2_ext: Option<HomeNetwork3gpp2Ext>,
            pub home_network_3gpp_mnc: Option<HomeNetwork3gppMnc>,
            pub network_name_source: Option<u32>,
            pub home_network: HomeNetwork,
        }
        impl Response {
            pub const TLV_RESULT: u8 = 2;
            pub const TLV_HOME_SYSTEM_ID: u8 = 16;
            pub const TLV_HOME_NETWORK_3GPP2_EXT: u8 = 17;
            pub const TLV_HOME_NETWORK_3GPP_MNC: u8 = 18;
            pub const TLV_NETWORK_NAME_SOURCE: u8 = 19;
            pub const TLV_HOME_NETWORK: u8 = 1;
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = Vec::new();
                if let Some(value) = &self.home_system_id {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(16));
                }
                if let Some(value) = &self.home_network_3gpp2_ext {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(17));
                }
                if let Some(value) = &self.home_network_3gpp_mnc {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(18));
                }
                if let Some(value) = &self.network_name_source {
                    let mut writer = TlvWriter::new();
                    writer.write_u32(*value, Endian::Little);
                    tlvs.push(writer.into_tlv(19));
                }
                {
                    let mut writer = TlvWriter::new();
                    (&self.home_network).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(1));
                }
                Ok(tlvs)
            }
            pub fn from_tlvs(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                let mut out = Self::default();
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 16) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = HomeSystemId::decode_from(&mut reader)?;
                    out.home_system_id = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 17) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = HomeNetwork3gpp2Ext::decode_from(&mut reader)?;
                    out.home_network_3gpp2_ext = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 18) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = HomeNetwork3gppMnc::decode_from(&mut reader)?;
                    out.home_network_3gpp_mnc = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 19) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u32(Endian::Little)?;
                    out.network_name_source = Some(value);
                }
                let tlv = tlvs
                    .iter()
                    .find(|tlv| tlv.id == 1)
                    .ok_or(Error::MissingTlv { id: 1 })?;
                let mut reader = TlvReader::new(&tlv.value);
                out.home_network = HomeNetwork::decode_from(&mut reader)?;
                Ok(out)
            }
        }
        impl crate::Response for Response {
            type Request = Request;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = crate::result::success_tlvs();
                tlvs.extend(self.to_tlvs()?);
                Ok(tlvs)
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                crate::result::check_result(tlvs)?;
                Response::from_tlvs(tlvs)
            }
        }
        impl crate::Request for Request {
            const SERVICE: u16 = super::super::SERVICE_ID;
            const MESSAGE_ID: u16 = 37;
            type Response = Response;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                self.to_tlvs()
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Self::from_tlvs(tlvs)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct HomeSystemId {
            pub sid: u16,
            pub nid: u16,
        }
        impl HomeSystemId {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u16(*&self.sid, Endian::Little);
                writer.write_u16(*&self.nid, Endian::Little);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.sid = reader.read_u16(Endian::Little)?;
                out.nid = reader.read_u16(Endian::Little)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct HomeNetwork3gpp2Ext {
            pub mcc: u16,
            pub mnc: u16,
            pub display_description: u8,
            pub description_encoding: u8,
            pub description: Vec<u8>,
        }
        impl HomeNetwork3gpp2Ext {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u16(*&self.mcc, Endian::Little);
                writer.write_u16(*&self.mnc, Endian::Little);
                writer.write_u8(*&self.display_description);
                writer.write_u8(*&self.description_encoding);
                writer
                    .write_u8(u8::try_from((&self.description).len()).map_err(|_| Error::TooLong)?);
                for item in &self.description {
                    writer.write_u8(*item);
                }
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.mcc = reader.read_u16(Endian::Little)?;
                out.mnc = reader.read_u16(Endian::Little)?;
                out.display_description = reader.read_u8()?;
                out.description_encoding = reader.read_u8()?;
                let count = reader.read_u8()? as usize;
                let mut items = Vec::with_capacity(count.min(256));
                for _ in 0..count {
                    items.push(reader.read_u8()?);
                }
                out.description = items;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct HomeNetwork3gppMnc {
            pub is_3gpp: bool,
            pub includes_pcs_digit: bool,
        }
        impl HomeNetwork3gppMnc {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(u8::from(*&self.is_3gpp));
                writer.write_u8(u8::from(*&self.includes_pcs_digit));
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.is_3gpp = reader.read_u8()? != 0;
                out.includes_pcs_digit = reader.read_u8()? != 0;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct HomeNetwork {
            pub mcc: u16,
            pub mnc: u16,
            pub description: String,
        }
        impl HomeNetwork {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u16(*&self.mcc, Endian::Little);
                writer.write_u16(*&self.mnc, Endian::Little);
                writer.write_string(&self.description, 1, None)?;
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.mcc = reader.read_u16(Endian::Little)?;
                out.mnc = reader.read_u16(Endian::Little)?;
                out.description = reader.read_string(1, 0)?;
                Ok(out)
            }
        }
    }
    #[doc = "Get Preferred Networks"]
    pub mod get_preferred_networks {
        use crate::{Endian, Error, TlvBuf, TlvReader, TlvWriter};
        use alloc::string::String;
        use alloc::vec::Vec;
        pub const MESSAGE_ID: u16 = 38;
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Request {}
        impl Request {
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                Ok(Vec::new())
            }
            pub fn from_tlvs(_tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Ok(Self::default())
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Response {
            pub static_mnc_pcs_digit_include_status: Option<Vec<Element>>,
            pub preferred_networks: Option<Vec<Element2>>,
            pub static_preferred_networks: Option<Vec<Element3>>,
            pub mnc_pcs_digit_include_status: Option<Vec<Element4>>,
        }
        impl Response {
            pub const TLV_RESULT: u8 = 2;
            pub const TLV_STATIC_MNC_PCS_DIGIT_INCLUDE_STATUS: u8 = 19;
            pub const TLV_PREFERRED_NETWORKS: u8 = 16;
            pub const TLV_STATIC_PREFERRED_NETWORKS: u8 = 17;
            pub const TLV_MNC_PCS_DIGIT_INCLUDE_STATUS: u8 = 18;
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = Vec::new();
                if let Some(value) = &self.static_mnc_pcs_digit_include_status {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::try_from((value).len()).map_err(|_| Error::TooLong)?);
                    for item in value {
                        (item).encode_to(&mut writer)?;
                    }
                    tlvs.push(writer.into_tlv(19));
                }
                if let Some(value) = &self.preferred_networks {
                    let mut writer = TlvWriter::new();
                    writer.write_u16(
                        u16::try_from((value).len()).map_err(|_| Error::TooLong)?,
                        Endian::Little,
                    );
                    for item in value {
                        (item).encode_to(&mut writer)?;
                    }
                    tlvs.push(writer.into_tlv(16));
                }
                if let Some(value) = &self.static_preferred_networks {
                    let mut writer = TlvWriter::new();
                    writer.write_u16(
                        u16::try_from((value).len()).map_err(|_| Error::TooLong)?,
                        Endian::Little,
                    );
                    for item in value {
                        (item).encode_to(&mut writer)?;
                    }
                    tlvs.push(writer.into_tlv(17));
                }
                if let Some(value) = &self.mnc_pcs_digit_include_status {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::try_from((value).len()).map_err(|_| Error::TooLong)?);
                    for item in value {
                        (item).encode_to(&mut writer)?;
                    }
                    tlvs.push(writer.into_tlv(18));
                }
                Ok(tlvs)
            }
            pub fn from_tlvs(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                let mut out = Self::default();
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 19) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let count = reader.read_u8()? as usize;
                    let mut items = Vec::with_capacity(count.min(256));
                    for _ in 0..count {
                        items.push(Element::decode_from(&mut reader)?);
                    }
                    let value = items;
                    out.static_mnc_pcs_digit_include_status = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 16) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let count = reader.read_u16(Endian::Little)? as usize;
                    let mut items = Vec::with_capacity(count.min(256));
                    for _ in 0..count {
                        items.push(Element2::decode_from(&mut reader)?);
                    }
                    let value = items;
                    out.preferred_networks = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 17) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let count = reader.read_u16(Endian::Little)? as usize;
                    let mut items = Vec::with_capacity(count.min(256));
                    for _ in 0..count {
                        items.push(Element3::decode_from(&mut reader)?);
                    }
                    let value = items;
                    out.static_preferred_networks = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 18) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let count = reader.read_u8()? as usize;
                    let mut items = Vec::with_capacity(count.min(256));
                    for _ in 0..count {
                        items.push(Element4::decode_from(&mut reader)?);
                    }
                    let value = items;
                    out.mnc_pcs_digit_include_status = Some(value);
                }
                Ok(out)
            }
        }
        impl crate::Response for Response {
            type Request = Request;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = crate::result::success_tlvs();
                tlvs.extend(self.to_tlvs()?);
                Ok(tlvs)
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                crate::result::check_result(tlvs)?;
                Response::from_tlvs(tlvs)
            }
        }
        impl crate::Request for Request {
            const SERVICE: u16 = super::super::SERVICE_ID;
            const MESSAGE_ID: u16 = 38;
            type Response = Response;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                self.to_tlvs()
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Self::from_tlvs(tlvs)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Element {
            pub mcc: u16,
            pub mnc: u16,
            pub includes_pcs_digit: bool,
        }
        impl Element {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u16(*&self.mcc, Endian::Little);
                writer.write_u16(*&self.mnc, Endian::Little);
                writer.write_u8(u8::from(*&self.includes_pcs_digit));
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.mcc = reader.read_u16(Endian::Little)?;
                out.mnc = reader.read_u16(Endian::Little)?;
                out.includes_pcs_digit = reader.read_u8()? != 0;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Element2 {
            pub mcc: u16,
            pub mnc: u16,
            pub radio_access_technology: u16,
        }
        impl Element2 {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u16(*&self.mcc, Endian::Little);
                writer.write_u16(*&self.mnc, Endian::Little);
                writer.write_u16(*&self.radio_access_technology, Endian::Little);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.mcc = reader.read_u16(Endian::Little)?;
                out.mnc = reader.read_u16(Endian::Little)?;
                out.radio_access_technology = reader.read_u16(Endian::Little)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Element3 {
            pub mcc: u16,
            pub mnc: u16,
            pub radio_access_technology: u16,
        }
        impl Element3 {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u16(*&self.mcc, Endian::Little);
                writer.write_u16(*&self.mnc, Endian::Little);
                writer.write_u16(*&self.radio_access_technology, Endian::Little);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.mcc = reader.read_u16(Endian::Little)?;
                out.mnc = reader.read_u16(Endian::Little)?;
                out.radio_access_technology = reader.read_u16(Endian::Little)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Element4 {
            pub mcc: u16,
            pub mnc: u16,
            pub includes_pcs_digit: bool,
        }
        impl Element4 {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u16(*&self.mcc, Endian::Little);
                writer.write_u16(*&self.mnc, Endian::Little);
                writer.write_u8(u8::from(*&self.includes_pcs_digit));
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.mcc = reader.read_u16(Endian::Little)?;
                out.mnc = reader.read_u16(Endian::Little)?;
                out.includes_pcs_digit = reader.read_u8()? != 0;
                Ok(out)
            }
        }
    }
    #[doc = "Set Preferred Networks"]
    pub mod set_preferred_networks {
        use crate::{Endian, Error, TlvBuf, TlvReader, TlvWriter};
        use alloc::string::String;
        use alloc::vec::Vec;
        pub const MESSAGE_ID: u16 = 39;
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Request {
            pub preferred_networks: Option<Vec<Element>>,
            pub mnc_pcs_digit_include_status: Option<Vec<Element2>>,
            pub clear_previous_preferred_networks: Option<bool>,
        }
        impl Request {
            pub const TLV_PREFERRED_NETWORKS: u8 = 16;
            pub const TLV_MNC_PCS_DIGIT_INCLUDE_STATUS: u8 = 17;
            pub const TLV_CLEAR_PREVIOUS_PREFERRED_NETWORKS: u8 = 18;
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = Vec::new();
                if let Some(value) = &self.preferred_networks {
                    let mut writer = TlvWriter::new();
                    writer.write_u16(
                        u16::try_from((value).len()).map_err(|_| Error::TooLong)?,
                        Endian::Little,
                    );
                    for item in value {
                        (item).encode_to(&mut writer)?;
                    }
                    tlvs.push(writer.into_tlv(16));
                }
                if let Some(value) = &self.mnc_pcs_digit_include_status {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::try_from((value).len()).map_err(|_| Error::TooLong)?);
                    for item in value {
                        (item).encode_to(&mut writer)?;
                    }
                    tlvs.push(writer.into_tlv(17));
                }
                if let Some(value) = &self.clear_previous_preferred_networks {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::from(*value));
                    tlvs.push(writer.into_tlv(18));
                }
                Ok(tlvs)
            }
            pub fn from_tlvs(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                let mut out = Self::default();
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 16) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let count = reader.read_u16(Endian::Little)? as usize;
                    let mut items = Vec::with_capacity(count.min(256));
                    for _ in 0..count {
                        items.push(Element::decode_from(&mut reader)?);
                    }
                    let value = items;
                    out.preferred_networks = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 17) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let count = reader.read_u8()? as usize;
                    let mut items = Vec::with_capacity(count.min(256));
                    for _ in 0..count {
                        items.push(Element2::decode_from(&mut reader)?);
                    }
                    let value = items;
                    out.mnc_pcs_digit_include_status = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 18) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u8()? != 0;
                    out.clear_previous_preferred_networks = Some(value);
                }
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Response {}
        impl Response {
            pub const TLV_RESULT: u8 = 2;
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                Ok(Vec::new())
            }
            pub fn from_tlvs(_tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Ok(Self::default())
            }
        }
        impl crate::Response for Response {
            type Request = Request;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = crate::result::success_tlvs();
                tlvs.extend(self.to_tlvs()?);
                Ok(tlvs)
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                crate::result::check_result(tlvs)?;
                Response::from_tlvs(tlvs)
            }
        }
        impl crate::Request for Request {
            const SERVICE: u16 = super::super::SERVICE_ID;
            const MESSAGE_ID: u16 = 39;
            type Response = Response;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                self.to_tlvs()
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Self::from_tlvs(tlvs)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Element {
            pub mcc: u16,
            pub mnc: u16,
            pub radio_access_technology: u16,
        }
        impl Element {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u16(*&self.mcc, Endian::Little);
                writer.write_u16(*&self.mnc, Endian::Little);
                writer.write_u16(*&self.radio_access_technology, Endian::Little);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.mcc = reader.read_u16(Endian::Little)?;
                out.mnc = reader.read_u16(Endian::Little)?;
                out.radio_access_technology = reader.read_u16(Endian::Little)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Element2 {
            pub mcc: u16,
            pub mnc: u16,
            pub includes_pcs_digit: bool,
        }
        impl Element2 {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u16(*&self.mcc, Endian::Little);
                writer.write_u16(*&self.mnc, Endian::Little);
                writer.write_u8(u8::from(*&self.includes_pcs_digit));
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.mcc = reader.read_u16(Endian::Little)?;
                out.mnc = reader.read_u16(Endian::Little)?;
                out.includes_pcs_digit = reader.read_u8()? != 0;
                Ok(out)
            }
        }
    }
    #[doc = "Set Technology Preference"]
    pub mod set_technology_preference {
        use crate::{Endian, Error, TlvBuf, TlvReader, TlvWriter};
        use alloc::string::String;
        use alloc::vec::Vec;
        pub const MESSAGE_ID: u16 = 42;
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Request {
            pub current: Current,
        }
        impl Request {
            pub const TLV_CURRENT: u8 = 1;
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = Vec::new();
                {
                    let mut writer = TlvWriter::new();
                    (&self.current).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(1));
                }
                Ok(tlvs)
            }
            pub fn from_tlvs(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                let mut out = Self::default();
                let tlv = tlvs
                    .iter()
                    .find(|tlv| tlv.id == 1)
                    .ok_or(Error::MissingTlv { id: 1 })?;
                let mut reader = TlvReader::new(&tlv.value);
                out.current = Current::decode_from(&mut reader)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Response {}
        impl Response {
            pub const TLV_RESULT: u8 = 2;
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                Ok(Vec::new())
            }
            pub fn from_tlvs(_tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Ok(Self::default())
            }
        }
        impl crate::Response for Response {
            type Request = Request;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = crate::result::success_tlvs();
                tlvs.extend(self.to_tlvs()?);
                Ok(tlvs)
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                crate::result::check_result(tlvs)?;
                Response::from_tlvs(tlvs)
            }
        }
        impl crate::Request for Request {
            const SERVICE: u16 = super::super::SERVICE_ID;
            const MESSAGE_ID: u16 = 42;
            type Response = Response;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                self.to_tlvs()
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Self::from_tlvs(tlvs)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Current {
            pub technology_preference: u16,
            pub technology_preference_duration: u8,
        }
        impl Current {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u16(*&self.technology_preference, Endian::Little);
                writer.write_u8(*&self.technology_preference_duration);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.technology_preference = reader.read_u16(Endian::Little)?;
                out.technology_preference_duration = reader.read_u8()?;
                Ok(out)
            }
        }
    }
    #[doc = "Get Technology Preference"]
    pub mod get_technology_preference {
        use crate::{Endian, Error, TlvBuf, TlvReader, TlvWriter};
        use alloc::string::String;
        use alloc::vec::Vec;
        pub const MESSAGE_ID: u16 = 43;
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Request {}
        impl Request {
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                Ok(Vec::new())
            }
            pub fn from_tlvs(_tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Ok(Self::default())
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Response {
            pub persistent: Option<u16>,
            pub active: Active,
        }
        impl Response {
            pub const TLV_RESULT: u8 = 2;
            pub const TLV_PERSISTENT: u8 = 16;
            pub const TLV_ACTIVE: u8 = 1;
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = Vec::new();
                if let Some(value) = &self.persistent {
                    let mut writer = TlvWriter::new();
                    writer.write_u16(*value, Endian::Little);
                    tlvs.push(writer.into_tlv(16));
                }
                {
                    let mut writer = TlvWriter::new();
                    (&self.active).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(1));
                }
                Ok(tlvs)
            }
            pub fn from_tlvs(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                let mut out = Self::default();
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 16) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u16(Endian::Little)?;
                    out.persistent = Some(value);
                }
                let tlv = tlvs
                    .iter()
                    .find(|tlv| tlv.id == 1)
                    .ok_or(Error::MissingTlv { id: 1 })?;
                let mut reader = TlvReader::new(&tlv.value);
                out.active = Active::decode_from(&mut reader)?;
                Ok(out)
            }
        }
        impl crate::Response for Response {
            type Request = Request;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = crate::result::success_tlvs();
                tlvs.extend(self.to_tlvs()?);
                Ok(tlvs)
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                crate::result::check_result(tlvs)?;
                Response::from_tlvs(tlvs)
            }
        }
        impl crate::Request for Request {
            const SERVICE: u16 = super::super::SERVICE_ID;
            const MESSAGE_ID: u16 = 43;
            type Response = Response;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                self.to_tlvs()
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Self::from_tlvs(tlvs)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Active {
            pub technology_preference: u16,
            pub technology_preference_duration: u8,
        }
        impl Active {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u16(*&self.technology_preference, Endian::Little);
                writer.write_u8(*&self.technology_preference_duration);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.technology_preference = reader.read_u16(Endian::Little)?;
                out.technology_preference_duration = reader.read_u8()?;
                Ok(out)
            }
        }
    }
    #[doc = "Get RF Band Information"]
    pub mod get_rf_band_information {
        use crate::{Endian, Error, TlvBuf, TlvReader, TlvWriter};
        use alloc::string::String;
        use alloc::vec::Vec;
        pub const MESSAGE_ID: u16 = 49;
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Request {}
        impl Request {
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                Ok(Vec::new())
            }
            pub fn from_tlvs(_tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Ok(Self::default())
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Response {
            pub list: Vec<Element>,
            pub extended_list: Option<Vec<Element2>>,
            pub bandwidth_list: Option<Vec<Element3>>,
        }
        impl Response {
            pub const TLV_RESULT: u8 = 2;
            pub const TLV_LIST: u8 = 1;
            pub const TLV_EXTENDED_LIST: u8 = 17;
            pub const TLV_BANDWIDTH_LIST: u8 = 18;
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = Vec::new();
                {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::try_from((&self.list).len()).map_err(|_| Error::TooLong)?);
                    for item in &self.list {
                        (item).encode_to(&mut writer)?;
                    }
                    tlvs.push(writer.into_tlv(1));
                }
                if let Some(value) = &self.extended_list {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::try_from((value).len()).map_err(|_| Error::TooLong)?);
                    for item in value {
                        (item).encode_to(&mut writer)?;
                    }
                    tlvs.push(writer.into_tlv(17));
                }
                if let Some(value) = &self.bandwidth_list {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::try_from((value).len()).map_err(|_| Error::TooLong)?);
                    for item in value {
                        (item).encode_to(&mut writer)?;
                    }
                    tlvs.push(writer.into_tlv(18));
                }
                Ok(tlvs)
            }
            pub fn from_tlvs(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                let mut out = Self::default();
                let tlv = tlvs
                    .iter()
                    .find(|tlv| tlv.id == 1)
                    .ok_or(Error::MissingTlv { id: 1 })?;
                let mut reader = TlvReader::new(&tlv.value);
                let count = reader.read_u8()? as usize;
                let mut items = Vec::with_capacity(count.min(256));
                for _ in 0..count {
                    items.push(Element::decode_from(&mut reader)?);
                }
                out.list = items;
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 17) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let count = reader.read_u8()? as usize;
                    let mut items = Vec::with_capacity(count.min(256));
                    for _ in 0..count {
                        items.push(Element2::decode_from(&mut reader)?);
                    }
                    let value = items;
                    out.extended_list = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 18) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let count = reader.read_u8()? as usize;
                    let mut items = Vec::with_capacity(count.min(256));
                    for _ in 0..count {
                        items.push(Element3::decode_from(&mut reader)?);
                    }
                    let value = items;
                    out.bandwidth_list = Some(value);
                }
                Ok(out)
            }
        }
        impl crate::Response for Response {
            type Request = Request;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = crate::result::success_tlvs();
                tlvs.extend(self.to_tlvs()?);
                Ok(tlvs)
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                crate::result::check_result(tlvs)?;
                Response::from_tlvs(tlvs)
            }
        }
        impl crate::Request for Request {
            const SERVICE: u16 = super::super::SERVICE_ID;
            const MESSAGE_ID: u16 = 49;
            type Response = Response;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                self.to_tlvs()
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Self::from_tlvs(tlvs)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Element {
            pub radio_interface: i8,
            pub active_band_class: u16,
            pub active_channel: u16,
        }
        impl Element {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_i8(*&self.radio_interface);
                writer.write_u16(*&self.active_band_class, Endian::Little);
                writer.write_u16(*&self.active_channel, Endian::Little);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.radio_interface = reader.read_i8()?;
                out.active_band_class = reader.read_u16(Endian::Little)?;
                out.active_channel = reader.read_u16(Endian::Little)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Element2 {
            pub radio_interface: i8,
            pub active_band_class: u16,
            pub active_channel: u32,
        }
        impl Element2 {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_i8(*&self.radio_interface);
                writer.write_u16(*&self.active_band_class, Endian::Little);
                writer.write_u32(*&self.active_channel, Endian::Little);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.radio_interface = reader.read_i8()?;
                out.active_band_class = reader.read_u16(Endian::Little)?;
                out.active_channel = reader.read_u32(Endian::Little)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Element3 {
            pub radio_interface: i8,
            pub bandwidth: u32,
        }
        impl Element3 {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_i8(*&self.radio_interface);
                writer.write_u32(*&self.bandwidth, Endian::Little);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.radio_interface = reader.read_i8()?;
                out.bandwidth = reader.read_u32(Endian::Little)?;
                Ok(out)
            }
        }
    }
    #[doc = "Set System Selection Preference"]
    pub mod set_system_selection_preference {
        use crate::{Endian, Error, TlvBuf, TlvReader, TlvWriter};
        use alloc::string::String;
        use alloc::vec::Vec;
        pub const MESSAGE_ID: u16 = 51;
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Request {
            pub emergency_mode: Option<bool>,
            pub mode_preference: Option<u16>,
            pub band_preference: Option<u64>,
            pub cdma_prl_preference: Option<u16>,
            pub roaming_preference: Option<u16>,
            pub lte_band_preference: Option<u64>,
            pub network_selection_preference: Option<NetworkSelectionPreference>,
            pub change_duration: Option<u8>,
            pub service_domain_preference: Option<u32>,
            pub gsm_wcdma_acquisition_order_preference: Option<u32>,
            pub mnc_pcs_digit_include_status: Option<bool>,
            pub td_scdma_band_preference: Option<u64>,
            pub acquisition_order_preference: Option<Vec<i8>>,
            pub network_selection_registration_restriction: Option<u32>,
            pub usage_preference: Option<u32>,
            pub voice_domain_preference: Option<u32>,
            pub extended_lte_band_preference: Option<ExtendedLteBandPreference>,
            pub nr5g_sa_band_preference: Option<Nr5gSaBandPreference>,
            pub nr5g_nsa_band_preference: Option<Nr5gNsaBandPreference>,
        }
        impl Request {
            pub const TLV_EMERGENCY_MODE: u8 = 16;
            pub const TLV_MODE_PREFERENCE: u8 = 17;
            pub const TLV_BAND_PREFERENCE: u8 = 18;
            pub const TLV_CDMA_PRL_PREFERENCE: u8 = 19;
            pub const TLV_ROAMING_PREFERENCE: u8 = 20;
            pub const TLV_LTE_BAND_PREFERENCE: u8 = 21;
            pub const TLV_NETWORK_SELECTION_PREFERENCE: u8 = 22;
            pub const TLV_CHANGE_DURATION: u8 = 23;
            pub const TLV_SERVICE_DOMAIN_PREFERENCE: u8 = 24;
            pub const TLV_GSM_WCDMA_ACQUISITION_ORDER_PREFERENCE: u8 = 25;
            pub const TLV_MNC_PCS_DIGIT_INCLUDE_STATUS: u8 = 26;
            pub const TLV_TD_SCDMA_BAND_PREFERENCE: u8 = 29;
            pub const TLV_ACQUISITION_ORDER_PREFERENCE: u8 = 30;
            pub const TLV_NETWORK_SELECTION_REGISTRATION_RESTRICTION: u8 = 31;
            pub const TLV_USAGE_PREFERENCE: u8 = 33;
            pub const TLV_VOICE_DOMAIN_PREFERENCE: u8 = 35;
            pub const TLV_EXTENDED_LTE_BAND_PREFERENCE: u8 = 36;
            pub const TLV_NR5G_SA_BAND_PREFERENCE: u8 = 47;
            pub const TLV_NR5G_NSA_BAND_PREFERENCE: u8 = 48;
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = Vec::new();
                if let Some(value) = &self.emergency_mode {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::from(*value));
                    tlvs.push(writer.into_tlv(16));
                }
                if let Some(value) = &self.mode_preference {
                    let mut writer = TlvWriter::new();
                    writer.write_u16(*value, Endian::Little);
                    tlvs.push(writer.into_tlv(17));
                }
                if let Some(value) = &self.band_preference {
                    let mut writer = TlvWriter::new();
                    writer.write_u64(*value, Endian::Little);
                    tlvs.push(writer.into_tlv(18));
                }
                if let Some(value) = &self.cdma_prl_preference {
                    let mut writer = TlvWriter::new();
                    writer.write_u16(*value, Endian::Little);
                    tlvs.push(writer.into_tlv(19));
                }
                if let Some(value) = &self.roaming_preference {
                    let mut writer = TlvWriter::new();
                    writer.write_u16(*value, Endian::Little);
                    tlvs.push(writer.into_tlv(20));
                }
                if let Some(value) = &self.lte_band_preference {
                    let mut writer = TlvWriter::new();
                    writer.write_u64(*value, Endian::Little);
                    tlvs.push(writer.into_tlv(21));
                }
                if let Some(value) = &self.network_selection_preference {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(22));
                }
                if let Some(value) = &self.change_duration {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(*value);
                    tlvs.push(writer.into_tlv(23));
                }
                if let Some(value) = &self.service_domain_preference {
                    let mut writer = TlvWriter::new();
                    writer.write_u32(*value, Endian::Little);
                    tlvs.push(writer.into_tlv(24));
                }
                if let Some(value) = &self.gsm_wcdma_acquisition_order_preference {
                    let mut writer = TlvWriter::new();
                    writer.write_u32(*value, Endian::Little);
                    tlvs.push(writer.into_tlv(25));
                }
                if let Some(value) = &self.mnc_pcs_digit_include_status {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::from(*value));
                    tlvs.push(writer.into_tlv(26));
                }
                if let Some(value) = &self.td_scdma_band_preference {
                    let mut writer = TlvWriter::new();
                    writer.write_u64(*value, Endian::Little);
                    tlvs.push(writer.into_tlv(29));
                }
                if let Some(value) = &self.acquisition_order_preference {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::try_from((value).len()).map_err(|_| Error::TooLong)?);
                    for item in value {
                        writer.write_i8(*item);
                    }
                    tlvs.push(writer.into_tlv(30));
                }
                if let Some(value) = &self.network_selection_registration_restriction {
                    let mut writer = TlvWriter::new();
                    writer.write_u32(*value, Endian::Little);
                    tlvs.push(writer.into_tlv(31));
                }
                if let Some(value) = &self.usage_preference {
                    let mut writer = TlvWriter::new();
                    writer.write_u32(*value, Endian::Little);
                    tlvs.push(writer.into_tlv(33));
                }
                if let Some(value) = &self.voice_domain_preference {
                    let mut writer = TlvWriter::new();
                    writer.write_u32(*value, Endian::Little);
                    tlvs.push(writer.into_tlv(35));
                }
                if let Some(value) = &self.extended_lte_band_preference {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(36));
                }
                if let Some(value) = &self.nr5g_sa_band_preference {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(47));
                }
                if let Some(value) = &self.nr5g_nsa_band_preference {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(48));
                }
                Ok(tlvs)
            }
            pub fn from_tlvs(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                let mut out = Self::default();
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 16) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u8()? != 0;
                    out.emergency_mode = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 17) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u16(Endian::Little)?;
                    out.mode_preference = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 18) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u64(Endian::Little)?;
                    out.band_preference = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 19) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u16(Endian::Little)?;
                    out.cdma_prl_preference = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 20) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u16(Endian::Little)?;
                    out.roaming_preference = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 21) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u64(Endian::Little)?;
                    out.lte_band_preference = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 22) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = NetworkSelectionPreference::decode_from(&mut reader)?;
                    out.network_selection_preference = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 23) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u8()?;
                    out.change_duration = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 24) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u32(Endian::Little)?;
                    out.service_domain_preference = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 25) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u32(Endian::Little)?;
                    out.gsm_wcdma_acquisition_order_preference = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 26) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u8()? != 0;
                    out.mnc_pcs_digit_include_status = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 29) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u64(Endian::Little)?;
                    out.td_scdma_band_preference = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 30) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let count = reader.read_u8()? as usize;
                    let mut items = Vec::with_capacity(count.min(256));
                    for _ in 0..count {
                        items.push(reader.read_i8()?);
                    }
                    let value = items;
                    out.acquisition_order_preference = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 31) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u32(Endian::Little)?;
                    out.network_selection_registration_restriction = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 33) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u32(Endian::Little)?;
                    out.usage_preference = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 35) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u32(Endian::Little)?;
                    out.voice_domain_preference = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 36) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = ExtendedLteBandPreference::decode_from(&mut reader)?;
                    out.extended_lte_band_preference = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 47) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = Nr5gSaBandPreference::decode_from(&mut reader)?;
                    out.nr5g_sa_band_preference = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 48) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = Nr5gNsaBandPreference::decode_from(&mut reader)?;
                    out.nr5g_nsa_band_preference = Some(value);
                }
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Response {}
        impl Response {
            pub const TLV_RESULT: u8 = 2;
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                Ok(Vec::new())
            }
            pub fn from_tlvs(_tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Ok(Self::default())
            }
        }
        impl crate::Response for Response {
            type Request = Request;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = crate::result::success_tlvs();
                tlvs.extend(self.to_tlvs()?);
                Ok(tlvs)
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                crate::result::check_result(tlvs)?;
                Response::from_tlvs(tlvs)
            }
        }
        impl crate::Request for Request {
            const SERVICE: u16 = super::super::SERVICE_ID;
            const MESSAGE_ID: u16 = 51;
            type Response = Response;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                self.to_tlvs()
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Self::from_tlvs(tlvs)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct NetworkSelectionPreference {
            pub mode: u8,
            pub mcc: u16,
            pub mnc: u16,
        }
        impl NetworkSelectionPreference {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(*&self.mode);
                writer.write_u16(*&self.mcc, Endian::Little);
                writer.write_u16(*&self.mnc, Endian::Little);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.mode = reader.read_u8()?;
                out.mcc = reader.read_u16(Endian::Little)?;
                out.mnc = reader.read_u16(Endian::Little)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct ExtendedLteBandPreference {
            pub mask_low: u64,
            pub mask_mid_low: u64,
            pub mask_mid_high: u64,
            pub mask_high: u64,
        }
        impl ExtendedLteBandPreference {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u64(*&self.mask_low, Endian::Little);
                writer.write_u64(*&self.mask_mid_low, Endian::Little);
                writer.write_u64(*&self.mask_mid_high, Endian::Little);
                writer.write_u64(*&self.mask_high, Endian::Little);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.mask_low = reader.read_u64(Endian::Little)?;
                out.mask_mid_low = reader.read_u64(Endian::Little)?;
                out.mask_mid_high = reader.read_u64(Endian::Little)?;
                out.mask_high = reader.read_u64(Endian::Little)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Nr5gSaBandPreference {
            pub mask_0: u64,
            pub mask_1: u64,
            pub mask_2: u64,
            pub mask_3: u64,
            pub mask_4: u64,
            pub mask_5: u64,
            pub mask_6: u64,
            pub mask_7: u64,
        }
        impl Nr5gSaBandPreference {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u64(*&self.mask_0, Endian::Little);
                writer.write_u64(*&self.mask_1, Endian::Little);
                writer.write_u64(*&self.mask_2, Endian::Little);
                writer.write_u64(*&self.mask_3, Endian::Little);
                writer.write_u64(*&self.mask_4, Endian::Little);
                writer.write_u64(*&self.mask_5, Endian::Little);
                writer.write_u64(*&self.mask_6, Endian::Little);
                writer.write_u64(*&self.mask_7, Endian::Little);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.mask_0 = reader.read_u64(Endian::Little)?;
                out.mask_1 = reader.read_u64(Endian::Little)?;
                out.mask_2 = reader.read_u64(Endian::Little)?;
                out.mask_3 = reader.read_u64(Endian::Little)?;
                out.mask_4 = reader.read_u64(Endian::Little)?;
                out.mask_5 = reader.read_u64(Endian::Little)?;
                out.mask_6 = reader.read_u64(Endian::Little)?;
                out.mask_7 = reader.read_u64(Endian::Little)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Nr5gNsaBandPreference {
            pub mask_0: u64,
            pub mask_1: u64,
            pub mask_2: u64,
            pub mask_3: u64,
            pub mask_4: u64,
            pub mask_5: u64,
            pub mask_6: u64,
            pub mask_7: u64,
        }
        impl Nr5gNsaBandPreference {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u64(*&self.mask_0, Endian::Little);
                writer.write_u64(*&self.mask_1, Endian::Little);
                writer.write_u64(*&self.mask_2, Endian::Little);
                writer.write_u64(*&self.mask_3, Endian::Little);
                writer.write_u64(*&self.mask_4, Endian::Little);
                writer.write_u64(*&self.mask_5, Endian::Little);
                writer.write_u64(*&self.mask_6, Endian::Little);
                writer.write_u64(*&self.mask_7, Endian::Little);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.mask_0 = reader.read_u64(Endian::Little)?;
                out.mask_1 = reader.read_u64(Endian::Little)?;
                out.mask_2 = reader.read_u64(Endian::Little)?;
                out.mask_3 = reader.read_u64(Endian::Little)?;
                out.mask_4 = reader.read_u64(Endian::Little)?;
                out.mask_5 = reader.read_u64(Endian::Little)?;
                out.mask_6 = reader.read_u64(Endian::Little)?;
                out.mask_7 = reader.read_u64(Endian::Little)?;
                Ok(out)
            }
        }
    }
    #[doc = "Get System Selection Preference"]
    pub mod get_system_selection_preference {
        use crate::{Endian, Error, TlvBuf, TlvReader, TlvWriter};
        use alloc::string::String;
        use alloc::vec::Vec;
        pub const MESSAGE_ID: u16 = 52;
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Request {}
        impl Request {
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                Ok(Vec::new())
            }
            pub fn from_tlvs(_tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Ok(Self::default())
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Response {
            pub emergency_mode: Option<bool>,
            pub mode_preference: Option<u16>,
            pub band_preference: Option<u64>,
            pub cdma_prl_preference: Option<u16>,
            pub roaming_preference: Option<u16>,
            pub lte_band_preference: Option<u64>,
            pub network_selection_preference: Option<u8>,
            pub service_domain_preference: Option<u32>,
            pub gsm_wcdma_acquisition_order_preference: Option<u32>,
            pub td_scdma_band_preference: Option<u64>,
            pub acquisition_order_preference: Option<Vec<i8>>,
            pub manual_network_selection: Option<ManualNetworkSelection>,
            pub network_selection_registration_restriction: Option<u32>,
            pub usage_preference: Option<u32>,
            pub voice_domain_preference: Option<u32>,
            pub disabled_modes: Option<u16>,
            pub extended_lte_band_preference: Option<ExtendedLteBandPreference>,
            pub nr5g_sa_band_preference: Option<Nr5gSaBandPreference>,
            pub nr5g_nsa_band_preference: Option<Nr5gNsaBandPreference>,
        }
        impl Response {
            pub const TLV_RESULT: u8 = 2;
            pub const TLV_EMERGENCY_MODE: u8 = 16;
            pub const TLV_MODE_PREFERENCE: u8 = 17;
            pub const TLV_BAND_PREFERENCE: u8 = 18;
            pub const TLV_CDMA_PRL_PREFERENCE: u8 = 19;
            pub const TLV_ROAMING_PREFERENCE: u8 = 20;
            pub const TLV_LTE_BAND_PREFERENCE: u8 = 21;
            pub const TLV_NETWORK_SELECTION_PREFERENCE: u8 = 22;
            pub const TLV_SERVICE_DOMAIN_PREFERENCE: u8 = 24;
            pub const TLV_GSM_WCDMA_ACQUISITION_ORDER_PREFERENCE: u8 = 25;
            pub const TLV_TD_SCDMA_BAND_PREFERENCE: u8 = 26;
            pub const TLV_ACQUISITION_ORDER_PREFERENCE: u8 = 28;
            pub const TLV_MANUAL_NETWORK_SELECTION: u8 = 27;
            pub const TLV_NETWORK_SELECTION_REGISTRATION_RESTRICTION: u8 = 29;
            pub const TLV_USAGE_PREFERENCE: u8 = 31;
            pub const TLV_VOICE_DOMAIN_PREFERENCE: u8 = 32;
            pub const TLV_DISABLED_MODES: u8 = 34;
            pub const TLV_EXTENDED_LTE_BAND_PREFERENCE: u8 = 35;
            pub const TLV_NR5G_SA_BAND_PREFERENCE: u8 = 44;
            pub const TLV_NR5G_NSA_BAND_PREFERENCE: u8 = 45;
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = Vec::new();
                if let Some(value) = &self.emergency_mode {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::from(*value));
                    tlvs.push(writer.into_tlv(16));
                }
                if let Some(value) = &self.mode_preference {
                    let mut writer = TlvWriter::new();
                    writer.write_u16(*value, Endian::Little);
                    tlvs.push(writer.into_tlv(17));
                }
                if let Some(value) = &self.band_preference {
                    let mut writer = TlvWriter::new();
                    writer.write_u64(*value, Endian::Little);
                    tlvs.push(writer.into_tlv(18));
                }
                if let Some(value) = &self.cdma_prl_preference {
                    let mut writer = TlvWriter::new();
                    writer.write_u16(*value, Endian::Little);
                    tlvs.push(writer.into_tlv(19));
                }
                if let Some(value) = &self.roaming_preference {
                    let mut writer = TlvWriter::new();
                    writer.write_u16(*value, Endian::Little);
                    tlvs.push(writer.into_tlv(20));
                }
                if let Some(value) = &self.lte_band_preference {
                    let mut writer = TlvWriter::new();
                    writer.write_u64(*value, Endian::Little);
                    tlvs.push(writer.into_tlv(21));
                }
                if let Some(value) = &self.network_selection_preference {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(*value);
                    tlvs.push(writer.into_tlv(22));
                }
                if let Some(value) = &self.service_domain_preference {
                    let mut writer = TlvWriter::new();
                    writer.write_u32(*value, Endian::Little);
                    tlvs.push(writer.into_tlv(24));
                }
                if let Some(value) = &self.gsm_wcdma_acquisition_order_preference {
                    let mut writer = TlvWriter::new();
                    writer.write_u32(*value, Endian::Little);
                    tlvs.push(writer.into_tlv(25));
                }
                if let Some(value) = &self.td_scdma_band_preference {
                    let mut writer = TlvWriter::new();
                    writer.write_u64(*value, Endian::Little);
                    tlvs.push(writer.into_tlv(26));
                }
                if let Some(value) = &self.acquisition_order_preference {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::try_from((value).len()).map_err(|_| Error::TooLong)?);
                    for item in value {
                        writer.write_i8(*item);
                    }
                    tlvs.push(writer.into_tlv(28));
                }
                if let Some(value) = &self.manual_network_selection {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(27));
                }
                if let Some(value) = &self.network_selection_registration_restriction {
                    let mut writer = TlvWriter::new();
                    writer.write_u32(*value, Endian::Little);
                    tlvs.push(writer.into_tlv(29));
                }
                if let Some(value) = &self.usage_preference {
                    let mut writer = TlvWriter::new();
                    writer.write_u32(*value, Endian::Little);
                    tlvs.push(writer.into_tlv(31));
                }
                if let Some(value) = &self.voice_domain_preference {
                    let mut writer = TlvWriter::new();
                    writer.write_u32(*value, Endian::Little);
                    tlvs.push(writer.into_tlv(32));
                }
                if let Some(value) = &self.disabled_modes {
                    let mut writer = TlvWriter::new();
                    writer.write_u16(*value, Endian::Little);
                    tlvs.push(writer.into_tlv(34));
                }
                if let Some(value) = &self.extended_lte_band_preference {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(35));
                }
                if let Some(value) = &self.nr5g_sa_band_preference {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(44));
                }
                if let Some(value) = &self.nr5g_nsa_band_preference {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(45));
                }
                Ok(tlvs)
            }
            pub fn from_tlvs(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                let mut out = Self::default();
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 16) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u8()? != 0;
                    out.emergency_mode = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 17) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u16(Endian::Little)?;
                    out.mode_preference = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 18) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u64(Endian::Little)?;
                    out.band_preference = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 19) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u16(Endian::Little)?;
                    out.cdma_prl_preference = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 20) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u16(Endian::Little)?;
                    out.roaming_preference = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 21) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u64(Endian::Little)?;
                    out.lte_band_preference = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 22) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u8()?;
                    out.network_selection_preference = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 24) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u32(Endian::Little)?;
                    out.service_domain_preference = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 25) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u32(Endian::Little)?;
                    out.gsm_wcdma_acquisition_order_preference = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 26) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u64(Endian::Little)?;
                    out.td_scdma_band_preference = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 28) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let count = reader.read_u8()? as usize;
                    let mut items = Vec::with_capacity(count.min(256));
                    for _ in 0..count {
                        items.push(reader.read_i8()?);
                    }
                    let value = items;
                    out.acquisition_order_preference = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 27) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = ManualNetworkSelection::decode_from(&mut reader)?;
                    out.manual_network_selection = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 29) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u32(Endian::Little)?;
                    out.network_selection_registration_restriction = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 31) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u32(Endian::Little)?;
                    out.usage_preference = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 32) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u32(Endian::Little)?;
                    out.voice_domain_preference = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 34) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u16(Endian::Little)?;
                    out.disabled_modes = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 35) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = ExtendedLteBandPreference::decode_from(&mut reader)?;
                    out.extended_lte_band_preference = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 44) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = Nr5gSaBandPreference::decode_from(&mut reader)?;
                    out.nr5g_sa_band_preference = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 45) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = Nr5gNsaBandPreference::decode_from(&mut reader)?;
                    out.nr5g_nsa_band_preference = Some(value);
                }
                Ok(out)
            }
        }
        impl crate::Response for Response {
            type Request = Request;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = crate::result::success_tlvs();
                tlvs.extend(self.to_tlvs()?);
                Ok(tlvs)
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                crate::result::check_result(tlvs)?;
                Response::from_tlvs(tlvs)
            }
        }
        impl crate::Request for Request {
            const SERVICE: u16 = super::super::SERVICE_ID;
            const MESSAGE_ID: u16 = 52;
            type Response = Response;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                self.to_tlvs()
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Self::from_tlvs(tlvs)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct ManualNetworkSelection {
            pub mcc: u16,
            pub mnc: u16,
            pub includes_pcs_digit: bool,
        }
        impl ManualNetworkSelection {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u16(*&self.mcc, Endian::Little);
                writer.write_u16(*&self.mnc, Endian::Little);
                writer.write_u8(u8::from(*&self.includes_pcs_digit));
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.mcc = reader.read_u16(Endian::Little)?;
                out.mnc = reader.read_u16(Endian::Little)?;
                out.includes_pcs_digit = reader.read_u8()? != 0;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct ExtendedLteBandPreference {
            pub mask_low: u64,
            pub mask_mid_low: u64,
            pub mask_mid_high: u64,
            pub mask_high: u64,
        }
        impl ExtendedLteBandPreference {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u64(*&self.mask_low, Endian::Little);
                writer.write_u64(*&self.mask_mid_low, Endian::Little);
                writer.write_u64(*&self.mask_mid_high, Endian::Little);
                writer.write_u64(*&self.mask_high, Endian::Little);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.mask_low = reader.read_u64(Endian::Little)?;
                out.mask_mid_low = reader.read_u64(Endian::Little)?;
                out.mask_mid_high = reader.read_u64(Endian::Little)?;
                out.mask_high = reader.read_u64(Endian::Little)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Nr5gSaBandPreference {
            pub mask_0: u64,
            pub mask_1: u64,
            pub mask_2: u64,
            pub mask_3: u64,
            pub mask_4: u64,
            pub mask_5: u64,
            pub mask_6: u64,
            pub mask_7: u64,
        }
        impl Nr5gSaBandPreference {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u64(*&self.mask_0, Endian::Little);
                writer.write_u64(*&self.mask_1, Endian::Little);
                writer.write_u64(*&self.mask_2, Endian::Little);
                writer.write_u64(*&self.mask_3, Endian::Little);
                writer.write_u64(*&self.mask_4, Endian::Little);
                writer.write_u64(*&self.mask_5, Endian::Little);
                writer.write_u64(*&self.mask_6, Endian::Little);
                writer.write_u64(*&self.mask_7, Endian::Little);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.mask_0 = reader.read_u64(Endian::Little)?;
                out.mask_1 = reader.read_u64(Endian::Little)?;
                out.mask_2 = reader.read_u64(Endian::Little)?;
                out.mask_3 = reader.read_u64(Endian::Little)?;
                out.mask_4 = reader.read_u64(Endian::Little)?;
                out.mask_5 = reader.read_u64(Endian::Little)?;
                out.mask_6 = reader.read_u64(Endian::Little)?;
                out.mask_7 = reader.read_u64(Endian::Little)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Nr5gNsaBandPreference {
            pub mask_0: u64,
            pub mask_1: u64,
            pub mask_2: u64,
            pub mask_3: u64,
            pub mask_4: u64,
            pub mask_5: u64,
            pub mask_6: u64,
            pub mask_7: u64,
        }
        impl Nr5gNsaBandPreference {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u64(*&self.mask_0, Endian::Little);
                writer.write_u64(*&self.mask_1, Endian::Little);
                writer.write_u64(*&self.mask_2, Endian::Little);
                writer.write_u64(*&self.mask_3, Endian::Little);
                writer.write_u64(*&self.mask_4, Endian::Little);
                writer.write_u64(*&self.mask_5, Endian::Little);
                writer.write_u64(*&self.mask_6, Endian::Little);
                writer.write_u64(*&self.mask_7, Endian::Little);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.mask_0 = reader.read_u64(Endian::Little)?;
                out.mask_1 = reader.read_u64(Endian::Little)?;
                out.mask_2 = reader.read_u64(Endian::Little)?;
                out.mask_3 = reader.read_u64(Endian::Little)?;
                out.mask_4 = reader.read_u64(Endian::Little)?;
                out.mask_5 = reader.read_u64(Endian::Little)?;
                out.mask_6 = reader.read_u64(Endian::Little)?;
                out.mask_7 = reader.read_u64(Endian::Little)?;
                Ok(out)
            }
        }
    }
    #[doc = "Get Operator Name"]
    pub mod get_operator_name {
        use crate::{Endian, Error, TlvBuf, TlvReader, TlvWriter};
        use alloc::string::String;
        use alloc::vec::Vec;
        pub const MESSAGE_ID: u16 = 57;
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Request {}
        impl Request {
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                Ok(Vec::new())
            }
            pub fn from_tlvs(_tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Ok(Self::default())
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Response {
            pub service_provider_name: Option<ServiceProviderName>,
            pub operator_plmn_list: Option<Vec<Element>>,
            pub operator_plmn_name: Option<Vec<Element2>>,
            pub operator_string_name: Option<String>,
            pub nitz_information: Option<NitzInformation>,
        }
        impl Response {
            pub const TLV_RESULT: u8 = 2;
            pub const TLV_SERVICE_PROVIDER_NAME: u8 = 16;
            pub const TLV_OPERATOR_PLMN_LIST: u8 = 17;
            pub const TLV_OPERATOR_PLMN_NAME: u8 = 18;
            pub const TLV_OPERATOR_STRING_NAME: u8 = 19;
            pub const TLV_NITZ_INFORMATION: u8 = 20;
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = Vec::new();
                if let Some(value) = &self.service_provider_name {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(16));
                }
                if let Some(value) = &self.operator_plmn_list {
                    let mut writer = TlvWriter::new();
                    writer.write_u16(
                        u16::try_from((value).len()).map_err(|_| Error::TooLong)?,
                        Endian::Little,
                    );
                    for item in value {
                        (item).encode_to(&mut writer)?;
                    }
                    tlvs.push(writer.into_tlv(17));
                }
                if let Some(value) = &self.operator_plmn_name {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::try_from((value).len()).map_err(|_| Error::TooLong)?);
                    for item in value {
                        (item).encode_to(&mut writer)?;
                    }
                    tlvs.push(writer.into_tlv(18));
                }
                if let Some(value) = &self.operator_string_name {
                    let mut writer = TlvWriter::new();
                    writer.write_string(value, 0, None)?;
                    tlvs.push(writer.into_tlv(19));
                }
                if let Some(value) = &self.nitz_information {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(20));
                }
                Ok(tlvs)
            }
            pub fn from_tlvs(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                let mut out = Self::default();
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 16) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = ServiceProviderName::decode_from(&mut reader)?;
                    out.service_provider_name = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 17) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let count = reader.read_u16(Endian::Little)? as usize;
                    let mut items = Vec::with_capacity(count.min(256));
                    for _ in 0..count {
                        items.push(Element::decode_from(&mut reader)?);
                    }
                    let value = items;
                    out.operator_plmn_list = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 18) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let count = reader.read_u8()? as usize;
                    let mut items = Vec::with_capacity(count.min(256));
                    for _ in 0..count {
                        items.push(Element2::decode_from(&mut reader)?);
                    }
                    let value = items;
                    out.operator_plmn_name = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 19) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_string(0, 0)?;
                    out.operator_string_name = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 20) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = NitzInformation::decode_from(&mut reader)?;
                    out.nitz_information = Some(value);
                }
                Ok(out)
            }
        }
        impl crate::Response for Response {
            type Request = Request;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = crate::result::success_tlvs();
                tlvs.extend(self.to_tlvs()?);
                Ok(tlvs)
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                crate::result::check_result(tlvs)?;
                Response::from_tlvs(tlvs)
            }
        }
        impl crate::Request for Request {
            const SERVICE: u16 = super::super::SERVICE_ID;
            const MESSAGE_ID: u16 = 57;
            type Response = Response;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                self.to_tlvs()
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Self::from_tlvs(tlvs)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct ServiceProviderName {
            pub name_display_condition: u8,
            pub name: String,
        }
        impl ServiceProviderName {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(*&self.name_display_condition);
                writer.write_string(&self.name, 1, None)?;
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.name_display_condition = reader.read_u8()?;
                out.name = reader.read_string(1, 0)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Element {
            pub mcc: String,
            pub mnc: String,
            pub lac1: u16,
            pub lac2: u16,
            pub plmn_name_record_identifier: u8,
        }
        impl Element {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_string(&self.mcc, 0, Some(3))?;
                writer.write_string(&self.mnc, 0, Some(3))?;
                writer.write_u16(*&self.lac1, Endian::Little);
                writer.write_u16(*&self.lac2, Endian::Little);
                writer.write_u8(*&self.plmn_name_record_identifier);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.mcc = reader.read_fixed_string(3)?;
                out.mnc = reader.read_fixed_string(3)?;
                out.lac1 = reader.read_u16(Endian::Little)?;
                out.lac2 = reader.read_u16(Endian::Little)?;
                out.plmn_name_record_identifier = reader.read_u8()?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Element2 {
            pub name_encoding: u8,
            pub short_country_initials: u8,
            pub long_name_spare_bits: u8,
            pub short_name_spare_bits: u8,
            pub long_name: Vec<u8>,
            pub short_name: Vec<u8>,
        }
        impl Element2 {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(*&self.name_encoding);
                writer.write_u8(*&self.short_country_initials);
                writer.write_u8(*&self.long_name_spare_bits);
                writer.write_u8(*&self.short_name_spare_bits);
                writer.write_u8(u8::try_from((&self.long_name).len()).map_err(|_| Error::TooLong)?);
                for item in &self.long_name {
                    writer.write_u8(*item);
                }
                writer
                    .write_u8(u8::try_from((&self.short_name).len()).map_err(|_| Error::TooLong)?);
                for item in &self.short_name {
                    writer.write_u8(*item);
                }
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.name_encoding = reader.read_u8()?;
                out.short_country_initials = reader.read_u8()?;
                out.long_name_spare_bits = reader.read_u8()?;
                out.short_name_spare_bits = reader.read_u8()?;
                let count = reader.read_u8()? as usize;
                let mut items = Vec::with_capacity(count.min(256));
                for _ in 0..count {
                    items.push(reader.read_u8()?);
                }
                out.long_name = items;
                let count = reader.read_u8()? as usize;
                let mut items = Vec::with_capacity(count.min(256));
                for _ in 0..count {
                    items.push(reader.read_u8()?);
                }
                out.short_name = items;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct NitzInformation {
            pub name_encoding: u8,
            pub short_country_initials: u8,
            pub long_name_spare_bits: u8,
            pub short_name_spare_bits: u8,
            pub long_name: Vec<u8>,
            pub short_name: Vec<u8>,
        }
        impl NitzInformation {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(*&self.name_encoding);
                writer.write_u8(*&self.short_country_initials);
                writer.write_u8(*&self.long_name_spare_bits);
                writer.write_u8(*&self.short_name_spare_bits);
                writer.write_u8(u8::try_from((&self.long_name).len()).map_err(|_| Error::TooLong)?);
                for item in &self.long_name {
                    writer.write_u8(*item);
                }
                writer
                    .write_u8(u8::try_from((&self.short_name).len()).map_err(|_| Error::TooLong)?);
                for item in &self.short_name {
                    writer.write_u8(*item);
                }
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.name_encoding = reader.read_u8()?;
                out.short_country_initials = reader.read_u8()?;
                out.long_name_spare_bits = reader.read_u8()?;
                out.short_name_spare_bits = reader.read_u8()?;
                let count = reader.read_u8()? as usize;
                let mut items = Vec::with_capacity(count.min(256));
                for _ in 0..count {
                    items.push(reader.read_u8()?);
                }
                out.long_name = items;
                let count = reader.read_u8()? as usize;
                let mut items = Vec::with_capacity(count.min(256));
                for _ in 0..count {
                    items.push(reader.read_u8()?);
                }
                out.short_name = items;
                Ok(out)
            }
        }
    }
    #[doc = "Get Cell Location Info"]
    pub mod get_cell_location_info {
        use crate::{Endian, Error, TlvBuf, TlvReader, TlvWriter};
        use alloc::string::String;
        use alloc::vec::Vec;
        pub const MESSAGE_ID: u16 = 67;
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Request {}
        impl Request {
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                Ok(Vec::new())
            }
            pub fn from_tlvs(_tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Ok(Self::default())
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Response {
            pub geran_info_v2: Option<GeranInfoV2>,
            pub umts_info_v2: Option<UmtsInfoV2>,
            pub cdma_info: Option<CdmaInfo>,
            pub intrafrequency_lte_info_v2: Option<IntrafrequencyLteInfoV2>,
            pub interfrequency_lte_info: Option<InterfrequencyLteInfo>,
            pub lte_info_neighboring_gsm: Option<LteInfoNeighboringGsm>,
            pub lte_info_neighboring_wcdma: Option<LteInfoNeighboringWcdma>,
            pub umts_cell_id: Option<u32>,
            pub umts_info_neighboring_lte: Option<UmtsInfoNeighboringLte>,
            pub lte_info_timing_advance: Option<u32>,
            pub nr5g_arfcn: Option<u32>,
            pub nr5g_cell_information: Option<Nr5gCellInformation>,
        }
        impl Response {
            pub const TLV_RESULT: u8 = 2;
            pub const TLV_GERAN_INFO_V2: u8 = 16;
            pub const TLV_UMTS_INFO_V2: u8 = 17;
            pub const TLV_CDMA_INFO: u8 = 18;
            pub const TLV_INTRAFREQUENCY_LTE_INFO_V2: u8 = 19;
            pub const TLV_INTERFREQUENCY_LTE_INFO: u8 = 20;
            pub const TLV_LTE_INFO_NEIGHBORING_GSM: u8 = 21;
            pub const TLV_LTE_INFO_NEIGHBORING_WCDMA: u8 = 22;
            pub const TLV_UMTS_CELL_ID: u8 = 23;
            pub const TLV_UMTS_INFO_NEIGHBORING_LTE: u8 = 24;
            pub const TLV_LTE_INFO_TIMING_ADVANCE: u8 = 30;
            pub const TLV_NR5G_ARFCN: u8 = 46;
            pub const TLV_NR5G_CELL_INFORMATION: u8 = 47;
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = Vec::new();
                if let Some(value) = &self.geran_info_v2 {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(16));
                }
                if let Some(value) = &self.umts_info_v2 {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(17));
                }
                if let Some(value) = &self.cdma_info {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(18));
                }
                if let Some(value) = &self.intrafrequency_lte_info_v2 {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(19));
                }
                if let Some(value) = &self.interfrequency_lte_info {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(20));
                }
                if let Some(value) = &self.lte_info_neighboring_gsm {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(21));
                }
                if let Some(value) = &self.lte_info_neighboring_wcdma {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(22));
                }
                if let Some(value) = &self.umts_cell_id {
                    let mut writer = TlvWriter::new();
                    writer.write_u32(*value, Endian::Little);
                    tlvs.push(writer.into_tlv(23));
                }
                if let Some(value) = &self.umts_info_neighboring_lte {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(24));
                }
                if let Some(value) = &self.lte_info_timing_advance {
                    let mut writer = TlvWriter::new();
                    writer.write_u32(*value, Endian::Little);
                    tlvs.push(writer.into_tlv(30));
                }
                if let Some(value) = &self.nr5g_arfcn {
                    let mut writer = TlvWriter::new();
                    writer.write_u32(*value, Endian::Little);
                    tlvs.push(writer.into_tlv(46));
                }
                if let Some(value) = &self.nr5g_cell_information {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(47));
                }
                Ok(tlvs)
            }
            pub fn from_tlvs(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                let mut out = Self::default();
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 16) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = GeranInfoV2::decode_from(&mut reader)?;
                    out.geran_info_v2 = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 17) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = UmtsInfoV2::decode_from(&mut reader)?;
                    out.umts_info_v2 = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 18) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = CdmaInfo::decode_from(&mut reader)?;
                    out.cdma_info = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 19) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = IntrafrequencyLteInfoV2::decode_from(&mut reader)?;
                    out.intrafrequency_lte_info_v2 = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 20) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = InterfrequencyLteInfo::decode_from(&mut reader)?;
                    out.interfrequency_lte_info = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 21) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = LteInfoNeighboringGsm::decode_from(&mut reader)?;
                    out.lte_info_neighboring_gsm = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 22) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = LteInfoNeighboringWcdma::decode_from(&mut reader)?;
                    out.lte_info_neighboring_wcdma = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 23) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u32(Endian::Little)?;
                    out.umts_cell_id = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 24) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = UmtsInfoNeighboringLte::decode_from(&mut reader)?;
                    out.umts_info_neighboring_lte = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 30) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u32(Endian::Little)?;
                    out.lte_info_timing_advance = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 46) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u32(Endian::Little)?;
                    out.nr5g_arfcn = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 47) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = Nr5gCellInformation::decode_from(&mut reader)?;
                    out.nr5g_cell_information = Some(value);
                }
                Ok(out)
            }
        }
        impl crate::Response for Response {
            type Request = Request;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = crate::result::success_tlvs();
                tlvs.extend(self.to_tlvs()?);
                Ok(tlvs)
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                crate::result::check_result(tlvs)?;
                Response::from_tlvs(tlvs)
            }
        }
        impl crate::Request for Request {
            const SERVICE: u16 = super::super::SERVICE_ID;
            const MESSAGE_ID: u16 = 67;
            type Response = Response;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                self.to_tlvs()
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Self::from_tlvs(tlvs)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Element {
            pub cell_id: u32,
            pub plmn: Vec<u8>,
            pub lac: u16,
            pub geran_absolute_rf_channel_number: u16,
            pub base_station_identity_code: u8,
            pub rx_level: u16,
        }
        impl Element {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u32(*&self.cell_id, Endian::Little);
                if (&self.plmn).len() != 3 {
                    return Err(Error::InvalidLength);
                }
                for item in &self.plmn {
                    writer.write_u8(*item);
                }
                writer.write_u16(*&self.lac, Endian::Little);
                writer.write_u16(*&self.geran_absolute_rf_channel_number, Endian::Little);
                writer.write_u8(*&self.base_station_identity_code);
                writer.write_u16(*&self.rx_level, Endian::Little);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.cell_id = reader.read_u32(Endian::Little)?;
                let count = 3;
                let mut items = Vec::with_capacity(count.min(256));
                for _ in 0..count {
                    items.push(reader.read_u8()?);
                }
                out.plmn = items;
                out.lac = reader.read_u16(Endian::Little)?;
                out.geran_absolute_rf_channel_number = reader.read_u16(Endian::Little)?;
                out.base_station_identity_code = reader.read_u8()?;
                out.rx_level = reader.read_u16(Endian::Little)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct GeranInfoV2 {
            pub cell_id: u32,
            pub plmn: Vec<u8>,
            pub lac: u16,
            pub geran_absolute_rf_channel_number: u16,
            pub base_station_identity_code: u8,
            pub timing_advance: u32,
            pub rx_level: u16,
            pub cell: Vec<Element>,
        }
        impl GeranInfoV2 {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u32(*&self.cell_id, Endian::Little);
                if (&self.plmn).len() != 3 {
                    return Err(Error::InvalidLength);
                }
                for item in &self.plmn {
                    writer.write_u8(*item);
                }
                writer.write_u16(*&self.lac, Endian::Little);
                writer.write_u16(*&self.geran_absolute_rf_channel_number, Endian::Little);
                writer.write_u8(*&self.base_station_identity_code);
                writer.write_u32(*&self.timing_advance, Endian::Little);
                writer.write_u16(*&self.rx_level, Endian::Little);
                writer.write_u8(u8::try_from((&self.cell).len()).map_err(|_| Error::TooLong)?);
                for item in &self.cell {
                    (item).encode_to(writer)?;
                }
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.cell_id = reader.read_u32(Endian::Little)?;
                let count = 3;
                let mut items = Vec::with_capacity(count.min(256));
                for _ in 0..count {
                    items.push(reader.read_u8()?);
                }
                out.plmn = items;
                out.lac = reader.read_u16(Endian::Little)?;
                out.geran_absolute_rf_channel_number = reader.read_u16(Endian::Little)?;
                out.base_station_identity_code = reader.read_u8()?;
                out.timing_advance = reader.read_u32(Endian::Little)?;
                out.rx_level = reader.read_u16(Endian::Little)?;
                let count = reader.read_u8()? as usize;
                let mut items = Vec::with_capacity(count.min(256));
                for _ in 0..count {
                    items.push(Element::decode_from(reader)?);
                }
                out.cell = items;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Element2 {
            pub utra_absolute_rf_channel_number: u16,
            pub primary_scrambling_code: u16,
            pub rscp: i16,
            pub ecio: i16,
        }
        impl Element2 {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u16(*&self.utra_absolute_rf_channel_number, Endian::Little);
                writer.write_u16(*&self.primary_scrambling_code, Endian::Little);
                writer.write_i16(*&self.rscp, Endian::Little);
                writer.write_i16(*&self.ecio, Endian::Little);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.utra_absolute_rf_channel_number = reader.read_u16(Endian::Little)?;
                out.primary_scrambling_code = reader.read_u16(Endian::Little)?;
                out.rscp = reader.read_i16(Endian::Little)?;
                out.ecio = reader.read_i16(Endian::Little)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Element3 {
            pub geran_absolute_rf_channel_number: u16,
            pub network_color_code: u8,
            pub base_station_color_code: u8,
            pub rssi: i16,
        }
        impl Element3 {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u16(*&self.geran_absolute_rf_channel_number, Endian::Little);
                writer.write_u8(*&self.network_color_code);
                writer.write_u8(*&self.base_station_color_code);
                writer.write_i16(*&self.rssi, Endian::Little);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.geran_absolute_rf_channel_number = reader.read_u16(Endian::Little)?;
                out.network_color_code = reader.read_u8()?;
                out.base_station_color_code = reader.read_u8()?;
                out.rssi = reader.read_i16(Endian::Little)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct UmtsInfoV2 {
            pub cell_id: u16,
            pub plmn: Vec<u8>,
            pub lac: u16,
            pub utra_absolute_rf_channel_number: u16,
            pub primary_scrambling_code: u16,
            pub rscp: i16,
            pub ecio: i16,
            pub cell: Vec<Element2>,
            pub neighboring_geran: Vec<Element3>,
        }
        impl UmtsInfoV2 {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u16(*&self.cell_id, Endian::Little);
                if (&self.plmn).len() != 3 {
                    return Err(Error::InvalidLength);
                }
                for item in &self.plmn {
                    writer.write_u8(*item);
                }
                writer.write_u16(*&self.lac, Endian::Little);
                writer.write_u16(*&self.utra_absolute_rf_channel_number, Endian::Little);
                writer.write_u16(*&self.primary_scrambling_code, Endian::Little);
                writer.write_i16(*&self.rscp, Endian::Little);
                writer.write_i16(*&self.ecio, Endian::Little);
                writer.write_u8(u8::try_from((&self.cell).len()).map_err(|_| Error::TooLong)?);
                for item in &self.cell {
                    (item).encode_to(writer)?;
                }
                writer.write_u8(
                    u8::try_from((&self.neighboring_geran).len()).map_err(|_| Error::TooLong)?,
                );
                for item in &self.neighboring_geran {
                    (item).encode_to(writer)?;
                }
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.cell_id = reader.read_u16(Endian::Little)?;
                let count = 3;
                let mut items = Vec::with_capacity(count.min(256));
                for _ in 0..count {
                    items.push(reader.read_u8()?);
                }
                out.plmn = items;
                out.lac = reader.read_u16(Endian::Little)?;
                out.utra_absolute_rf_channel_number = reader.read_u16(Endian::Little)?;
                out.primary_scrambling_code = reader.read_u16(Endian::Little)?;
                out.rscp = reader.read_i16(Endian::Little)?;
                out.ecio = reader.read_i16(Endian::Little)?;
                let count = reader.read_u8()? as usize;
                let mut items = Vec::with_capacity(count.min(256));
                for _ in 0..count {
                    items.push(Element2::decode_from(reader)?);
                }
                out.cell = items;
                let count = reader.read_u8()? as usize;
                let mut items = Vec::with_capacity(count.min(256));
                for _ in 0..count {
                    items.push(Element3::decode_from(reader)?);
                }
                out.neighboring_geran = items;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct CdmaInfo {
            pub system_id: u16,
            pub network_id: u16,
            pub base_station_id: u16,
            pub reference_pn: u16,
            pub latitude: u32,
            pub longitude: u32,
        }
        impl CdmaInfo {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u16(*&self.system_id, Endian::Little);
                writer.write_u16(*&self.network_id, Endian::Little);
                writer.write_u16(*&self.base_station_id, Endian::Little);
                writer.write_u16(*&self.reference_pn, Endian::Little);
                writer.write_u32(*&self.latitude, Endian::Little);
                writer.write_u32(*&self.longitude, Endian::Little);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.system_id = reader.read_u16(Endian::Little)?;
                out.network_id = reader.read_u16(Endian::Little)?;
                out.base_station_id = reader.read_u16(Endian::Little)?;
                out.reference_pn = reader.read_u16(Endian::Little)?;
                out.latitude = reader.read_u32(Endian::Little)?;
                out.longitude = reader.read_u32(Endian::Little)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Element4 {
            pub physical_cell_id: u16,
            pub rsrq: i16,
            pub rsrp: i16,
            pub rssi: i16,
            pub cell_selection_rx_level: i16,
        }
        impl Element4 {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u16(*&self.physical_cell_id, Endian::Little);
                writer.write_i16(*&self.rsrq, Endian::Little);
                writer.write_i16(*&self.rsrp, Endian::Little);
                writer.write_i16(*&self.rssi, Endian::Little);
                writer.write_i16(*&self.cell_selection_rx_level, Endian::Little);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.physical_cell_id = reader.read_u16(Endian::Little)?;
                out.rsrq = reader.read_i16(Endian::Little)?;
                out.rsrp = reader.read_i16(Endian::Little)?;
                out.rssi = reader.read_i16(Endian::Little)?;
                out.cell_selection_rx_level = reader.read_i16(Endian::Little)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct IntrafrequencyLteInfoV2 {
            pub ue_in_idle: bool,
            pub plmn: Vec<u8>,
            pub tracking_area_code: u16,
            pub global_cell_id: u32,
            pub eutra_absolute_rf_channel_number: u16,
            pub serving_cell_id: u16,
            pub cell_reselection_priority: u8,
            pub s_non_intra_search_threshold: u8,
            pub serving_cell_low_threshold: u8,
            pub s_intra_search_threshold: u8,
            pub cell: Vec<Element4>,
        }
        impl IntrafrequencyLteInfoV2 {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(u8::from(*&self.ue_in_idle));
                if (&self.plmn).len() != 3 {
                    return Err(Error::InvalidLength);
                }
                for item in &self.plmn {
                    writer.write_u8(*item);
                }
                writer.write_u16(*&self.tracking_area_code, Endian::Little);
                writer.write_u32(*&self.global_cell_id, Endian::Little);
                writer.write_u16(*&self.eutra_absolute_rf_channel_number, Endian::Little);
                writer.write_u16(*&self.serving_cell_id, Endian::Little);
                writer.write_u8(*&self.cell_reselection_priority);
                writer.write_u8(*&self.s_non_intra_search_threshold);
                writer.write_u8(*&self.serving_cell_low_threshold);
                writer.write_u8(*&self.s_intra_search_threshold);
                writer.write_u8(u8::try_from((&self.cell).len()).map_err(|_| Error::TooLong)?);
                for item in &self.cell {
                    (item).encode_to(writer)?;
                }
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.ue_in_idle = reader.read_u8()? != 0;
                let count = 3;
                let mut items = Vec::with_capacity(count.min(256));
                for _ in 0..count {
                    items.push(reader.read_u8()?);
                }
                out.plmn = items;
                out.tracking_area_code = reader.read_u16(Endian::Little)?;
                out.global_cell_id = reader.read_u32(Endian::Little)?;
                out.eutra_absolute_rf_channel_number = reader.read_u16(Endian::Little)?;
                out.serving_cell_id = reader.read_u16(Endian::Little)?;
                out.cell_reselection_priority = reader.read_u8()?;
                out.s_non_intra_search_threshold = reader.read_u8()?;
                out.serving_cell_low_threshold = reader.read_u8()?;
                out.s_intra_search_threshold = reader.read_u8()?;
                let count = reader.read_u8()? as usize;
                let mut items = Vec::with_capacity(count.min(256));
                for _ in 0..count {
                    items.push(Element4::decode_from(reader)?);
                }
                out.cell = items;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Element6 {
            pub physical_cell_id: u16,
            pub rsrq: i16,
            pub rsrp: i16,
            pub rssi: i16,
            pub cell_selection_rx_level: i16,
        }
        impl Element6 {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u16(*&self.physical_cell_id, Endian::Little);
                writer.write_i16(*&self.rsrq, Endian::Little);
                writer.write_i16(*&self.rsrp, Endian::Little);
                writer.write_i16(*&self.rssi, Endian::Little);
                writer.write_i16(*&self.cell_selection_rx_level, Endian::Little);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.physical_cell_id = reader.read_u16(Endian::Little)?;
                out.rsrq = reader.read_i16(Endian::Little)?;
                out.rsrp = reader.read_i16(Endian::Little)?;
                out.rssi = reader.read_i16(Endian::Little)?;
                out.cell_selection_rx_level = reader.read_i16(Endian::Little)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Element5 {
            pub eutra_absolute_rf_channel_number: u16,
            pub cell_selection_rx_level_low_threshold: u8,
            pub cell_selection_rx_level_high_threshold: u8,
            pub cell_reselection_priority: u8,
            pub cell: Vec<Element6>,
        }
        impl Element5 {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u16(*&self.eutra_absolute_rf_channel_number, Endian::Little);
                writer.write_u8(*&self.cell_selection_rx_level_low_threshold);
                writer.write_u8(*&self.cell_selection_rx_level_high_threshold);
                writer.write_u8(*&self.cell_reselection_priority);
                writer.write_u8(u8::try_from((&self.cell).len()).map_err(|_| Error::TooLong)?);
                for item in &self.cell {
                    (item).encode_to(writer)?;
                }
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.eutra_absolute_rf_channel_number = reader.read_u16(Endian::Little)?;
                out.cell_selection_rx_level_low_threshold = reader.read_u8()?;
                out.cell_selection_rx_level_high_threshold = reader.read_u8()?;
                out.cell_reselection_priority = reader.read_u8()?;
                let count = reader.read_u8()? as usize;
                let mut items = Vec::with_capacity(count.min(256));
                for _ in 0..count {
                    items.push(Element6::decode_from(reader)?);
                }
                out.cell = items;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct InterfrequencyLteInfo {
            pub ue_in_idle: bool,
            pub frequency: Vec<Element5>,
        }
        impl InterfrequencyLteInfo {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(u8::from(*&self.ue_in_idle));
                writer.write_u8(u8::try_from((&self.frequency).len()).map_err(|_| Error::TooLong)?);
                for item in &self.frequency {
                    (item).encode_to(writer)?;
                }
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.ue_in_idle = reader.read_u8()? != 0;
                let count = reader.read_u8()? as usize;
                let mut items = Vec::with_capacity(count.min(256));
                for _ in 0..count {
                    items.push(Element5::decode_from(reader)?);
                }
                out.frequency = items;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Element8 {
            pub geran_absolute_rf_channel_number: u16,
            pub band_is_1900: bool,
            pub cell_id_valid: bool,
            pub base_station_identity_code: u8,
            pub rssi: i16,
            pub cell_selection_rx_level: i16,
        }
        impl Element8 {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u16(*&self.geran_absolute_rf_channel_number, Endian::Little);
                writer.write_u8(u8::from(*&self.band_is_1900));
                writer.write_u8(u8::from(*&self.cell_id_valid));
                writer.write_u8(*&self.base_station_identity_code);
                writer.write_i16(*&self.rssi, Endian::Little);
                writer.write_i16(*&self.cell_selection_rx_level, Endian::Little);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.geran_absolute_rf_channel_number = reader.read_u16(Endian::Little)?;
                out.band_is_1900 = reader.read_u8()? != 0;
                out.cell_id_valid = reader.read_u8()? != 0;
                out.base_station_identity_code = reader.read_u8()?;
                out.rssi = reader.read_i16(Endian::Little)?;
                out.cell_selection_rx_level = reader.read_i16(Endian::Little)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Element7 {
            pub cell_reselection_priority: u8,
            pub cell_reselection_high_threshold: u8,
            pub cell_reselection_low_threshold: u8,
            pub ncc_permitted: u8,
            pub cell: Vec<Element8>,
        }
        impl Element7 {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(*&self.cell_reselection_priority);
                writer.write_u8(*&self.cell_reselection_high_threshold);
                writer.write_u8(*&self.cell_reselection_low_threshold);
                writer.write_u8(*&self.ncc_permitted);
                writer.write_u8(u8::try_from((&self.cell).len()).map_err(|_| Error::TooLong)?);
                for item in &self.cell {
                    (item).encode_to(writer)?;
                }
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.cell_reselection_priority = reader.read_u8()?;
                out.cell_reselection_high_threshold = reader.read_u8()?;
                out.cell_reselection_low_threshold = reader.read_u8()?;
                out.ncc_permitted = reader.read_u8()?;
                let count = reader.read_u8()? as usize;
                let mut items = Vec::with_capacity(count.min(256));
                for _ in 0..count {
                    items.push(Element8::decode_from(reader)?);
                }
                out.cell = items;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct LteInfoNeighboringGsm {
            pub ue_in_idle: bool,
            pub frequency: Vec<Element7>,
        }
        impl LteInfoNeighboringGsm {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(u8::from(*&self.ue_in_idle));
                writer.write_u8(u8::try_from((&self.frequency).len()).map_err(|_| Error::TooLong)?);
                for item in &self.frequency {
                    (item).encode_to(writer)?;
                }
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.ue_in_idle = reader.read_u8()? != 0;
                let count = reader.read_u8()? as usize;
                let mut items = Vec::with_capacity(count.min(256));
                for _ in 0..count {
                    items.push(Element7::decode_from(reader)?);
                }
                out.frequency = items;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Element10 {
            pub primary_scrambling_code: u16,
            pub cpich_rscp: i16,
            pub cpich_ecno: i16,
            pub cell_selection_rx_level: i16,
        }
        impl Element10 {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u16(*&self.primary_scrambling_code, Endian::Little);
                writer.write_i16(*&self.cpich_rscp, Endian::Little);
                writer.write_i16(*&self.cpich_ecno, Endian::Little);
                writer.write_i16(*&self.cell_selection_rx_level, Endian::Little);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.primary_scrambling_code = reader.read_u16(Endian::Little)?;
                out.cpich_rscp = reader.read_i16(Endian::Little)?;
                out.cpich_ecno = reader.read_i16(Endian::Little)?;
                out.cell_selection_rx_level = reader.read_i16(Endian::Little)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Element9 {
            pub utra_absolute_rf_channel_number: u16,
            pub cell_reselection_priority: u8,
            pub cell_reselection_high_threshold: u16,
            pub cell_reselection_low_threshold: u16,
            pub cell: Vec<Element10>,
        }
        impl Element9 {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u16(*&self.utra_absolute_rf_channel_number, Endian::Little);
                writer.write_u8(*&self.cell_reselection_priority);
                writer.write_u16(*&self.cell_reselection_high_threshold, Endian::Little);
                writer.write_u16(*&self.cell_reselection_low_threshold, Endian::Little);
                writer.write_u8(u8::try_from((&self.cell).len()).map_err(|_| Error::TooLong)?);
                for item in &self.cell {
                    (item).encode_to(writer)?;
                }
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.utra_absolute_rf_channel_number = reader.read_u16(Endian::Little)?;
                out.cell_reselection_priority = reader.read_u8()?;
                out.cell_reselection_high_threshold = reader.read_u16(Endian::Little)?;
                out.cell_reselection_low_threshold = reader.read_u16(Endian::Little)?;
                let count = reader.read_u8()? as usize;
                let mut items = Vec::with_capacity(count.min(256));
                for _ in 0..count {
                    items.push(Element10::decode_from(reader)?);
                }
                out.cell = items;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct LteInfoNeighboringWcdma {
            pub ue_in_idle: bool,
            pub frequency: Vec<Element9>,
        }
        impl LteInfoNeighboringWcdma {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(u8::from(*&self.ue_in_idle));
                writer.write_u8(u8::try_from((&self.frequency).len()).map_err(|_| Error::TooLong)?);
                for item in &self.frequency {
                    (item).encode_to(writer)?;
                }
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.ue_in_idle = reader.read_u8()? != 0;
                let count = reader.read_u8()? as usize;
                let mut items = Vec::with_capacity(count.min(256));
                for _ in 0..count {
                    items.push(Element9::decode_from(reader)?);
                }
                out.frequency = items;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Element11 {
            pub eutra_absolute_rf_channel_number: u16,
            pub physical_cell_id: u16,
            pub rsrp: f32,
            pub rsrq: f32,
            pub cell_selection_rx_level: i16,
            pub is_tdd: bool,
        }
        impl Element11 {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u16(*&self.eutra_absolute_rf_channel_number, Endian::Little);
                writer.write_u16(*&self.physical_cell_id, Endian::Little);
                writer.write_f32(*&self.rsrp, Endian::Little);
                writer.write_f32(*&self.rsrq, Endian::Little);
                writer.write_i16(*&self.cell_selection_rx_level, Endian::Little);
                writer.write_u8(u8::from(*&self.is_tdd));
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.eutra_absolute_rf_channel_number = reader.read_u16(Endian::Little)?;
                out.physical_cell_id = reader.read_u16(Endian::Little)?;
                out.rsrp = reader.read_f32(Endian::Little)?;
                out.rsrq = reader.read_f32(Endian::Little)?;
                out.cell_selection_rx_level = reader.read_i16(Endian::Little)?;
                out.is_tdd = reader.read_u8()? != 0;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct UmtsInfoNeighboringLte {
            pub rrc_state: u32,
            pub frequency: Vec<Element11>,
        }
        impl UmtsInfoNeighboringLte {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u32(*&self.rrc_state, Endian::Little);
                writer.write_u8(u8::try_from((&self.frequency).len()).map_err(|_| Error::TooLong)?);
                for item in &self.frequency {
                    (item).encode_to(writer)?;
                }
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.rrc_state = reader.read_u32(Endian::Little)?;
                let count = reader.read_u8()? as usize;
                let mut items = Vec::with_capacity(count.min(256));
                for _ in 0..count {
                    items.push(Element11::decode_from(reader)?);
                }
                out.frequency = items;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Nr5gCellInformation {
            pub plmn: Vec<u8>,
            pub tracking_area_code: Vec<u8>,
            pub global_cell_id: u64,
            pub physical_cell_id: u16,
            pub rsrq: i16,
            pub rsrp: i16,
            pub snr: i16,
        }
        impl Nr5gCellInformation {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                if (&self.plmn).len() != 3 {
                    return Err(Error::InvalidLength);
                }
                for item in &self.plmn {
                    writer.write_u8(*item);
                }
                if (&self.tracking_area_code).len() != 3 {
                    return Err(Error::InvalidLength);
                }
                for item in &self.tracking_area_code {
                    writer.write_u8(*item);
                }
                writer.write_u64(*&self.global_cell_id, Endian::Little);
                writer.write_u16(*&self.physical_cell_id, Endian::Little);
                writer.write_i16(*&self.rsrq, Endian::Little);
                writer.write_i16(*&self.rsrp, Endian::Little);
                writer.write_i16(*&self.snr, Endian::Little);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                let count = 3;
                let mut items = Vec::with_capacity(count.min(256));
                for _ in 0..count {
                    items.push(reader.read_u8()?);
                }
                out.plmn = items;
                let count = 3;
                let mut items = Vec::with_capacity(count.min(256));
                for _ in 0..count {
                    items.push(reader.read_u8()?);
                }
                out.tracking_area_code = items;
                out.global_cell_id = reader.read_u64(Endian::Little)?;
                out.physical_cell_id = reader.read_u16(Endian::Little)?;
                out.rsrq = reader.read_i16(Endian::Little)?;
                out.rsrp = reader.read_i16(Endian::Little)?;
                out.snr = reader.read_i16(Endian::Little)?;
                Ok(out)
            }
        }
    }
    #[doc = "Get PLMN Name"]
    pub mod get_plmn_name {
        use crate::{Endian, Error, TlvBuf, TlvReader, TlvWriter};
        use alloc::string::String;
        use alloc::vec::Vec;
        pub const MESSAGE_ID: u16 = 68;
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Request {
            pub plmn: Plmn,
            pub suppress_sim_error: Option<bool>,
            pub mnc_pcs_digit_include_status: Option<bool>,
            pub always_send_plmn_name: Option<bool>,
            pub use_static_table_only: Option<bool>,
            pub csg_id: Option<u32>,
            pub radio_access_technology: Option<u8>,
            pub send_all_information: Option<bool>,
        }
        impl Request {
            pub const TLV_PLMN: u8 = 1;
            pub const TLV_SUPPRESS_SIM_ERROR: u8 = 16;
            pub const TLV_MNC_PCS_DIGIT_INCLUDE_STATUS: u8 = 17;
            pub const TLV_ALWAYS_SEND_PLMN_NAME: u8 = 18;
            pub const TLV_USE_STATIC_TABLE_ONLY: u8 = 19;
            pub const TLV_CSG_ID: u8 = 20;
            pub const TLV_RADIO_ACCESS_TECHNOLOGY: u8 = 21;
            pub const TLV_SEND_ALL_INFORMATION: u8 = 22;
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = Vec::new();
                {
                    let mut writer = TlvWriter::new();
                    (&self.plmn).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(1));
                }
                if let Some(value) = &self.suppress_sim_error {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::from(*value));
                    tlvs.push(writer.into_tlv(16));
                }
                if let Some(value) = &self.mnc_pcs_digit_include_status {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::from(*value));
                    tlvs.push(writer.into_tlv(17));
                }
                if let Some(value) = &self.always_send_plmn_name {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::from(*value));
                    tlvs.push(writer.into_tlv(18));
                }
                if let Some(value) = &self.use_static_table_only {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::from(*value));
                    tlvs.push(writer.into_tlv(19));
                }
                if let Some(value) = &self.csg_id {
                    let mut writer = TlvWriter::new();
                    writer.write_u32(*value, Endian::Little);
                    tlvs.push(writer.into_tlv(20));
                }
                if let Some(value) = &self.radio_access_technology {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(*value);
                    tlvs.push(writer.into_tlv(21));
                }
                if let Some(value) = &self.send_all_information {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::from(*value));
                    tlvs.push(writer.into_tlv(22));
                }
                Ok(tlvs)
            }
            pub fn from_tlvs(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                let mut out = Self::default();
                let tlv = tlvs
                    .iter()
                    .find(|tlv| tlv.id == 1)
                    .ok_or(Error::MissingTlv { id: 1 })?;
                let mut reader = TlvReader::new(&tlv.value);
                out.plmn = Plmn::decode_from(&mut reader)?;
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 16) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u8()? != 0;
                    out.suppress_sim_error = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 17) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u8()? != 0;
                    out.mnc_pcs_digit_include_status = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 18) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u8()? != 0;
                    out.always_send_plmn_name = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 19) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u8()? != 0;
                    out.use_static_table_only = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 20) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u32(Endian::Little)?;
                    out.csg_id = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 21) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u8()?;
                    out.radio_access_technology = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 22) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u8()? != 0;
                    out.send_all_information = Some(value);
                }
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Response {
            pub n_3gpp_eons_plmn_name: Option<N3gppEonsPlmnName>,
            pub display_bit_information: Option<DisplayBitInformation>,
            pub network_information: Option<u32>,
            pub plmn_name_with_language_id: Option<Vec<Element>>,
            pub additional_information: Option<Vec<u16>>,
            pub network_name_source: Option<u32>,
        }
        impl Response {
            pub const TLV_RESULT: u8 = 2;
            pub const TLV_N_3GPP_EONS_PLMN_NAME: u8 = 16;
            pub const TLV_DISPLAY_BIT_INFORMATION: u8 = 17;
            pub const TLV_NETWORK_INFORMATION: u8 = 18;
            pub const TLV_PLMN_NAME_WITH_LANGUAGE_ID: u8 = 19;
            pub const TLV_ADDITIONAL_INFORMATION: u8 = 20;
            pub const TLV_NETWORK_NAME_SOURCE: u8 = 21;
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = Vec::new();
                if let Some(value) = &self.n_3gpp_eons_plmn_name {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(16));
                }
                if let Some(value) = &self.display_bit_information {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(17));
                }
                if let Some(value) = &self.network_information {
                    let mut writer = TlvWriter::new();
                    writer.write_u32(*value, Endian::Little);
                    tlvs.push(writer.into_tlv(18));
                }
                if let Some(value) = &self.plmn_name_with_language_id {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::try_from((value).len()).map_err(|_| Error::TooLong)?);
                    for item in value {
                        (item).encode_to(&mut writer)?;
                    }
                    tlvs.push(writer.into_tlv(19));
                }
                if let Some(value) = &self.additional_information {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::try_from((value).len()).map_err(|_| Error::TooLong)?);
                    for item in value {
                        writer.write_u16(*item, Endian::Little);
                    }
                    tlvs.push(writer.into_tlv(20));
                }
                if let Some(value) = &self.network_name_source {
                    let mut writer = TlvWriter::new();
                    writer.write_u32(*value, Endian::Little);
                    tlvs.push(writer.into_tlv(21));
                }
                Ok(tlvs)
            }
            pub fn from_tlvs(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                let mut out = Self::default();
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 16) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = N3gppEonsPlmnName::decode_from(&mut reader)?;
                    out.n_3gpp_eons_plmn_name = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 17) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = DisplayBitInformation::decode_from(&mut reader)?;
                    out.display_bit_information = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 18) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u32(Endian::Little)?;
                    out.network_information = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 19) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let count = reader.read_u8()? as usize;
                    let mut items = Vec::with_capacity(count.min(256));
                    for _ in 0..count {
                        items.push(Element::decode_from(&mut reader)?);
                    }
                    let value = items;
                    out.plmn_name_with_language_id = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 20) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let count = reader.read_u8()? as usize;
                    let mut items = Vec::with_capacity(count.min(256));
                    for _ in 0..count {
                        items.push(reader.read_u16(Endian::Little)?);
                    }
                    let value = items;
                    out.additional_information = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 21) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u32(Endian::Little)?;
                    out.network_name_source = Some(value);
                }
                Ok(out)
            }
        }
        impl crate::Response for Response {
            type Request = Request;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = crate::result::success_tlvs();
                tlvs.extend(self.to_tlvs()?);
                Ok(tlvs)
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                crate::result::check_result(tlvs)?;
                Response::from_tlvs(tlvs)
            }
        }
        impl crate::Request for Request {
            const SERVICE: u16 = super::super::SERVICE_ID;
            const MESSAGE_ID: u16 = 68;
            type Response = Response;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                self.to_tlvs()
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Self::from_tlvs(tlvs)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Plmn {
            pub mcc: u16,
            pub mnc: u16,
        }
        impl Plmn {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u16(*&self.mcc, Endian::Little);
                writer.write_u16(*&self.mnc, Endian::Little);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.mcc = reader.read_u16(Endian::Little)?;
                out.mnc = reader.read_u16(Endian::Little)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct N3gppEonsPlmnName {
            pub service_provider_name_encoding: u8,
            pub service_provider_name: Vec<u8>,
            pub short_name_encoding: u8,
            pub short_name_country_initials: u8,
            pub short_name_spare_bits: u8,
            pub short_name: Vec<u8>,
            pub long_name_encoding: u8,
            pub long_name_country_initials: u8,
            pub long_name_spare_bits: u8,
            pub long_name: Vec<u8>,
        }
        impl N3gppEonsPlmnName {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(*&self.service_provider_name_encoding);
                writer.write_u8(
                    u8::try_from((&self.service_provider_name).len())
                        .map_err(|_| Error::TooLong)?,
                );
                for item in &self.service_provider_name {
                    writer.write_u8(*item);
                }
                writer.write_u8(*&self.short_name_encoding);
                writer.write_u8(*&self.short_name_country_initials);
                writer.write_u8(*&self.short_name_spare_bits);
                writer
                    .write_u8(u8::try_from((&self.short_name).len()).map_err(|_| Error::TooLong)?);
                for item in &self.short_name {
                    writer.write_u8(*item);
                }
                writer.write_u8(*&self.long_name_encoding);
                writer.write_u8(*&self.long_name_country_initials);
                writer.write_u8(*&self.long_name_spare_bits);
                writer.write_u8(u8::try_from((&self.long_name).len()).map_err(|_| Error::TooLong)?);
                for item in &self.long_name {
                    writer.write_u8(*item);
                }
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.service_provider_name_encoding = reader.read_u8()?;
                let count = reader.read_u8()? as usize;
                let mut items = Vec::with_capacity(count.min(256));
                for _ in 0..count {
                    items.push(reader.read_u8()?);
                }
                out.service_provider_name = items;
                out.short_name_encoding = reader.read_u8()?;
                out.short_name_country_initials = reader.read_u8()?;
                out.short_name_spare_bits = reader.read_u8()?;
                let count = reader.read_u8()? as usize;
                let mut items = Vec::with_capacity(count.min(256));
                for _ in 0..count {
                    items.push(reader.read_u8()?);
                }
                out.short_name = items;
                out.long_name_encoding = reader.read_u8()?;
                out.long_name_country_initials = reader.read_u8()?;
                out.long_name_spare_bits = reader.read_u8()?;
                let count = reader.read_u8()? as usize;
                let mut items = Vec::with_capacity(count.min(256));
                for _ in 0..count {
                    items.push(reader.read_u8()?);
                }
                out.long_name = items;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct DisplayBitInformation {
            pub service_provider_name_set: u32,
            pub plmn_name_set: u32,
        }
        impl DisplayBitInformation {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u32(*&self.service_provider_name_set, Endian::Little);
                writer.write_u32(*&self.plmn_name_set, Endian::Little);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.service_provider_name_set = reader.read_u32(Endian::Little)?;
                out.plmn_name_set = reader.read_u32(Endian::Little)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Element {
            pub long_name: Vec<u16>,
            pub short_name: Vec<u16>,
            pub language_id: u32,
        }
        impl Element {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(u8::try_from((&self.long_name).len()).map_err(|_| Error::TooLong)?);
                for item in &self.long_name {
                    writer.write_u16(*item, Endian::Little);
                }
                writer
                    .write_u8(u8::try_from((&self.short_name).len()).map_err(|_| Error::TooLong)?);
                for item in &self.short_name {
                    writer.write_u16(*item, Endian::Little);
                }
                writer.write_u32(*&self.language_id, Endian::Little);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                let count = reader.read_u8()? as usize;
                let mut items = Vec::with_capacity(count.min(256));
                for _ in 0..count {
                    items.push(reader.read_u16(Endian::Little)?);
                }
                out.long_name = items;
                let count = reader.read_u8()? as usize;
                let mut items = Vec::with_capacity(count.min(256));
                for _ in 0..count {
                    items.push(reader.read_u16(Endian::Little)?);
                }
                out.short_name = items;
                out.language_id = reader.read_u32(Endian::Little)?;
                Ok(out)
            }
        }
    }
    #[doc = "Get System Info"]
    pub mod get_system_info {
        use crate::{Endian, Error, TlvBuf, TlvReader, TlvWriter};
        use alloc::string::String;
        use alloc::vec::Vec;
        pub const MESSAGE_ID: u16 = 77;
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Request {}
        impl Request {
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                Ok(Vec::new())
            }
            pub fn from_tlvs(_tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Ok(Self::default())
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Response {
            pub cdma_service_status: Option<CdmaServiceStatus>,
            pub hdr_service_status: Option<HdrServiceStatus>,
            pub gsm_service_status: Option<GsmServiceStatus>,
            pub wcdma_service_status: Option<WcdmaServiceStatus>,
            pub lte_service_status: Option<LteServiceStatus>,
            pub cdma_system_info: Option<CdmaSystemInfo>,
            pub hdr_system_info: Option<HdrSystemInfo>,
            pub gsm_system_info_v2: Option<GsmSystemInfoV2>,
            pub wcdma_system_info_v2: Option<WcdmaSystemInfoV2>,
            pub lte_system_info_v2: Option<LteSystemInfoV2>,
            pub additional_cdma_system_info: Option<AdditionalCdmaSystemInfo>,
            pub additional_hdr_system_info: Option<AdditionalHdrSystemInfo>,
            pub additional_gsm_system_info: Option<AdditionalGsmSystemInfo>,
            pub additional_wcdma_system_info: Option<AdditionalWcdmaSystemInfo>,
            pub additional_lte_system_info: Option<AdditionalLteSystemInfo>,
            pub gsm_call_barring_status: Option<GsmCallBarringStatus>,
            pub wcdma_call_barring_status: Option<WcdmaCallBarringStatus>,
            pub lte_voice_support: Option<bool>,
            pub gsm_cipher_domain: Option<u8>,
            pub wcdma_cipher_domain: Option<u8>,
            pub td_scdma_service_status: Option<TdScdmaServiceStatus>,
            pub td_scdma_system_info_v2: Option<TdScdmaSystemInfoV2>,
            pub lte_embms_coverage_info_support: Option<bool>,
            pub sim_reject_info: Option<u32>,
            pub ims_voice_support: Option<bool>,
            pub lte_voice_domain: Option<u32>,
            pub cdma_registration_zone_id: Option<u16>,
            pub gsm_routing_area_code: Option<u8>,
            pub wcdma_routing_area_code: Option<u8>,
            pub cdma_resolved_mcc: Option<u16>,
            pub network_selection_registration_restriction: Option<u32>,
            pub lte_registration_domain: Option<u32>,
            pub lte_embms_coverage_info_trace_id: Option<u16>,
            pub lte_cell_access_status: Option<u32>,
            pub nr5g_service_status_info: Option<Nr5gServiceStatusInfo>,
            pub nr5g_system_info: Option<Nr5gSystemInfo>,
            pub eutra_with_nr5g_availability: Option<bool>,
            pub dcnr_restriction_info: Option<bool>,
            pub nr5g_tracking_area_code: Option<Vec<u8>>,
        }
        impl Response {
            pub const TLV_RESULT: u8 = 2;
            pub const TLV_CDMA_SERVICE_STATUS: u8 = 16;
            pub const TLV_HDR_SERVICE_STATUS: u8 = 17;
            pub const TLV_GSM_SERVICE_STATUS: u8 = 18;
            pub const TLV_WCDMA_SERVICE_STATUS: u8 = 19;
            pub const TLV_LTE_SERVICE_STATUS: u8 = 20;
            pub const TLV_CDMA_SYSTEM_INFO: u8 = 21;
            pub const TLV_HDR_SYSTEM_INFO: u8 = 22;
            pub const TLV_GSM_SYSTEM_INFO_V2: u8 = 23;
            pub const TLV_WCDMA_SYSTEM_INFO_V2: u8 = 24;
            pub const TLV_LTE_SYSTEM_INFO_V2: u8 = 25;
            pub const TLV_ADDITIONAL_CDMA_SYSTEM_INFO: u8 = 26;
            pub const TLV_ADDITIONAL_HDR_SYSTEM_INFO: u8 = 27;
            pub const TLV_ADDITIONAL_GSM_SYSTEM_INFO: u8 = 28;
            pub const TLV_ADDITIONAL_WCDMA_SYSTEM_INFO: u8 = 29;
            pub const TLV_ADDITIONAL_LTE_SYSTEM_INFO: u8 = 30;
            pub const TLV_GSM_CALL_BARRING_STATUS: u8 = 31;
            pub const TLV_WCDMA_CALL_BARRING_STATUS: u8 = 32;
            pub const TLV_LTE_VOICE_SUPPORT: u8 = 33;
            pub const TLV_GSM_CIPHER_DOMAIN: u8 = 34;
            pub const TLV_WCDMA_CIPHER_DOMAIN: u8 = 35;
            pub const TLV_TD_SCDMA_SERVICE_STATUS: u8 = 36;
            pub const TLV_TD_SCDMA_SYSTEM_INFO_V2: u8 = 37;
            pub const TLV_LTE_EMBMS_COVERAGE_INFO_SUPPORT: u8 = 38;
            pub const TLV_SIM_REJECT_INFO: u8 = 39;
            pub const TLV_IMS_VOICE_SUPPORT: u8 = 41;
            pub const TLV_LTE_VOICE_DOMAIN: u8 = 42;
            pub const TLV_CDMA_REGISTRATION_ZONE_ID: u8 = 43;
            pub const TLV_GSM_ROUTING_AREA_CODE: u8 = 44;
            pub const TLV_WCDMA_ROUTING_AREA_CODE: u8 = 45;
            pub const TLV_CDMA_RESOLVED_MCC: u8 = 46;
            pub const TLV_NETWORK_SELECTION_REGISTRATION_RESTRICTION: u8 = 47;
            pub const TLV_LTE_REGISTRATION_DOMAIN: u8 = 49;
            pub const TLV_LTE_EMBMS_COVERAGE_INFO_TRACE_ID: u8 = 52;
            pub const TLV_LTE_CELL_ACCESS_STATUS: u8 = 68;
            pub const TLV_NR5G_SERVICE_STATUS_INFO: u8 = 74;
            pub const TLV_NR5G_SYSTEM_INFO: u8 = 75;
            pub const TLV_EUTRA_WITH_NR5G_AVAILABILITY: u8 = 78;
            pub const TLV_DCNR_RESTRICTION_INFO: u8 = 79;
            pub const TLV_NR5G_TRACKING_AREA_CODE: u8 = 80;
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = Vec::new();
                if let Some(value) = &self.cdma_service_status {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(16));
                }
                if let Some(value) = &self.hdr_service_status {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(17));
                }
                if let Some(value) = &self.gsm_service_status {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(18));
                }
                if let Some(value) = &self.wcdma_service_status {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(19));
                }
                if let Some(value) = &self.lte_service_status {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(20));
                }
                if let Some(value) = &self.cdma_system_info {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(21));
                }
                if let Some(value) = &self.hdr_system_info {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(22));
                }
                if let Some(value) = &self.gsm_system_info_v2 {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(23));
                }
                if let Some(value) = &self.wcdma_system_info_v2 {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(24));
                }
                if let Some(value) = &self.lte_system_info_v2 {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(25));
                }
                if let Some(value) = &self.additional_cdma_system_info {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(26));
                }
                if let Some(value) = &self.additional_hdr_system_info {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(27));
                }
                if let Some(value) = &self.additional_gsm_system_info {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(28));
                }
                if let Some(value) = &self.additional_wcdma_system_info {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(29));
                }
                if let Some(value) = &self.additional_lte_system_info {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(30));
                }
                if let Some(value) = &self.gsm_call_barring_status {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(31));
                }
                if let Some(value) = &self.wcdma_call_barring_status {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(32));
                }
                if let Some(value) = &self.lte_voice_support {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::from(*value));
                    tlvs.push(writer.into_tlv(33));
                }
                if let Some(value) = &self.gsm_cipher_domain {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(*value);
                    tlvs.push(writer.into_tlv(34));
                }
                if let Some(value) = &self.wcdma_cipher_domain {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(*value);
                    tlvs.push(writer.into_tlv(35));
                }
                if let Some(value) = &self.td_scdma_service_status {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(36));
                }
                if let Some(value) = &self.td_scdma_system_info_v2 {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(37));
                }
                if let Some(value) = &self.lte_embms_coverage_info_support {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::from(*value));
                    tlvs.push(writer.into_tlv(38));
                }
                if let Some(value) = &self.sim_reject_info {
                    let mut writer = TlvWriter::new();
                    writer.write_u32(*value, Endian::Little);
                    tlvs.push(writer.into_tlv(39));
                }
                if let Some(value) = &self.ims_voice_support {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::from(*value));
                    tlvs.push(writer.into_tlv(41));
                }
                if let Some(value) = &self.lte_voice_domain {
                    let mut writer = TlvWriter::new();
                    writer.write_u32(*value, Endian::Little);
                    tlvs.push(writer.into_tlv(42));
                }
                if let Some(value) = &self.cdma_registration_zone_id {
                    let mut writer = TlvWriter::new();
                    writer.write_u16(*value, Endian::Little);
                    tlvs.push(writer.into_tlv(43));
                }
                if let Some(value) = &self.gsm_routing_area_code {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(*value);
                    tlvs.push(writer.into_tlv(44));
                }
                if let Some(value) = &self.wcdma_routing_area_code {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(*value);
                    tlvs.push(writer.into_tlv(45));
                }
                if let Some(value) = &self.cdma_resolved_mcc {
                    let mut writer = TlvWriter::new();
                    writer.write_u16(*value, Endian::Little);
                    tlvs.push(writer.into_tlv(46));
                }
                if let Some(value) = &self.network_selection_registration_restriction {
                    let mut writer = TlvWriter::new();
                    writer.write_u32(*value, Endian::Little);
                    tlvs.push(writer.into_tlv(47));
                }
                if let Some(value) = &self.lte_registration_domain {
                    let mut writer = TlvWriter::new();
                    writer.write_u32(*value, Endian::Little);
                    tlvs.push(writer.into_tlv(49));
                }
                if let Some(value) = &self.lte_embms_coverage_info_trace_id {
                    let mut writer = TlvWriter::new();
                    writer.write_u16(*value, Endian::Little);
                    tlvs.push(writer.into_tlv(52));
                }
                if let Some(value) = &self.lte_cell_access_status {
                    let mut writer = TlvWriter::new();
                    writer.write_u32(*value, Endian::Little);
                    tlvs.push(writer.into_tlv(68));
                }
                if let Some(value) = &self.nr5g_service_status_info {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(74));
                }
                if let Some(value) = &self.nr5g_system_info {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(75));
                }
                if let Some(value) = &self.eutra_with_nr5g_availability {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::from(*value));
                    tlvs.push(writer.into_tlv(78));
                }
                if let Some(value) = &self.dcnr_restriction_info {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::from(*value));
                    tlvs.push(writer.into_tlv(79));
                }
                if let Some(value) = &self.nr5g_tracking_area_code {
                    let mut writer = TlvWriter::new();
                    if (value).len() != 3 {
                        return Err(Error::InvalidLength);
                    }
                    for item in value {
                        writer.write_u8(*item);
                    }
                    tlvs.push(writer.into_tlv(80));
                }
                Ok(tlvs)
            }
            pub fn from_tlvs(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                let mut out = Self::default();
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 16) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = CdmaServiceStatus::decode_from(&mut reader)?;
                    out.cdma_service_status = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 17) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = HdrServiceStatus::decode_from(&mut reader)?;
                    out.hdr_service_status = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 18) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = GsmServiceStatus::decode_from(&mut reader)?;
                    out.gsm_service_status = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 19) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = WcdmaServiceStatus::decode_from(&mut reader)?;
                    out.wcdma_service_status = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 20) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = LteServiceStatus::decode_from(&mut reader)?;
                    out.lte_service_status = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 21) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = CdmaSystemInfo::decode_from(&mut reader)?;
                    out.cdma_system_info = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 22) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = HdrSystemInfo::decode_from(&mut reader)?;
                    out.hdr_system_info = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 23) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = GsmSystemInfoV2::decode_from(&mut reader)?;
                    out.gsm_system_info_v2 = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 24) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = WcdmaSystemInfoV2::decode_from(&mut reader)?;
                    out.wcdma_system_info_v2 = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 25) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = LteSystemInfoV2::decode_from(&mut reader)?;
                    out.lte_system_info_v2 = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 26) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = AdditionalCdmaSystemInfo::decode_from(&mut reader)?;
                    out.additional_cdma_system_info = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 27) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = AdditionalHdrSystemInfo::decode_from(&mut reader)?;
                    out.additional_hdr_system_info = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 28) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = AdditionalGsmSystemInfo::decode_from(&mut reader)?;
                    out.additional_gsm_system_info = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 29) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = AdditionalWcdmaSystemInfo::decode_from(&mut reader)?;
                    out.additional_wcdma_system_info = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 30) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = AdditionalLteSystemInfo::decode_from(&mut reader)?;
                    out.additional_lte_system_info = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 31) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = GsmCallBarringStatus::decode_from(&mut reader)?;
                    out.gsm_call_barring_status = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 32) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = WcdmaCallBarringStatus::decode_from(&mut reader)?;
                    out.wcdma_call_barring_status = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 33) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u8()? != 0;
                    out.lte_voice_support = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 34) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u8()?;
                    out.gsm_cipher_domain = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 35) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u8()?;
                    out.wcdma_cipher_domain = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 36) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = TdScdmaServiceStatus::decode_from(&mut reader)?;
                    out.td_scdma_service_status = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 37) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = TdScdmaSystemInfoV2::decode_from(&mut reader)?;
                    out.td_scdma_system_info_v2 = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 38) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u8()? != 0;
                    out.lte_embms_coverage_info_support = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 39) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u32(Endian::Little)?;
                    out.sim_reject_info = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 41) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u8()? != 0;
                    out.ims_voice_support = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 42) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u32(Endian::Little)?;
                    out.lte_voice_domain = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 43) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u16(Endian::Little)?;
                    out.cdma_registration_zone_id = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 44) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u8()?;
                    out.gsm_routing_area_code = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 45) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u8()?;
                    out.wcdma_routing_area_code = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 46) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u16(Endian::Little)?;
                    out.cdma_resolved_mcc = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 47) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u32(Endian::Little)?;
                    out.network_selection_registration_restriction = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 49) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u32(Endian::Little)?;
                    out.lte_registration_domain = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 52) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u16(Endian::Little)?;
                    out.lte_embms_coverage_info_trace_id = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 68) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u32(Endian::Little)?;
                    out.lte_cell_access_status = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 74) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = Nr5gServiceStatusInfo::decode_from(&mut reader)?;
                    out.nr5g_service_status_info = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 75) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = Nr5gSystemInfo::decode_from(&mut reader)?;
                    out.nr5g_system_info = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 78) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u8()? != 0;
                    out.eutra_with_nr5g_availability = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 79) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u8()? != 0;
                    out.dcnr_restriction_info = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 80) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let count = 3;
                    let mut items = Vec::with_capacity(count.min(256));
                    for _ in 0..count {
                        items.push(reader.read_u8()?);
                    }
                    let value = items;
                    out.nr5g_tracking_area_code = Some(value);
                }
                Ok(out)
            }
        }
        impl crate::Response for Response {
            type Request = Request;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = crate::result::success_tlvs();
                tlvs.extend(self.to_tlvs()?);
                Ok(tlvs)
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                crate::result::check_result(tlvs)?;
                Response::from_tlvs(tlvs)
            }
        }
        impl crate::Request for Request {
            const SERVICE: u16 = super::super::SERVICE_ID;
            const MESSAGE_ID: u16 = 77;
            type Response = Response;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                self.to_tlvs()
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Self::from_tlvs(tlvs)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct CdmaServiceStatus {
            pub service_status: u8,
            pub preferred_data_path: bool,
        }
        impl CdmaServiceStatus {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(*&self.service_status);
                writer.write_u8(u8::from(*&self.preferred_data_path));
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.service_status = reader.read_u8()?;
                out.preferred_data_path = reader.read_u8()? != 0;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct HdrServiceStatus {
            pub service_status: u8,
            pub preferred_data_path: bool,
        }
        impl HdrServiceStatus {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(*&self.service_status);
                writer.write_u8(u8::from(*&self.preferred_data_path));
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.service_status = reader.read_u8()?;
                out.preferred_data_path = reader.read_u8()? != 0;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct GsmServiceStatus {
            pub service_status: u8,
            pub true_service_status: u8,
            pub preferred_data_path: bool,
        }
        impl GsmServiceStatus {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(*&self.service_status);
                writer.write_u8(*&self.true_service_status);
                writer.write_u8(u8::from(*&self.preferred_data_path));
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.service_status = reader.read_u8()?;
                out.true_service_status = reader.read_u8()?;
                out.preferred_data_path = reader.read_u8()? != 0;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct WcdmaServiceStatus {
            pub service_status: u8,
            pub true_service_status: u8,
            pub preferred_data_path: bool,
        }
        impl WcdmaServiceStatus {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(*&self.service_status);
                writer.write_u8(*&self.true_service_status);
                writer.write_u8(u8::from(*&self.preferred_data_path));
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.service_status = reader.read_u8()?;
                out.true_service_status = reader.read_u8()?;
                out.preferred_data_path = reader.read_u8()? != 0;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct LteServiceStatus {
            pub service_status: u8,
            pub true_service_status: u8,
            pub preferred_data_path: bool,
        }
        impl LteServiceStatus {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(*&self.service_status);
                writer.write_u8(*&self.true_service_status);
                writer.write_u8(u8::from(*&self.preferred_data_path));
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.service_status = reader.read_u8()?;
                out.true_service_status = reader.read_u8()?;
                out.preferred_data_path = reader.read_u8()? != 0;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct CdmaSystemInfo {
            pub domain_valid: bool,
            pub domain: u8,
            pub service_capability_valid: bool,
            pub service_capability: u8,
            pub roaming_status_valid: bool,
            pub roaming_status: u8,
            pub forbidden_valid: bool,
            pub forbidden: bool,
            pub prl_match_valid: bool,
            pub prl_match: bool,
            pub p_rev_valid: bool,
            pub p_rev: u8,
            pub base_station_p_rev_valid: bool,
            pub base_station_p_rev: u8,
            pub concurrent_service_support_valid: bool,
            pub concurrent_service_support: bool,
            pub cdma_system_id_valid: bool,
            pub sid: u16,
            pub nid: u16,
            pub base_station_info_valid: bool,
            pub base_station_id: u16,
            pub base_station_latitude: i32,
            pub base_station_longitude: i32,
            pub packet_zone_valid: bool,
            pub packet_zone: u16,
            pub network_id_valid: bool,
            pub mcc: String,
            pub mnc: String,
        }
        impl CdmaSystemInfo {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(u8::from(*&self.domain_valid));
                writer.write_u8(*&self.domain);
                writer.write_u8(u8::from(*&self.service_capability_valid));
                writer.write_u8(*&self.service_capability);
                writer.write_u8(u8::from(*&self.roaming_status_valid));
                writer.write_u8(*&self.roaming_status);
                writer.write_u8(u8::from(*&self.forbidden_valid));
                writer.write_u8(u8::from(*&self.forbidden));
                writer.write_u8(u8::from(*&self.prl_match_valid));
                writer.write_u8(u8::from(*&self.prl_match));
                writer.write_u8(u8::from(*&self.p_rev_valid));
                writer.write_u8(*&self.p_rev);
                writer.write_u8(u8::from(*&self.base_station_p_rev_valid));
                writer.write_u8(*&self.base_station_p_rev);
                writer.write_u8(u8::from(*&self.concurrent_service_support_valid));
                writer.write_u8(u8::from(*&self.concurrent_service_support));
                writer.write_u8(u8::from(*&self.cdma_system_id_valid));
                writer.write_u16(*&self.sid, Endian::Little);
                writer.write_u16(*&self.nid, Endian::Little);
                writer.write_u8(u8::from(*&self.base_station_info_valid));
                writer.write_u16(*&self.base_station_id, Endian::Little);
                writer.write_i32(*&self.base_station_latitude, Endian::Little);
                writer.write_i32(*&self.base_station_longitude, Endian::Little);
                writer.write_u8(u8::from(*&self.packet_zone_valid));
                writer.write_u16(*&self.packet_zone, Endian::Little);
                writer.write_u8(u8::from(*&self.network_id_valid));
                writer.write_string(&self.mcc, 0, Some(3))?;
                writer.write_string(&self.mnc, 0, Some(3))?;
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.domain_valid = reader.read_u8()? != 0;
                out.domain = reader.read_u8()?;
                out.service_capability_valid = reader.read_u8()? != 0;
                out.service_capability = reader.read_u8()?;
                out.roaming_status_valid = reader.read_u8()? != 0;
                out.roaming_status = reader.read_u8()?;
                out.forbidden_valid = reader.read_u8()? != 0;
                out.forbidden = reader.read_u8()? != 0;
                out.prl_match_valid = reader.read_u8()? != 0;
                out.prl_match = reader.read_u8()? != 0;
                out.p_rev_valid = reader.read_u8()? != 0;
                out.p_rev = reader.read_u8()?;
                out.base_station_p_rev_valid = reader.read_u8()? != 0;
                out.base_station_p_rev = reader.read_u8()?;
                out.concurrent_service_support_valid = reader.read_u8()? != 0;
                out.concurrent_service_support = reader.read_u8()? != 0;
                out.cdma_system_id_valid = reader.read_u8()? != 0;
                out.sid = reader.read_u16(Endian::Little)?;
                out.nid = reader.read_u16(Endian::Little)?;
                out.base_station_info_valid = reader.read_u8()? != 0;
                out.base_station_id = reader.read_u16(Endian::Little)?;
                out.base_station_latitude = reader.read_i32(Endian::Little)?;
                out.base_station_longitude = reader.read_i32(Endian::Little)?;
                out.packet_zone_valid = reader.read_u8()? != 0;
                out.packet_zone = reader.read_u16(Endian::Little)?;
                out.network_id_valid = reader.read_u8()? != 0;
                out.mcc = reader.read_fixed_string(3)?;
                out.mnc = reader.read_fixed_string(3)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct HdrSystemInfo {
            pub domain_valid: bool,
            pub domain: u8,
            pub service_capability_valid: bool,
            pub service_capability: u8,
            pub roaming_status_valid: bool,
            pub roaming_status: u8,
            pub forbidden_valid: bool,
            pub forbidden: bool,
            pub prl_match_valid: bool,
            pub prl_match: bool,
            pub personality_valid: bool,
            pub personality: u8,
            pub protocol_revision_valid: bool,
            pub protocol_revision: u8,
            pub is_856_system_id_valid: bool,
            pub is_856_system_id: String,
        }
        impl HdrSystemInfo {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(u8::from(*&self.domain_valid));
                writer.write_u8(*&self.domain);
                writer.write_u8(u8::from(*&self.service_capability_valid));
                writer.write_u8(*&self.service_capability);
                writer.write_u8(u8::from(*&self.roaming_status_valid));
                writer.write_u8(*&self.roaming_status);
                writer.write_u8(u8::from(*&self.forbidden_valid));
                writer.write_u8(u8::from(*&self.forbidden));
                writer.write_u8(u8::from(*&self.prl_match_valid));
                writer.write_u8(u8::from(*&self.prl_match));
                writer.write_u8(u8::from(*&self.personality_valid));
                writer.write_u8(*&self.personality);
                writer.write_u8(u8::from(*&self.protocol_revision_valid));
                writer.write_u8(*&self.protocol_revision);
                writer.write_u8(u8::from(*&self.is_856_system_id_valid));
                writer.write_string(&self.is_856_system_id, 0, Some(16))?;
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.domain_valid = reader.read_u8()? != 0;
                out.domain = reader.read_u8()?;
                out.service_capability_valid = reader.read_u8()? != 0;
                out.service_capability = reader.read_u8()?;
                out.roaming_status_valid = reader.read_u8()? != 0;
                out.roaming_status = reader.read_u8()?;
                out.forbidden_valid = reader.read_u8()? != 0;
                out.forbidden = reader.read_u8()? != 0;
                out.prl_match_valid = reader.read_u8()? != 0;
                out.prl_match = reader.read_u8()? != 0;
                out.personality_valid = reader.read_u8()? != 0;
                out.personality = reader.read_u8()?;
                out.protocol_revision_valid = reader.read_u8()? != 0;
                out.protocol_revision = reader.read_u8()?;
                out.is_856_system_id_valid = reader.read_u8()? != 0;
                out.is_856_system_id = reader.read_fixed_string(16)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct GsmSystemInfoV2 {
            pub domain_valid: bool,
            pub domain: u8,
            pub service_capability_valid: bool,
            pub service_capability: u8,
            pub roaming_status_valid: bool,
            pub roaming_status: u8,
            pub forbidden_valid: bool,
            pub forbidden: bool,
            pub lac_valid: bool,
            pub lac: u16,
            pub cid_valid: bool,
            pub cid: u32,
            pub registration_reject_info_valid: bool,
            pub registration_reject_domain: u8,
            pub registration_reject_cause: u8,
            pub network_id_valid: bool,
            pub mcc: String,
            pub mnc: String,
            pub egprs_support_valid: bool,
            pub egprs_support: bool,
            pub dtm_support_valid: bool,
            pub dtm_support: bool,
        }
        impl GsmSystemInfoV2 {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(u8::from(*&self.domain_valid));
                writer.write_u8(*&self.domain);
                writer.write_u8(u8::from(*&self.service_capability_valid));
                writer.write_u8(*&self.service_capability);
                writer.write_u8(u8::from(*&self.roaming_status_valid));
                writer.write_u8(*&self.roaming_status);
                writer.write_u8(u8::from(*&self.forbidden_valid));
                writer.write_u8(u8::from(*&self.forbidden));
                writer.write_u8(u8::from(*&self.lac_valid));
                writer.write_u16(*&self.lac, Endian::Little);
                writer.write_u8(u8::from(*&self.cid_valid));
                writer.write_u32(*&self.cid, Endian::Little);
                writer.write_u8(u8::from(*&self.registration_reject_info_valid));
                writer.write_u8(*&self.registration_reject_domain);
                writer.write_u8(*&self.registration_reject_cause);
                writer.write_u8(u8::from(*&self.network_id_valid));
                writer.write_string(&self.mcc, 0, Some(3))?;
                writer.write_string(&self.mnc, 0, Some(3))?;
                writer.write_u8(u8::from(*&self.egprs_support_valid));
                writer.write_u8(u8::from(*&self.egprs_support));
                writer.write_u8(u8::from(*&self.dtm_support_valid));
                writer.write_u8(u8::from(*&self.dtm_support));
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.domain_valid = reader.read_u8()? != 0;
                out.domain = reader.read_u8()?;
                out.service_capability_valid = reader.read_u8()? != 0;
                out.service_capability = reader.read_u8()?;
                out.roaming_status_valid = reader.read_u8()? != 0;
                out.roaming_status = reader.read_u8()?;
                out.forbidden_valid = reader.read_u8()? != 0;
                out.forbidden = reader.read_u8()? != 0;
                out.lac_valid = reader.read_u8()? != 0;
                out.lac = reader.read_u16(Endian::Little)?;
                out.cid_valid = reader.read_u8()? != 0;
                out.cid = reader.read_u32(Endian::Little)?;
                out.registration_reject_info_valid = reader.read_u8()? != 0;
                out.registration_reject_domain = reader.read_u8()?;
                out.registration_reject_cause = reader.read_u8()?;
                out.network_id_valid = reader.read_u8()? != 0;
                out.mcc = reader.read_fixed_string(3)?;
                out.mnc = reader.read_fixed_string(3)?;
                out.egprs_support_valid = reader.read_u8()? != 0;
                out.egprs_support = reader.read_u8()? != 0;
                out.dtm_support_valid = reader.read_u8()? != 0;
                out.dtm_support = reader.read_u8()? != 0;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct WcdmaSystemInfoV2 {
            pub domain_valid: bool,
            pub domain: u8,
            pub service_capability_valid: bool,
            pub service_capability: u8,
            pub roaming_status_valid: bool,
            pub roaming_status: u8,
            pub forbidden_valid: bool,
            pub forbidden: bool,
            pub lac_valid: bool,
            pub lac: u16,
            pub cid_valid: bool,
            pub cid: u32,
            pub registration_reject_info_valid: bool,
            pub registration_reject_domain: u8,
            pub registration_reject_cause: u8,
            pub network_id_valid: bool,
            pub mcc: String,
            pub mnc: String,
            pub hs_call_status_valid: bool,
            pub hs_call_status: u8,
            pub hs_service_valid: bool,
            pub hs_service: u8,
            pub primary_scrambling_code_valid: bool,
            pub primary_scrambling_code: u16,
        }
        impl WcdmaSystemInfoV2 {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(u8::from(*&self.domain_valid));
                writer.write_u8(*&self.domain);
                writer.write_u8(u8::from(*&self.service_capability_valid));
                writer.write_u8(*&self.service_capability);
                writer.write_u8(u8::from(*&self.roaming_status_valid));
                writer.write_u8(*&self.roaming_status);
                writer.write_u8(u8::from(*&self.forbidden_valid));
                writer.write_u8(u8::from(*&self.forbidden));
                writer.write_u8(u8::from(*&self.lac_valid));
                writer.write_u16(*&self.lac, Endian::Little);
                writer.write_u8(u8::from(*&self.cid_valid));
                writer.write_u32(*&self.cid, Endian::Little);
                writer.write_u8(u8::from(*&self.registration_reject_info_valid));
                writer.write_u8(*&self.registration_reject_domain);
                writer.write_u8(*&self.registration_reject_cause);
                writer.write_u8(u8::from(*&self.network_id_valid));
                writer.write_string(&self.mcc, 0, Some(3))?;
                writer.write_string(&self.mnc, 0, Some(3))?;
                writer.write_u8(u8::from(*&self.hs_call_status_valid));
                writer.write_u8(*&self.hs_call_status);
                writer.write_u8(u8::from(*&self.hs_service_valid));
                writer.write_u8(*&self.hs_service);
                writer.write_u8(u8::from(*&self.primary_scrambling_code_valid));
                writer.write_u16(*&self.primary_scrambling_code, Endian::Little);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.domain_valid = reader.read_u8()? != 0;
                out.domain = reader.read_u8()?;
                out.service_capability_valid = reader.read_u8()? != 0;
                out.service_capability = reader.read_u8()?;
                out.roaming_status_valid = reader.read_u8()? != 0;
                out.roaming_status = reader.read_u8()?;
                out.forbidden_valid = reader.read_u8()? != 0;
                out.forbidden = reader.read_u8()? != 0;
                out.lac_valid = reader.read_u8()? != 0;
                out.lac = reader.read_u16(Endian::Little)?;
                out.cid_valid = reader.read_u8()? != 0;
                out.cid = reader.read_u32(Endian::Little)?;
                out.registration_reject_info_valid = reader.read_u8()? != 0;
                out.registration_reject_domain = reader.read_u8()?;
                out.registration_reject_cause = reader.read_u8()?;
                out.network_id_valid = reader.read_u8()? != 0;
                out.mcc = reader.read_fixed_string(3)?;
                out.mnc = reader.read_fixed_string(3)?;
                out.hs_call_status_valid = reader.read_u8()? != 0;
                out.hs_call_status = reader.read_u8()?;
                out.hs_service_valid = reader.read_u8()? != 0;
                out.hs_service = reader.read_u8()?;
                out.primary_scrambling_code_valid = reader.read_u8()? != 0;
                out.primary_scrambling_code = reader.read_u16(Endian::Little)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct LteSystemInfoV2 {
            pub domain_valid: bool,
            pub domain: u8,
            pub service_capability_valid: bool,
            pub service_capability: u8,
            pub roaming_status_valid: bool,
            pub roaming_status: u8,
            pub forbidden_valid: bool,
            pub forbidden: bool,
            pub lac_valid: bool,
            pub lac: u16,
            pub cid_valid: bool,
            pub cid: u32,
            pub registration_reject_info_valid: bool,
            pub registration_reject_domain: u8,
            pub registration_reject_cause: u8,
            pub network_id_valid: bool,
            pub mcc: String,
            pub mnc: String,
            pub tac_valid: bool,
            pub tac: u16,
        }
        impl LteSystemInfoV2 {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(u8::from(*&self.domain_valid));
                writer.write_u8(*&self.domain);
                writer.write_u8(u8::from(*&self.service_capability_valid));
                writer.write_u8(*&self.service_capability);
                writer.write_u8(u8::from(*&self.roaming_status_valid));
                writer.write_u8(*&self.roaming_status);
                writer.write_u8(u8::from(*&self.forbidden_valid));
                writer.write_u8(u8::from(*&self.forbidden));
                writer.write_u8(u8::from(*&self.lac_valid));
                writer.write_u16(*&self.lac, Endian::Little);
                writer.write_u8(u8::from(*&self.cid_valid));
                writer.write_u32(*&self.cid, Endian::Little);
                writer.write_u8(u8::from(*&self.registration_reject_info_valid));
                writer.write_u8(*&self.registration_reject_domain);
                writer.write_u8(*&self.registration_reject_cause);
                writer.write_u8(u8::from(*&self.network_id_valid));
                writer.write_string(&self.mcc, 0, Some(3))?;
                writer.write_string(&self.mnc, 0, Some(3))?;
                writer.write_u8(u8::from(*&self.tac_valid));
                writer.write_u16(*&self.tac, Endian::Little);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.domain_valid = reader.read_u8()? != 0;
                out.domain = reader.read_u8()?;
                out.service_capability_valid = reader.read_u8()? != 0;
                out.service_capability = reader.read_u8()?;
                out.roaming_status_valid = reader.read_u8()? != 0;
                out.roaming_status = reader.read_u8()?;
                out.forbidden_valid = reader.read_u8()? != 0;
                out.forbidden = reader.read_u8()? != 0;
                out.lac_valid = reader.read_u8()? != 0;
                out.lac = reader.read_u16(Endian::Little)?;
                out.cid_valid = reader.read_u8()? != 0;
                out.cid = reader.read_u32(Endian::Little)?;
                out.registration_reject_info_valid = reader.read_u8()? != 0;
                out.registration_reject_domain = reader.read_u8()?;
                out.registration_reject_cause = reader.read_u8()?;
                out.network_id_valid = reader.read_u8()? != 0;
                out.mcc = reader.read_fixed_string(3)?;
                out.mnc = reader.read_fixed_string(3)?;
                out.tac_valid = reader.read_u8()? != 0;
                out.tac = reader.read_u16(Endian::Little)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct AdditionalCdmaSystemInfo {
            pub geo_system_index: u16,
            pub registration_period: u16,
        }
        impl AdditionalCdmaSystemInfo {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u16(*&self.geo_system_index, Endian::Little);
                writer.write_u16(*&self.registration_period, Endian::Little);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.geo_system_index = reader.read_u16(Endian::Little)?;
                out.registration_period = reader.read_u16(Endian::Little)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct AdditionalHdrSystemInfo {
            pub geo_system_index: u16,
        }
        impl AdditionalHdrSystemInfo {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u16(*&self.geo_system_index, Endian::Little);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.geo_system_index = reader.read_u16(Endian::Little)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct AdditionalGsmSystemInfo {
            pub geo_system_index: u16,
            pub cell_broadcast_support: u32,
        }
        impl AdditionalGsmSystemInfo {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u16(*&self.geo_system_index, Endian::Little);
                writer.write_u32(*&self.cell_broadcast_support, Endian::Little);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.geo_system_index = reader.read_u16(Endian::Little)?;
                out.cell_broadcast_support = reader.read_u32(Endian::Little)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct AdditionalWcdmaSystemInfo {
            pub geo_system_index: u16,
            pub cell_broadcast_support: u32,
        }
        impl AdditionalWcdmaSystemInfo {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u16(*&self.geo_system_index, Endian::Little);
                writer.write_u32(*&self.cell_broadcast_support, Endian::Little);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.geo_system_index = reader.read_u16(Endian::Little)?;
                out.cell_broadcast_support = reader.read_u32(Endian::Little)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct AdditionalLteSystemInfo {
            pub geo_system_index: u16,
        }
        impl AdditionalLteSystemInfo {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u16(*&self.geo_system_index, Endian::Little);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.geo_system_index = reader.read_u16(Endian::Little)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct GsmCallBarringStatus {
            pub cs_status: i32,
            pub ps_status: i32,
        }
        impl GsmCallBarringStatus {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_i32(*&self.cs_status, Endian::Little);
                writer.write_i32(*&self.ps_status, Endian::Little);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.cs_status = reader.read_i32(Endian::Little)?;
                out.ps_status = reader.read_i32(Endian::Little)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct WcdmaCallBarringStatus {
            pub cs_status: i32,
            pub ps_status: i32,
        }
        impl WcdmaCallBarringStatus {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_i32(*&self.cs_status, Endian::Little);
                writer.write_i32(*&self.ps_status, Endian::Little);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.cs_status = reader.read_i32(Endian::Little)?;
                out.ps_status = reader.read_i32(Endian::Little)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct TdScdmaServiceStatus {
            pub service_status: u8,
            pub true_service_status: u8,
            pub preferred_data_path: bool,
        }
        impl TdScdmaServiceStatus {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(*&self.service_status);
                writer.write_u8(*&self.true_service_status);
                writer.write_u8(u8::from(*&self.preferred_data_path));
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.service_status = reader.read_u8()?;
                out.true_service_status = reader.read_u8()?;
                out.preferred_data_path = reader.read_u8()? != 0;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct TdScdmaSystemInfoV2 {
            pub domain_valid: bool,
            pub domain: u8,
            pub service_capability_valid: bool,
            pub service_capability: u8,
            pub roaming_status_valid: bool,
            pub roaming_status: u8,
            pub forbidden_valid: bool,
            pub forbidden: bool,
            pub lac_valid: bool,
            pub lac: u16,
            pub cid_valid: bool,
            pub cid: u32,
            pub registration_reject_info_valid: bool,
            pub registration_reject_domain: u8,
            pub registration_reject_cause: u8,
            pub network_id_valid: bool,
            pub mcc: String,
            pub mnc: String,
            pub hs_call_status_valid: bool,
            pub hs_call_status: u8,
            pub hs_service_valid: bool,
            pub hs_service: u8,
            pub cell_parameter_id_valid: bool,
            pub cell_parameter_id: u16,
            pub cell_broadcast_support_valid: bool,
            pub cell_broadcast_support: u32,
            pub cs_call_barring_status_valid: bool,
            pub cs_call_barring_status: i32,
            pub ps_call_barring_status_valid: bool,
            pub ps_call_barring_status: i32,
            pub cipher_domain_valid: bool,
            pub cipher_domain: u8,
        }
        impl TdScdmaSystemInfoV2 {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(u8::from(*&self.domain_valid));
                writer.write_u8(*&self.domain);
                writer.write_u8(u8::from(*&self.service_capability_valid));
                writer.write_u8(*&self.service_capability);
                writer.write_u8(u8::from(*&self.roaming_status_valid));
                writer.write_u8(*&self.roaming_status);
                writer.write_u8(u8::from(*&self.forbidden_valid));
                writer.write_u8(u8::from(*&self.forbidden));
                writer.write_u8(u8::from(*&self.lac_valid));
                writer.write_u16(*&self.lac, Endian::Little);
                writer.write_u8(u8::from(*&self.cid_valid));
                writer.write_u32(*&self.cid, Endian::Little);
                writer.write_u8(u8::from(*&self.registration_reject_info_valid));
                writer.write_u8(*&self.registration_reject_domain);
                writer.write_u8(*&self.registration_reject_cause);
                writer.write_u8(u8::from(*&self.network_id_valid));
                writer.write_string(&self.mcc, 0, Some(3))?;
                writer.write_string(&self.mnc, 0, Some(3))?;
                writer.write_u8(u8::from(*&self.hs_call_status_valid));
                writer.write_u8(*&self.hs_call_status);
                writer.write_u8(u8::from(*&self.hs_service_valid));
                writer.write_u8(*&self.hs_service);
                writer.write_u8(u8::from(*&self.cell_parameter_id_valid));
                writer.write_u16(*&self.cell_parameter_id, Endian::Little);
                writer.write_u8(u8::from(*&self.cell_broadcast_support_valid));
                writer.write_u32(*&self.cell_broadcast_support, Endian::Little);
                writer.write_u8(u8::from(*&self.cs_call_barring_status_valid));
                writer.write_i32(*&self.cs_call_barring_status, Endian::Little);
                writer.write_u8(u8::from(*&self.ps_call_barring_status_valid));
                writer.write_i32(*&self.ps_call_barring_status, Endian::Little);
                writer.write_u8(u8::from(*&self.cipher_domain_valid));
                writer.write_u8(*&self.cipher_domain);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.domain_valid = reader.read_u8()? != 0;
                out.domain = reader.read_u8()?;
                out.service_capability_valid = reader.read_u8()? != 0;
                out.service_capability = reader.read_u8()?;
                out.roaming_status_valid = reader.read_u8()? != 0;
                out.roaming_status = reader.read_u8()?;
                out.forbidden_valid = reader.read_u8()? != 0;
                out.forbidden = reader.read_u8()? != 0;
                out.lac_valid = reader.read_u8()? != 0;
                out.lac = reader.read_u16(Endian::Little)?;
                out.cid_valid = reader.read_u8()? != 0;
                out.cid = reader.read_u32(Endian::Little)?;
                out.registration_reject_info_valid = reader.read_u8()? != 0;
                out.registration_reject_domain = reader.read_u8()?;
                out.registration_reject_cause = reader.read_u8()?;
                out.network_id_valid = reader.read_u8()? != 0;
                out.mcc = reader.read_fixed_string(3)?;
                out.mnc = reader.read_fixed_string(3)?;
                out.hs_call_status_valid = reader.read_u8()? != 0;
                out.hs_call_status = reader.read_u8()?;
                out.hs_service_valid = reader.read_u8()? != 0;
                out.hs_service = reader.read_u8()?;
                out.cell_parameter_id_valid = reader.read_u8()? != 0;
                out.cell_parameter_id = reader.read_u16(Endian::Little)?;
                out.cell_broadcast_support_valid = reader.read_u8()? != 0;
                out.cell_broadcast_support = reader.read_u32(Endian::Little)?;
                out.cs_call_barring_status_valid = reader.read_u8()? != 0;
                out.cs_call_barring_status = reader.read_i32(Endian::Little)?;
                out.ps_call_barring_status_valid = reader.read_u8()? != 0;
                out.ps_call_barring_status = reader.read_i32(Endian::Little)?;
                out.cipher_domain_valid = reader.read_u8()? != 0;
                out.cipher_domain = reader.read_u8()?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Nr5gServiceStatusInfo {
            pub service_status: u8,
            pub true_service_status: u8,
            pub preferred_data_path: bool,
        }
        impl Nr5gServiceStatusInfo {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(*&self.service_status);
                writer.write_u8(*&self.true_service_status);
                writer.write_u8(u8::from(*&self.preferred_data_path));
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.service_status = reader.read_u8()?;
                out.true_service_status = reader.read_u8()?;
                out.preferred_data_path = reader.read_u8()? != 0;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Nr5gSystemInfo {
            pub domain_valid: bool,
            pub domain: u8,
            pub service_capability_valid: bool,
            pub service_capability: u8,
            pub roaming_status_valid: bool,
            pub roaming_status: u8,
            pub forbidden_valid: bool,
            pub forbidden: bool,
            pub lac_valid: bool,
            pub lac: u16,
            pub cid_valid: bool,
            pub cid: u32,
            pub registration_reject_info_valid: bool,
            pub registration_reject_domain: u8,
            pub registration_reject_cause: u8,
            pub network_id_valid: bool,
            pub mcc: String,
            pub mnc: String,
            pub tac_valid: bool,
            pub tac: u16,
        }
        impl Nr5gSystemInfo {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(u8::from(*&self.domain_valid));
                writer.write_u8(*&self.domain);
                writer.write_u8(u8::from(*&self.service_capability_valid));
                writer.write_u8(*&self.service_capability);
                writer.write_u8(u8::from(*&self.roaming_status_valid));
                writer.write_u8(*&self.roaming_status);
                writer.write_u8(u8::from(*&self.forbidden_valid));
                writer.write_u8(u8::from(*&self.forbidden));
                writer.write_u8(u8::from(*&self.lac_valid));
                writer.write_u16(*&self.lac, Endian::Little);
                writer.write_u8(u8::from(*&self.cid_valid));
                writer.write_u32(*&self.cid, Endian::Little);
                writer.write_u8(u8::from(*&self.registration_reject_info_valid));
                writer.write_u8(*&self.registration_reject_domain);
                writer.write_u8(*&self.registration_reject_cause);
                writer.write_u8(u8::from(*&self.network_id_valid));
                writer.write_string(&self.mcc, 0, Some(3))?;
                writer.write_string(&self.mnc, 0, Some(3))?;
                writer.write_u8(u8::from(*&self.tac_valid));
                writer.write_u16(*&self.tac, Endian::Little);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.domain_valid = reader.read_u8()? != 0;
                out.domain = reader.read_u8()?;
                out.service_capability_valid = reader.read_u8()? != 0;
                out.service_capability = reader.read_u8()?;
                out.roaming_status_valid = reader.read_u8()? != 0;
                out.roaming_status = reader.read_u8()?;
                out.forbidden_valid = reader.read_u8()? != 0;
                out.forbidden = reader.read_u8()? != 0;
                out.lac_valid = reader.read_u8()? != 0;
                out.lac = reader.read_u16(Endian::Little)?;
                out.cid_valid = reader.read_u8()? != 0;
                out.cid = reader.read_u32(Endian::Little)?;
                out.registration_reject_info_valid = reader.read_u8()? != 0;
                out.registration_reject_domain = reader.read_u8()?;
                out.registration_reject_cause = reader.read_u8()?;
                out.network_id_valid = reader.read_u8()? != 0;
                out.mcc = reader.read_fixed_string(3)?;
                out.mnc = reader.read_fixed_string(3)?;
                out.tac_valid = reader.read_u8()? != 0;
                out.tac = reader.read_u16(Endian::Little)?;
                Ok(out)
            }
        }
    }
    #[doc = "Get Signal Info"]
    pub mod get_signal_info {
        use crate::{Endian, Error, TlvBuf, TlvReader, TlvWriter};
        use alloc::string::String;
        use alloc::vec::Vec;
        pub const MESSAGE_ID: u16 = 79;
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Request {}
        impl Request {
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                Ok(Vec::new())
            }
            pub fn from_tlvs(_tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Ok(Self::default())
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Response {
            pub cdma_signal_strength: Option<CdmaSignalStrength>,
            pub hdr_signal_strength: Option<HdrSignalStrength>,
            pub gsm_signal_strength: Option<i8>,
            pub wcdma_signal_strength: Option<WcdmaSignalStrength>,
            pub lte_signal_strength: Option<LteSignalStrength>,
            pub tdma_signal_strength: Option<i8>,
            pub tdma_signal_strength_extended: Option<TdmaSignalStrengthExtended>,
            pub n_5g_signal_strength: Option<N5gSignalStrength>,
            pub n_5g_signal_strength_extended: Option<i16>,
            pub wcdma_rscp: Option<i16>,
        }
        impl Response {
            pub const TLV_RESULT: u8 = 2;
            pub const TLV_CDMA_SIGNAL_STRENGTH: u8 = 16;
            pub const TLV_HDR_SIGNAL_STRENGTH: u8 = 17;
            pub const TLV_GSM_SIGNAL_STRENGTH: u8 = 18;
            pub const TLV_WCDMA_SIGNAL_STRENGTH: u8 = 19;
            pub const TLV_LTE_SIGNAL_STRENGTH: u8 = 20;
            pub const TLV_TDMA_SIGNAL_STRENGTH: u8 = 21;
            pub const TLV_TDMA_SIGNAL_STRENGTH_EXTENDED: u8 = 22;
            pub const TLV_N_5G_SIGNAL_STRENGTH: u8 = 23;
            pub const TLV_N_5G_SIGNAL_STRENGTH_EXTENDED: u8 = 24;
            pub const TLV_WCDMA_RSCP: u8 = 25;
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = Vec::new();
                if let Some(value) = &self.cdma_signal_strength {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(16));
                }
                if let Some(value) = &self.hdr_signal_strength {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(17));
                }
                if let Some(value) = &self.gsm_signal_strength {
                    let mut writer = TlvWriter::new();
                    writer.write_i8(*value);
                    tlvs.push(writer.into_tlv(18));
                }
                if let Some(value) = &self.wcdma_signal_strength {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(19));
                }
                if let Some(value) = &self.lte_signal_strength {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(20));
                }
                if let Some(value) = &self.tdma_signal_strength {
                    let mut writer = TlvWriter::new();
                    writer.write_i8(*value);
                    tlvs.push(writer.into_tlv(21));
                }
                if let Some(value) = &self.tdma_signal_strength_extended {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(22));
                }
                if let Some(value) = &self.n_5g_signal_strength {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(23));
                }
                if let Some(value) = &self.n_5g_signal_strength_extended {
                    let mut writer = TlvWriter::new();
                    writer.write_i16(*value, Endian::Little);
                    tlvs.push(writer.into_tlv(24));
                }
                if let Some(value) = &self.wcdma_rscp {
                    let mut writer = TlvWriter::new();
                    writer.write_i16(*value, Endian::Little);
                    tlvs.push(writer.into_tlv(25));
                }
                Ok(tlvs)
            }
            pub fn from_tlvs(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                let mut out = Self::default();
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 16) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = CdmaSignalStrength::decode_from(&mut reader)?;
                    out.cdma_signal_strength = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 17) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = HdrSignalStrength::decode_from(&mut reader)?;
                    out.hdr_signal_strength = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 18) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_i8()?;
                    out.gsm_signal_strength = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 19) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = WcdmaSignalStrength::decode_from(&mut reader)?;
                    out.wcdma_signal_strength = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 20) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = LteSignalStrength::decode_from(&mut reader)?;
                    out.lte_signal_strength = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 21) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_i8()?;
                    out.tdma_signal_strength = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 22) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = TdmaSignalStrengthExtended::decode_from(&mut reader)?;
                    out.tdma_signal_strength_extended = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 23) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = N5gSignalStrength::decode_from(&mut reader)?;
                    out.n_5g_signal_strength = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 24) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_i16(Endian::Little)?;
                    out.n_5g_signal_strength_extended = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 25) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_i16(Endian::Little)?;
                    out.wcdma_rscp = Some(value);
                }
                Ok(out)
            }
        }
        impl crate::Response for Response {
            type Request = Request;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = crate::result::success_tlvs();
                tlvs.extend(self.to_tlvs()?);
                Ok(tlvs)
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                crate::result::check_result(tlvs)?;
                Response::from_tlvs(tlvs)
            }
        }
        impl crate::Request for Request {
            const SERVICE: u16 = super::super::SERVICE_ID;
            const MESSAGE_ID: u16 = 79;
            type Response = Response;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                self.to_tlvs()
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Self::from_tlvs(tlvs)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct CdmaSignalStrength {
            pub rssi: i8,
            pub ecio: i16,
        }
        impl CdmaSignalStrength {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_i8(*&self.rssi);
                writer.write_i16(*&self.ecio, Endian::Little);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.rssi = reader.read_i8()?;
                out.ecio = reader.read_i16(Endian::Little)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct HdrSignalStrength {
            pub rssi: i8,
            pub ecio: i16,
            pub sinr: u8,
            pub io: i32,
        }
        impl HdrSignalStrength {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_i8(*&self.rssi);
                writer.write_i16(*&self.ecio, Endian::Little);
                writer.write_u8(*&self.sinr);
                writer.write_i32(*&self.io, Endian::Little);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.rssi = reader.read_i8()?;
                out.ecio = reader.read_i16(Endian::Little)?;
                out.sinr = reader.read_u8()?;
                out.io = reader.read_i32(Endian::Little)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct WcdmaSignalStrength {
            pub rssi: i8,
            pub ecio: i16,
        }
        impl WcdmaSignalStrength {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_i8(*&self.rssi);
                writer.write_i16(*&self.ecio, Endian::Little);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.rssi = reader.read_i8()?;
                out.ecio = reader.read_i16(Endian::Little)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct LteSignalStrength {
            pub rssi: i8,
            pub rsrq: i8,
            pub rsrp: i16,
            pub snr: i16,
        }
        impl LteSignalStrength {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_i8(*&self.rssi);
                writer.write_i8(*&self.rsrq);
                writer.write_i16(*&self.rsrp, Endian::Little);
                writer.write_i16(*&self.snr, Endian::Little);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.rssi = reader.read_i8()?;
                out.rsrq = reader.read_i8()?;
                out.rsrp = reader.read_i16(Endian::Little)?;
                out.snr = reader.read_i16(Endian::Little)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct TdmaSignalStrengthExtended {
            pub rssi: i32,
            pub rscp: i32,
            pub ecio: i32,
            pub sinr: i32,
        }
        impl TdmaSignalStrengthExtended {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_i32(*&self.rssi, Endian::Little);
                writer.write_i32(*&self.rscp, Endian::Little);
                writer.write_i32(*&self.ecio, Endian::Little);
                writer.write_i32(*&self.sinr, Endian::Little);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.rssi = reader.read_i32(Endian::Little)?;
                out.rscp = reader.read_i32(Endian::Little)?;
                out.ecio = reader.read_i32(Endian::Little)?;
                out.sinr = reader.read_i32(Endian::Little)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct N5gSignalStrength {
            pub rsrp: i16,
            pub snr: i16,
        }
        impl N5gSignalStrength {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_i16(*&self.rsrp, Endian::Little);
                writer.write_i16(*&self.snr, Endian::Little);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.rsrp = reader.read_i16(Endian::Little)?;
                out.snr = reader.read_i16(Endian::Little)?;
                Ok(out)
            }
        }
    }
    #[doc = "Config Signal Info"]
    pub mod config_signal_info {
        use crate::{Endian, Error, TlvBuf, TlvReader, TlvWriter};
        use alloc::string::String;
        use alloc::vec::Vec;
        pub const MESSAGE_ID: u16 = 80;
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Request {
            pub rssi_threshold: Option<Vec<i8>>,
            pub ecio_threshold: Option<Vec<i16>>,
            pub sinr_threshold: Option<Vec<u8>>,
            pub lte_snr_threshold: Option<Vec<i16>>,
            pub io_threshold: Option<Vec<i32>>,
            pub rsrq_threshold: Option<Vec<i8>>,
            pub rsrp_threshold: Option<Vec<i16>>,
            pub lte_report: Option<LteReport>,
            pub rscp_threshold: Option<Vec<i8>>,
        }
        impl Request {
            pub const TLV_RSSI_THRESHOLD: u8 = 16;
            pub const TLV_ECIO_THRESHOLD: u8 = 17;
            pub const TLV_SINR_THRESHOLD: u8 = 18;
            pub const TLV_LTE_SNR_THRESHOLD: u8 = 19;
            pub const TLV_IO_THRESHOLD: u8 = 20;
            pub const TLV_RSRQ_THRESHOLD: u8 = 21;
            pub const TLV_RSRP_THRESHOLD: u8 = 22;
            pub const TLV_LTE_REPORT: u8 = 23;
            pub const TLV_RSCP_THRESHOLD: u8 = 24;
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = Vec::new();
                if let Some(value) = &self.rssi_threshold {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::try_from((value).len()).map_err(|_| Error::TooLong)?);
                    for item in value {
                        writer.write_i8(*item);
                    }
                    tlvs.push(writer.into_tlv(16));
                }
                if let Some(value) = &self.ecio_threshold {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::try_from((value).len()).map_err(|_| Error::TooLong)?);
                    for item in value {
                        writer.write_i16(*item, Endian::Little);
                    }
                    tlvs.push(writer.into_tlv(17));
                }
                if let Some(value) = &self.sinr_threshold {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::try_from((value).len()).map_err(|_| Error::TooLong)?);
                    for item in value {
                        writer.write_u8(*item);
                    }
                    tlvs.push(writer.into_tlv(18));
                }
                if let Some(value) = &self.lte_snr_threshold {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::try_from((value).len()).map_err(|_| Error::TooLong)?);
                    for item in value {
                        writer.write_i16(*item, Endian::Little);
                    }
                    tlvs.push(writer.into_tlv(19));
                }
                if let Some(value) = &self.io_threshold {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::try_from((value).len()).map_err(|_| Error::TooLong)?);
                    for item in value {
                        writer.write_i32(*item, Endian::Little);
                    }
                    tlvs.push(writer.into_tlv(20));
                }
                if let Some(value) = &self.rsrq_threshold {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::try_from((value).len()).map_err(|_| Error::TooLong)?);
                    for item in value {
                        writer.write_i8(*item);
                    }
                    tlvs.push(writer.into_tlv(21));
                }
                if let Some(value) = &self.rsrp_threshold {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::try_from((value).len()).map_err(|_| Error::TooLong)?);
                    for item in value {
                        writer.write_i16(*item, Endian::Little);
                    }
                    tlvs.push(writer.into_tlv(22));
                }
                if let Some(value) = &self.lte_report {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(23));
                }
                if let Some(value) = &self.rscp_threshold {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::try_from((value).len()).map_err(|_| Error::TooLong)?);
                    for item in value {
                        writer.write_i8(*item);
                    }
                    tlvs.push(writer.into_tlv(24));
                }
                Ok(tlvs)
            }
            pub fn from_tlvs(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                let mut out = Self::default();
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 16) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let count = reader.read_u8()? as usize;
                    let mut items = Vec::with_capacity(count.min(256));
                    for _ in 0..count {
                        items.push(reader.read_i8()?);
                    }
                    let value = items;
                    out.rssi_threshold = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 17) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let count = reader.read_u8()? as usize;
                    let mut items = Vec::with_capacity(count.min(256));
                    for _ in 0..count {
                        items.push(reader.read_i16(Endian::Little)?);
                    }
                    let value = items;
                    out.ecio_threshold = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 18) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let count = reader.read_u8()? as usize;
                    let mut items = Vec::with_capacity(count.min(256));
                    for _ in 0..count {
                        items.push(reader.read_u8()?);
                    }
                    let value = items;
                    out.sinr_threshold = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 19) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let count = reader.read_u8()? as usize;
                    let mut items = Vec::with_capacity(count.min(256));
                    for _ in 0..count {
                        items.push(reader.read_i16(Endian::Little)?);
                    }
                    let value = items;
                    out.lte_snr_threshold = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 20) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let count = reader.read_u8()? as usize;
                    let mut items = Vec::with_capacity(count.min(256));
                    for _ in 0..count {
                        items.push(reader.read_i32(Endian::Little)?);
                    }
                    let value = items;
                    out.io_threshold = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 21) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let count = reader.read_u8()? as usize;
                    let mut items = Vec::with_capacity(count.min(256));
                    for _ in 0..count {
                        items.push(reader.read_i8()?);
                    }
                    let value = items;
                    out.rsrq_threshold = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 22) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let count = reader.read_u8()? as usize;
                    let mut items = Vec::with_capacity(count.min(256));
                    for _ in 0..count {
                        items.push(reader.read_i16(Endian::Little)?);
                    }
                    let value = items;
                    out.rsrp_threshold = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 23) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = LteReport::decode_from(&mut reader)?;
                    out.lte_report = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 24) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let count = reader.read_u8()? as usize;
                    let mut items = Vec::with_capacity(count.min(256));
                    for _ in 0..count {
                        items.push(reader.read_i8()?);
                    }
                    let value = items;
                    out.rscp_threshold = Some(value);
                }
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Response {}
        impl Response {
            pub const TLV_RESULT: u8 = 2;
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                Ok(Vec::new())
            }
            pub fn from_tlvs(_tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Ok(Self::default())
            }
        }
        impl crate::Response for Response {
            type Request = Request;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = crate::result::success_tlvs();
                tlvs.extend(self.to_tlvs()?);
                Ok(tlvs)
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                crate::result::check_result(tlvs)?;
                Response::from_tlvs(tlvs)
            }
        }
        impl crate::Request for Request {
            const SERVICE: u16 = super::super::SERVICE_ID;
            const MESSAGE_ID: u16 = 80;
            type Response = Response;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                self.to_tlvs()
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Self::from_tlvs(tlvs)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct LteReport {
            pub rate: u8,
            pub average_period: u8,
        }
        impl LteReport {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(*&self.rate);
                writer.write_u8(*&self.average_period);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.rate = reader.read_u8()?;
                out.average_period = reader.read_u8()?;
                Ok(out)
            }
        }
    }
    #[doc = "Config Signal Info v2"]
    pub mod config_signal_info_v2 {
        use crate::{Endian, Error, TlvBuf, TlvReader, TlvWriter};
        use alloc::string::String;
        use alloc::vec::Vec;
        pub const MESSAGE_ID: u16 = 108;
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Request {
            pub cdma_rssi_threshold_list: Option<Vec<i16>>,
            pub cdma_rssi_delta: Option<u16>,
            pub cdma_ecio_threshold_list: Option<Vec<i16>>,
            pub cdma_ecio_delta: Option<u16>,
            pub hdr_rssi_threshold_list: Option<Vec<i16>>,
            pub hdr_rssi_delta: Option<u16>,
            pub hdr_ecio_threshold_list: Option<Vec<i16>>,
            pub hdr_ecio_delta: Option<u16>,
            pub hdr_sinr_threshold_list: Option<Vec<i16>>,
            pub hdr_sinr_delta: Option<u16>,
            pub hdr_io_threshold_list: Option<Vec<i16>>,
            pub hdr_io_delta: Option<u16>,
            pub gsm_rssi_threshold_list: Option<Vec<i16>>,
            pub gsm_rssi_delta: Option<u16>,
            pub wcdma_rssi_threshold_list: Option<Vec<i16>>,
            pub wcdma_rssi_delta: Option<u16>,
            pub wcdma_ecio_threshold_list: Option<Vec<i16>>,
            pub wcdma_ecio_delta: Option<u16>,
            pub lte_rssi_threshold_list: Option<Vec<i16>>,
            pub lte_rssi_delta: Option<u16>,
            pub lte_snr_threshold_list: Option<Vec<i16>>,
            pub lte_snr_delta: Option<u16>,
            pub lte_rsrq_threshold_list: Option<Vec<i16>>,
            pub lte_rsrq_delta: Option<u16>,
            pub lte_rsrp_threshold_list: Option<Vec<i16>>,
            pub lte_rsrp_delta: Option<u16>,
            pub lte_report: Option<LteReport>,
            pub nr5g_snr_threshold_list: Option<Vec<i16>>,
            pub nr5g_snr_delta: Option<u16>,
            pub nr5g_rsrp_threshold_list: Option<Vec<i16>>,
            pub nr5g_rsrp_delta: Option<u16>,
            pub nr5g_report: Option<Nr5gReport>,
            pub nr5g_rsrq_threshold_list: Option<Vec<i16>>,
            pub nr5g_rsrq_delta: Option<u16>,
            pub wcdma_rscp_threshold_list: Option<Vec<i16>>,
            pub wcdma_rscp_delta: Option<u16>,
        }
        impl Request {
            pub const TLV_CDMA_RSSI_THRESHOLD_LIST: u8 = 16;
            pub const TLV_CDMA_RSSI_DELTA: u8 = 17;
            pub const TLV_CDMA_ECIO_THRESHOLD_LIST: u8 = 18;
            pub const TLV_CDMA_ECIO_DELTA: u8 = 19;
            pub const TLV_HDR_RSSI_THRESHOLD_LIST: u8 = 20;
            pub const TLV_HDR_RSSI_DELTA: u8 = 21;
            pub const TLV_HDR_ECIO_THRESHOLD_LIST: u8 = 22;
            pub const TLV_HDR_ECIO_DELTA: u8 = 23;
            pub const TLV_HDR_SINR_THRESHOLD_LIST: u8 = 24;
            pub const TLV_HDR_SINR_DELTA: u8 = 25;
            pub const TLV_HDR_IO_THRESHOLD_LIST: u8 = 26;
            pub const TLV_HDR_IO_DELTA: u8 = 27;
            pub const TLV_GSM_RSSI_THRESHOLD_LIST: u8 = 28;
            pub const TLV_GSM_RSSI_DELTA: u8 = 29;
            pub const TLV_WCDMA_RSSI_THRESHOLD_LIST: u8 = 30;
            pub const TLV_WCDMA_RSSI_DELTA: u8 = 31;
            pub const TLV_WCDMA_ECIO_THRESHOLD_LIST: u8 = 32;
            pub const TLV_WCDMA_ECIO_DELTA: u8 = 33;
            pub const TLV_LTE_RSSI_THRESHOLD_LIST: u8 = 34;
            pub const TLV_LTE_RSSI_DELTA: u8 = 35;
            pub const TLV_LTE_SNR_THRESHOLD_LIST: u8 = 36;
            pub const TLV_LTE_SNR_DELTA: u8 = 37;
            pub const TLV_LTE_RSRQ_THRESHOLD_LIST: u8 = 38;
            pub const TLV_LTE_RSRQ_DELTA: u8 = 39;
            pub const TLV_LTE_RSRP_THRESHOLD_LIST: u8 = 40;
            pub const TLV_LTE_RSRP_DELTA: u8 = 41;
            pub const TLV_LTE_REPORT: u8 = 42;
            pub const TLV_NR5G_SNR_THRESHOLD_LIST: u8 = 51;
            pub const TLV_NR5G_SNR_DELTA: u8 = 52;
            pub const TLV_NR5G_RSRP_THRESHOLD_LIST: u8 = 53;
            pub const TLV_NR5G_RSRP_DELTA: u8 = 54;
            pub const TLV_NR5G_REPORT: u8 = 55;
            pub const TLV_NR5G_RSRQ_THRESHOLD_LIST: u8 = 56;
            pub const TLV_NR5G_RSRQ_DELTA: u8 = 57;
            pub const TLV_WCDMA_RSCP_THRESHOLD_LIST: u8 = 58;
            pub const TLV_WCDMA_RSCP_DELTA: u8 = 59;
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = Vec::new();
                if let Some(value) = &self.cdma_rssi_threshold_list {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::try_from((value).len()).map_err(|_| Error::TooLong)?);
                    for item in value {
                        writer.write_i16(*item, Endian::Little);
                    }
                    tlvs.push(writer.into_tlv(16));
                }
                if let Some(value) = &self.cdma_rssi_delta {
                    let mut writer = TlvWriter::new();
                    writer.write_u16(*value, Endian::Little);
                    tlvs.push(writer.into_tlv(17));
                }
                if let Some(value) = &self.cdma_ecio_threshold_list {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::try_from((value).len()).map_err(|_| Error::TooLong)?);
                    for item in value {
                        writer.write_i16(*item, Endian::Little);
                    }
                    tlvs.push(writer.into_tlv(18));
                }
                if let Some(value) = &self.cdma_ecio_delta {
                    let mut writer = TlvWriter::new();
                    writer.write_u16(*value, Endian::Little);
                    tlvs.push(writer.into_tlv(19));
                }
                if let Some(value) = &self.hdr_rssi_threshold_list {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::try_from((value).len()).map_err(|_| Error::TooLong)?);
                    for item in value {
                        writer.write_i16(*item, Endian::Little);
                    }
                    tlvs.push(writer.into_tlv(20));
                }
                if let Some(value) = &self.hdr_rssi_delta {
                    let mut writer = TlvWriter::new();
                    writer.write_u16(*value, Endian::Little);
                    tlvs.push(writer.into_tlv(21));
                }
                if let Some(value) = &self.hdr_ecio_threshold_list {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::try_from((value).len()).map_err(|_| Error::TooLong)?);
                    for item in value {
                        writer.write_i16(*item, Endian::Little);
                    }
                    tlvs.push(writer.into_tlv(22));
                }
                if let Some(value) = &self.hdr_ecio_delta {
                    let mut writer = TlvWriter::new();
                    writer.write_u16(*value, Endian::Little);
                    tlvs.push(writer.into_tlv(23));
                }
                if let Some(value) = &self.hdr_sinr_threshold_list {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::try_from((value).len()).map_err(|_| Error::TooLong)?);
                    for item in value {
                        writer.write_i16(*item, Endian::Little);
                    }
                    tlvs.push(writer.into_tlv(24));
                }
                if let Some(value) = &self.hdr_sinr_delta {
                    let mut writer = TlvWriter::new();
                    writer.write_u16(*value, Endian::Little);
                    tlvs.push(writer.into_tlv(25));
                }
                if let Some(value) = &self.hdr_io_threshold_list {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::try_from((value).len()).map_err(|_| Error::TooLong)?);
                    for item in value {
                        writer.write_i16(*item, Endian::Little);
                    }
                    tlvs.push(writer.into_tlv(26));
                }
                if let Some(value) = &self.hdr_io_delta {
                    let mut writer = TlvWriter::new();
                    writer.write_u16(*value, Endian::Little);
                    tlvs.push(writer.into_tlv(27));
                }
                if let Some(value) = &self.gsm_rssi_threshold_list {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::try_from((value).len()).map_err(|_| Error::TooLong)?);
                    for item in value {
                        writer.write_i16(*item, Endian::Little);
                    }
                    tlvs.push(writer.into_tlv(28));
                }
                if let Some(value) = &self.gsm_rssi_delta {
                    let mut writer = TlvWriter::new();
                    writer.write_u16(*value, Endian::Little);
                    tlvs.push(writer.into_tlv(29));
                }
                if let Some(value) = &self.wcdma_rssi_threshold_list {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::try_from((value).len()).map_err(|_| Error::TooLong)?);
                    for item in value {
                        writer.write_i16(*item, Endian::Little);
                    }
                    tlvs.push(writer.into_tlv(30));
                }
                if let Some(value) = &self.wcdma_rssi_delta {
                    let mut writer = TlvWriter::new();
                    writer.write_u16(*value, Endian::Little);
                    tlvs.push(writer.into_tlv(31));
                }
                if let Some(value) = &self.wcdma_ecio_threshold_list {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::try_from((value).len()).map_err(|_| Error::TooLong)?);
                    for item in value {
                        writer.write_i16(*item, Endian::Little);
                    }
                    tlvs.push(writer.into_tlv(32));
                }
                if let Some(value) = &self.wcdma_ecio_delta {
                    let mut writer = TlvWriter::new();
                    writer.write_u16(*value, Endian::Little);
                    tlvs.push(writer.into_tlv(33));
                }
                if let Some(value) = &self.lte_rssi_threshold_list {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::try_from((value).len()).map_err(|_| Error::TooLong)?);
                    for item in value {
                        writer.write_i16(*item, Endian::Little);
                    }
                    tlvs.push(writer.into_tlv(34));
                }
                if let Some(value) = &self.lte_rssi_delta {
                    let mut writer = TlvWriter::new();
                    writer.write_u16(*value, Endian::Little);
                    tlvs.push(writer.into_tlv(35));
                }
                if let Some(value) = &self.lte_snr_threshold_list {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::try_from((value).len()).map_err(|_| Error::TooLong)?);
                    for item in value {
                        writer.write_i16(*item, Endian::Little);
                    }
                    tlvs.push(writer.into_tlv(36));
                }
                if let Some(value) = &self.lte_snr_delta {
                    let mut writer = TlvWriter::new();
                    writer.write_u16(*value, Endian::Little);
                    tlvs.push(writer.into_tlv(37));
                }
                if let Some(value) = &self.lte_rsrq_threshold_list {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::try_from((value).len()).map_err(|_| Error::TooLong)?);
                    for item in value {
                        writer.write_i16(*item, Endian::Little);
                    }
                    tlvs.push(writer.into_tlv(38));
                }
                if let Some(value) = &self.lte_rsrq_delta {
                    let mut writer = TlvWriter::new();
                    writer.write_u16(*value, Endian::Little);
                    tlvs.push(writer.into_tlv(39));
                }
                if let Some(value) = &self.lte_rsrp_threshold_list {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::try_from((value).len()).map_err(|_| Error::TooLong)?);
                    for item in value {
                        writer.write_i16(*item, Endian::Little);
                    }
                    tlvs.push(writer.into_tlv(40));
                }
                if let Some(value) = &self.lte_rsrp_delta {
                    let mut writer = TlvWriter::new();
                    writer.write_u16(*value, Endian::Little);
                    tlvs.push(writer.into_tlv(41));
                }
                if let Some(value) = &self.lte_report {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(42));
                }
                if let Some(value) = &self.nr5g_snr_threshold_list {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::try_from((value).len()).map_err(|_| Error::TooLong)?);
                    for item in value {
                        writer.write_i16(*item, Endian::Little);
                    }
                    tlvs.push(writer.into_tlv(51));
                }
                if let Some(value) = &self.nr5g_snr_delta {
                    let mut writer = TlvWriter::new();
                    writer.write_u16(*value, Endian::Little);
                    tlvs.push(writer.into_tlv(52));
                }
                if let Some(value) = &self.nr5g_rsrp_threshold_list {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::try_from((value).len()).map_err(|_| Error::TooLong)?);
                    for item in value {
                        writer.write_i16(*item, Endian::Little);
                    }
                    tlvs.push(writer.into_tlv(53));
                }
                if let Some(value) = &self.nr5g_rsrp_delta {
                    let mut writer = TlvWriter::new();
                    writer.write_u16(*value, Endian::Little);
                    tlvs.push(writer.into_tlv(54));
                }
                if let Some(value) = &self.nr5g_report {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(55));
                }
                if let Some(value) = &self.nr5g_rsrq_threshold_list {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::try_from((value).len()).map_err(|_| Error::TooLong)?);
                    for item in value {
                        writer.write_i16(*item, Endian::Little);
                    }
                    tlvs.push(writer.into_tlv(56));
                }
                if let Some(value) = &self.nr5g_rsrq_delta {
                    let mut writer = TlvWriter::new();
                    writer.write_u16(*value, Endian::Little);
                    tlvs.push(writer.into_tlv(57));
                }
                if let Some(value) = &self.wcdma_rscp_threshold_list {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::try_from((value).len()).map_err(|_| Error::TooLong)?);
                    for item in value {
                        writer.write_i16(*item, Endian::Little);
                    }
                    tlvs.push(writer.into_tlv(58));
                }
                if let Some(value) = &self.wcdma_rscp_delta {
                    let mut writer = TlvWriter::new();
                    writer.write_u16(*value, Endian::Little);
                    tlvs.push(writer.into_tlv(59));
                }
                Ok(tlvs)
            }
            pub fn from_tlvs(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                let mut out = Self::default();
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 16) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let count = reader.read_u8()? as usize;
                    let mut items = Vec::with_capacity(count.min(256));
                    for _ in 0..count {
                        items.push(reader.read_i16(Endian::Little)?);
                    }
                    let value = items;
                    out.cdma_rssi_threshold_list = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 17) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u16(Endian::Little)?;
                    out.cdma_rssi_delta = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 18) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let count = reader.read_u8()? as usize;
                    let mut items = Vec::with_capacity(count.min(256));
                    for _ in 0..count {
                        items.push(reader.read_i16(Endian::Little)?);
                    }
                    let value = items;
                    out.cdma_ecio_threshold_list = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 19) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u16(Endian::Little)?;
                    out.cdma_ecio_delta = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 20) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let count = reader.read_u8()? as usize;
                    let mut items = Vec::with_capacity(count.min(256));
                    for _ in 0..count {
                        items.push(reader.read_i16(Endian::Little)?);
                    }
                    let value = items;
                    out.hdr_rssi_threshold_list = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 21) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u16(Endian::Little)?;
                    out.hdr_rssi_delta = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 22) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let count = reader.read_u8()? as usize;
                    let mut items = Vec::with_capacity(count.min(256));
                    for _ in 0..count {
                        items.push(reader.read_i16(Endian::Little)?);
                    }
                    let value = items;
                    out.hdr_ecio_threshold_list = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 23) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u16(Endian::Little)?;
                    out.hdr_ecio_delta = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 24) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let count = reader.read_u8()? as usize;
                    let mut items = Vec::with_capacity(count.min(256));
                    for _ in 0..count {
                        items.push(reader.read_i16(Endian::Little)?);
                    }
                    let value = items;
                    out.hdr_sinr_threshold_list = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 25) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u16(Endian::Little)?;
                    out.hdr_sinr_delta = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 26) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let count = reader.read_u8()? as usize;
                    let mut items = Vec::with_capacity(count.min(256));
                    for _ in 0..count {
                        items.push(reader.read_i16(Endian::Little)?);
                    }
                    let value = items;
                    out.hdr_io_threshold_list = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 27) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u16(Endian::Little)?;
                    out.hdr_io_delta = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 28) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let count = reader.read_u8()? as usize;
                    let mut items = Vec::with_capacity(count.min(256));
                    for _ in 0..count {
                        items.push(reader.read_i16(Endian::Little)?);
                    }
                    let value = items;
                    out.gsm_rssi_threshold_list = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 29) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u16(Endian::Little)?;
                    out.gsm_rssi_delta = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 30) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let count = reader.read_u8()? as usize;
                    let mut items = Vec::with_capacity(count.min(256));
                    for _ in 0..count {
                        items.push(reader.read_i16(Endian::Little)?);
                    }
                    let value = items;
                    out.wcdma_rssi_threshold_list = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 31) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u16(Endian::Little)?;
                    out.wcdma_rssi_delta = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 32) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let count = reader.read_u8()? as usize;
                    let mut items = Vec::with_capacity(count.min(256));
                    for _ in 0..count {
                        items.push(reader.read_i16(Endian::Little)?);
                    }
                    let value = items;
                    out.wcdma_ecio_threshold_list = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 33) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u16(Endian::Little)?;
                    out.wcdma_ecio_delta = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 34) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let count = reader.read_u8()? as usize;
                    let mut items = Vec::with_capacity(count.min(256));
                    for _ in 0..count {
                        items.push(reader.read_i16(Endian::Little)?);
                    }
                    let value = items;
                    out.lte_rssi_threshold_list = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 35) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u16(Endian::Little)?;
                    out.lte_rssi_delta = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 36) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let count = reader.read_u8()? as usize;
                    let mut items = Vec::with_capacity(count.min(256));
                    for _ in 0..count {
                        items.push(reader.read_i16(Endian::Little)?);
                    }
                    let value = items;
                    out.lte_snr_threshold_list = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 37) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u16(Endian::Little)?;
                    out.lte_snr_delta = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 38) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let count = reader.read_u8()? as usize;
                    let mut items = Vec::with_capacity(count.min(256));
                    for _ in 0..count {
                        items.push(reader.read_i16(Endian::Little)?);
                    }
                    let value = items;
                    out.lte_rsrq_threshold_list = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 39) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u16(Endian::Little)?;
                    out.lte_rsrq_delta = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 40) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let count = reader.read_u8()? as usize;
                    let mut items = Vec::with_capacity(count.min(256));
                    for _ in 0..count {
                        items.push(reader.read_i16(Endian::Little)?);
                    }
                    let value = items;
                    out.lte_rsrp_threshold_list = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 41) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u16(Endian::Little)?;
                    out.lte_rsrp_delta = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 42) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = LteReport::decode_from(&mut reader)?;
                    out.lte_report = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 51) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let count = reader.read_u8()? as usize;
                    let mut items = Vec::with_capacity(count.min(256));
                    for _ in 0..count {
                        items.push(reader.read_i16(Endian::Little)?);
                    }
                    let value = items;
                    out.nr5g_snr_threshold_list = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 52) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u16(Endian::Little)?;
                    out.nr5g_snr_delta = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 53) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let count = reader.read_u8()? as usize;
                    let mut items = Vec::with_capacity(count.min(256));
                    for _ in 0..count {
                        items.push(reader.read_i16(Endian::Little)?);
                    }
                    let value = items;
                    out.nr5g_rsrp_threshold_list = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 54) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u16(Endian::Little)?;
                    out.nr5g_rsrp_delta = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 55) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = Nr5gReport::decode_from(&mut reader)?;
                    out.nr5g_report = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 56) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let count = reader.read_u8()? as usize;
                    let mut items = Vec::with_capacity(count.min(256));
                    for _ in 0..count {
                        items.push(reader.read_i16(Endian::Little)?);
                    }
                    let value = items;
                    out.nr5g_rsrq_threshold_list = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 57) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u16(Endian::Little)?;
                    out.nr5g_rsrq_delta = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 58) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let count = reader.read_u8()? as usize;
                    let mut items = Vec::with_capacity(count.min(256));
                    for _ in 0..count {
                        items.push(reader.read_i16(Endian::Little)?);
                    }
                    let value = items;
                    out.wcdma_rscp_threshold_list = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 59) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u16(Endian::Little)?;
                    out.wcdma_rscp_delta = Some(value);
                }
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Response {}
        impl Response {
            pub const TLV_RESULT: u8 = 2;
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                Ok(Vec::new())
            }
            pub fn from_tlvs(_tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Ok(Self::default())
            }
        }
        impl crate::Response for Response {
            type Request = Request;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = crate::result::success_tlvs();
                tlvs.extend(self.to_tlvs()?);
                Ok(tlvs)
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                crate::result::check_result(tlvs)?;
                Response::from_tlvs(tlvs)
            }
        }
        impl crate::Request for Request {
            const SERVICE: u16 = super::super::SERVICE_ID;
            const MESSAGE_ID: u16 = 108;
            type Response = Response;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                self.to_tlvs()
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Self::from_tlvs(tlvs)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct LteReport {
            pub rate: u8,
            pub average_period: u8,
        }
        impl LteReport {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(*&self.rate);
                writer.write_u8(*&self.average_period);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.rate = reader.read_u8()?;
                out.average_period = reader.read_u8()?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Nr5gReport {
            pub rate: u8,
            pub average_period: u8,
        }
        impl Nr5gReport {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(*&self.rate);
                writer.write_u8(*&self.average_period);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.rate = reader.read_u8()?;
                out.average_period = reader.read_u8()?;
                Ok(out)
            }
        }
    }
    #[doc = "Get Tx Rx Info"]
    pub mod get_tx_rx_info {
        use crate::{Endian, Error, TlvBuf, TlvReader, TlvWriter};
        use alloc::string::String;
        use alloc::vec::Vec;
        pub const MESSAGE_ID: u16 = 90;
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Request {
            pub radio_interface: i8,
        }
        impl Request {
            pub const TLV_RADIO_INTERFACE: u8 = 1;
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = Vec::new();
                {
                    let mut writer = TlvWriter::new();
                    writer.write_i8(*&self.radio_interface);
                    tlvs.push(writer.into_tlv(1));
                }
                Ok(tlvs)
            }
            pub fn from_tlvs(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                let mut out = Self::default();
                let tlv = tlvs
                    .iter()
                    .find(|tlv| tlv.id == 1)
                    .ok_or(Error::MissingTlv { id: 1 })?;
                let mut reader = TlvReader::new(&tlv.value);
                out.radio_interface = reader.read_i8()?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Response {
            pub rx_chain_0_info: Option<RxChain0Info>,
            pub rx_chain_1_info: Option<RxChain1Info>,
            pub tx_info: Option<TxInfo>,
            pub rx_chain_2_info: Option<RxChain2Info>,
            pub rx_chain_3_info: Option<RxChain3Info>,
        }
        impl Response {
            pub const TLV_RESULT: u8 = 2;
            pub const TLV_RX_CHAIN_0_INFO: u8 = 16;
            pub const TLV_RX_CHAIN_1_INFO: u8 = 17;
            pub const TLV_TX_INFO: u8 = 18;
            pub const TLV_RX_CHAIN_2_INFO: u8 = 21;
            pub const TLV_RX_CHAIN_3_INFO: u8 = 22;
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = Vec::new();
                if let Some(value) = &self.rx_chain_0_info {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(16));
                }
                if let Some(value) = &self.rx_chain_1_info {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(17));
                }
                if let Some(value) = &self.tx_info {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(18));
                }
                if let Some(value) = &self.rx_chain_2_info {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(21));
                }
                if let Some(value) = &self.rx_chain_3_info {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(22));
                }
                Ok(tlvs)
            }
            pub fn from_tlvs(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                let mut out = Self::default();
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 16) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = RxChain0Info::decode_from(&mut reader)?;
                    out.rx_chain_0_info = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 17) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = RxChain1Info::decode_from(&mut reader)?;
                    out.rx_chain_1_info = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 18) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = TxInfo::decode_from(&mut reader)?;
                    out.tx_info = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 21) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = RxChain2Info::decode_from(&mut reader)?;
                    out.rx_chain_2_info = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 22) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = RxChain3Info::decode_from(&mut reader)?;
                    out.rx_chain_3_info = Some(value);
                }
                Ok(out)
            }
        }
        impl crate::Response for Response {
            type Request = Request;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = crate::result::success_tlvs();
                tlvs.extend(self.to_tlvs()?);
                Ok(tlvs)
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                crate::result::check_result(tlvs)?;
                Response::from_tlvs(tlvs)
            }
        }
        impl crate::Request for Request {
            const SERVICE: u16 = super::super::SERVICE_ID;
            const MESSAGE_ID: u16 = 90;
            type Response = Response;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                self.to_tlvs()
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Self::from_tlvs(tlvs)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct RxChain0Info {
            pub is_radio_tuned: bool,
            pub rx_power: i32,
            pub ecio: i32,
            pub rscp: i32,
            pub rsrp: i32,
            pub phase: u32,
        }
        impl RxChain0Info {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(u8::from(*&self.is_radio_tuned));
                writer.write_i32(*&self.rx_power, Endian::Little);
                writer.write_i32(*&self.ecio, Endian::Little);
                writer.write_i32(*&self.rscp, Endian::Little);
                writer.write_i32(*&self.rsrp, Endian::Little);
                writer.write_u32(*&self.phase, Endian::Little);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.is_radio_tuned = reader.read_u8()? != 0;
                out.rx_power = reader.read_i32(Endian::Little)?;
                out.ecio = reader.read_i32(Endian::Little)?;
                out.rscp = reader.read_i32(Endian::Little)?;
                out.rsrp = reader.read_i32(Endian::Little)?;
                out.phase = reader.read_u32(Endian::Little)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct RxChain1Info {
            pub is_radio_tuned: bool,
            pub rx_power: i32,
            pub ecio: i32,
            pub rscp: i32,
            pub rsrp: i32,
            pub phase: u32,
        }
        impl RxChain1Info {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(u8::from(*&self.is_radio_tuned));
                writer.write_i32(*&self.rx_power, Endian::Little);
                writer.write_i32(*&self.ecio, Endian::Little);
                writer.write_i32(*&self.rscp, Endian::Little);
                writer.write_i32(*&self.rsrp, Endian::Little);
                writer.write_u32(*&self.phase, Endian::Little);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.is_radio_tuned = reader.read_u8()? != 0;
                out.rx_power = reader.read_i32(Endian::Little)?;
                out.ecio = reader.read_i32(Endian::Little)?;
                out.rscp = reader.read_i32(Endian::Little)?;
                out.rsrp = reader.read_i32(Endian::Little)?;
                out.phase = reader.read_u32(Endian::Little)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct TxInfo {
            pub is_in_traffic: bool,
            pub tx_power: i32,
        }
        impl TxInfo {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(u8::from(*&self.is_in_traffic));
                writer.write_i32(*&self.tx_power, Endian::Little);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.is_in_traffic = reader.read_u8()? != 0;
                out.tx_power = reader.read_i32(Endian::Little)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct RxChain2Info {
            pub is_radio_tuned: bool,
            pub rx_power: i32,
            pub ecio: i32,
            pub rscp: i32,
            pub rsrp: i32,
            pub phase: u32,
        }
        impl RxChain2Info {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(u8::from(*&self.is_radio_tuned));
                writer.write_i32(*&self.rx_power, Endian::Little);
                writer.write_i32(*&self.ecio, Endian::Little);
                writer.write_i32(*&self.rscp, Endian::Little);
                writer.write_i32(*&self.rsrp, Endian::Little);
                writer.write_u32(*&self.phase, Endian::Little);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.is_radio_tuned = reader.read_u8()? != 0;
                out.rx_power = reader.read_i32(Endian::Little)?;
                out.ecio = reader.read_i32(Endian::Little)?;
                out.rscp = reader.read_i32(Endian::Little)?;
                out.rsrp = reader.read_i32(Endian::Little)?;
                out.phase = reader.read_u32(Endian::Little)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct RxChain3Info {
            pub is_radio_tuned: bool,
            pub rx_power: i32,
            pub ecio: i32,
            pub rscp: i32,
            pub rsrp: i32,
            pub phase: u32,
        }
        impl RxChain3Info {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(u8::from(*&self.is_radio_tuned));
                writer.write_i32(*&self.rx_power, Endian::Little);
                writer.write_i32(*&self.ecio, Endian::Little);
                writer.write_i32(*&self.rscp, Endian::Little);
                writer.write_i32(*&self.rsrp, Endian::Little);
                writer.write_u32(*&self.phase, Endian::Little);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.is_radio_tuned = reader.read_u8()? != 0;
                out.rx_power = reader.read_i32(Endian::Little)?;
                out.ecio = reader.read_i32(Endian::Little)?;
                out.rscp = reader.read_i32(Endian::Little)?;
                out.rsrp = reader.read_i32(Endian::Little)?;
                out.phase = reader.read_u32(Endian::Little)?;
                Ok(out)
            }
        }
    }
    #[doc = "Get CDMA Position Info"]
    pub mod get_cdma_position_info {
        use crate::{Endian, Error, TlvBuf, TlvReader, TlvWriter};
        use alloc::string::String;
        use alloc::vec::Vec;
        pub const MESSAGE_ID: u16 = 101;
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Request {}
        impl Request {
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                Ok(Vec::new())
            }
            pub fn from_tlvs(_tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Ok(Self::default())
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Response {
            pub cdma_position_info: Option<CdmaPositionInfo>,
        }
        impl Response {
            pub const TLV_RESULT: u8 = 2;
            pub const TLV_CDMA_POSITION_INFO: u8 = 16;
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = Vec::new();
                if let Some(value) = &self.cdma_position_info {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(16));
                }
                Ok(tlvs)
            }
            pub fn from_tlvs(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                let mut out = Self::default();
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 16) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = CdmaPositionInfo::decode_from(&mut reader)?;
                    out.cdma_position_info = Some(value);
                }
                Ok(out)
            }
        }
        impl crate::Response for Response {
            type Request = Request;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = crate::result::success_tlvs();
                tlvs.extend(self.to_tlvs()?);
                Ok(tlvs)
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                crate::result::check_result(tlvs)?;
                Response::from_tlvs(tlvs)
            }
        }
        impl crate::Request for Request {
            const SERVICE: u16 = super::super::SERVICE_ID;
            const MESSAGE_ID: u16 = 101;
            type Response = Response;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                self.to_tlvs()
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Self::from_tlvs(tlvs)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Basestation {
            pub pilot_type: u32,
            pub system_id: u16,
            pub network_id: u16,
            pub base_station_id: u16,
            pub pilot_pn: u16,
            pub pilot_strength: u16,
            pub latitude: i32,
            pub longitude: i32,
            pub gps_time_in_milliseconds: u64,
        }
        impl Basestation {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u32(*&self.pilot_type, Endian::Little);
                writer.write_u16(*&self.system_id, Endian::Little);
                writer.write_u16(*&self.network_id, Endian::Little);
                writer.write_u16(*&self.base_station_id, Endian::Little);
                writer.write_u16(*&self.pilot_pn, Endian::Little);
                writer.write_u16(*&self.pilot_strength, Endian::Little);
                writer.write_i32(*&self.latitude, Endian::Little);
                writer.write_i32(*&self.longitude, Endian::Little);
                writer.write_u64(*&self.gps_time_in_milliseconds, Endian::Little);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.pilot_type = reader.read_u32(Endian::Little)?;
                out.system_id = reader.read_u16(Endian::Little)?;
                out.network_id = reader.read_u16(Endian::Little)?;
                out.base_station_id = reader.read_u16(Endian::Little)?;
                out.pilot_pn = reader.read_u16(Endian::Little)?;
                out.pilot_strength = reader.read_u16(Endian::Little)?;
                out.latitude = reader.read_i32(Endian::Little)?;
                out.longitude = reader.read_i32(Endian::Little)?;
                out.gps_time_in_milliseconds = reader.read_u64(Endian::Little)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct CdmaPositionInfo {
            pub ui_in_idle_mode: i8,
            pub basestations: Vec<Basestation>,
        }
        impl CdmaPositionInfo {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_i8(*&self.ui_in_idle_mode);
                writer.write_u8(
                    u8::try_from((&self.basestations).len()).map_err(|_| Error::TooLong)?,
                );
                for item in &self.basestations {
                    (item).encode_to(writer)?;
                }
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.ui_in_idle_mode = reader.read_i8()?;
                let count = reader.read_u8()? as usize;
                let mut items = Vec::with_capacity(count.min(256));
                for _ in 0..count {
                    items.push(Basestation::decode_from(reader)?);
                }
                out.basestations = items;
                Ok(out)
            }
        }
    }
    #[doc = "Force Network Search"]
    pub mod force_network_search {
        use crate::{Endian, Error, TlvBuf, TlvReader, TlvWriter};
        use alloc::string::String;
        use alloc::vec::Vec;
        pub const MESSAGE_ID: u16 = 103;
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Request {}
        impl Request {
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                Ok(Vec::new())
            }
            pub fn from_tlvs(_tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Ok(Self::default())
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Response {}
        impl Response {
            pub const TLV_RESULT: u8 = 2;
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                Ok(Vec::new())
            }
            pub fn from_tlvs(_tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Ok(Self::default())
            }
        }
        impl crate::Response for Response {
            type Request = Request;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = crate::result::success_tlvs();
                tlvs.extend(self.to_tlvs()?);
                Ok(tlvs)
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                crate::result::check_result(tlvs)?;
                Response::from_tlvs(tlvs)
            }
        }
        impl crate::Request for Request {
            const SERVICE: u16 = super::super::SERVICE_ID;
            const MESSAGE_ID: u16 = 103;
            type Response = Response;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                self.to_tlvs()
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Self::from_tlvs(tlvs)
            }
        }
    }
    #[doc = "Get Network Time"]
    pub mod get_network_time {
        use crate::{Endian, Error, TlvBuf, TlvReader, TlvWriter};
        use alloc::string::String;
        use alloc::vec::Vec;
        pub const MESSAGE_ID: u16 = 125;
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Request {}
        impl Request {
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                Ok(Vec::new())
            }
            pub fn from_tlvs(_tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Ok(Self::default())
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Response {
            pub n_3gpp2_time: Option<N3gpp2Time>,
            pub n_3gpp_time: Option<N3gppTime>,
        }
        impl Response {
            pub const TLV_RESULT: u8 = 2;
            pub const TLV_N_3GPP2_TIME: u8 = 16;
            pub const TLV_N_3GPP_TIME: u8 = 17;
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = Vec::new();
                if let Some(value) = &self.n_3gpp2_time {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(16));
                }
                if let Some(value) = &self.n_3gpp_time {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(17));
                }
                Ok(tlvs)
            }
            pub fn from_tlvs(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                let mut out = Self::default();
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 16) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = N3gpp2Time::decode_from(&mut reader)?;
                    out.n_3gpp2_time = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 17) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = N3gppTime::decode_from(&mut reader)?;
                    out.n_3gpp_time = Some(value);
                }
                Ok(out)
            }
        }
        impl crate::Response for Response {
            type Request = Request;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = crate::result::success_tlvs();
                tlvs.extend(self.to_tlvs()?);
                Ok(tlvs)
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                crate::result::check_result(tlvs)?;
                Response::from_tlvs(tlvs)
            }
        }
        impl crate::Request for Request {
            const SERVICE: u16 = super::super::SERVICE_ID;
            const MESSAGE_ID: u16 = 125;
            type Response = Response;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                self.to_tlvs()
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Self::from_tlvs(tlvs)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct UniversalTime {
            pub year: u16,
            pub month: u8,
            pub day: u8,
            pub hour: u8,
            pub minute: u8,
            pub second: u8,
            pub day_of_week: u8,
        }
        impl UniversalTime {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u16(*&self.year, Endian::Little);
                writer.write_u8(*&self.month);
                writer.write_u8(*&self.day);
                writer.write_u8(*&self.hour);
                writer.write_u8(*&self.minute);
                writer.write_u8(*&self.second);
                writer.write_u8(*&self.day_of_week);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.year = reader.read_u16(Endian::Little)?;
                out.month = reader.read_u8()?;
                out.day = reader.read_u8()?;
                out.hour = reader.read_u8()?;
                out.minute = reader.read_u8()?;
                out.second = reader.read_u8()?;
                out.day_of_week = reader.read_u8()?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct N3gpp2Time {
            pub universal_time: UniversalTime,
            pub timezone_offset: i8,
            pub daylight_savings_adjustment: u8,
            pub radio_interface: i8,
        }
        impl N3gpp2Time {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                (&self.universal_time).encode_to(writer)?;
                writer.write_i8(*&self.timezone_offset);
                writer.write_u8(*&self.daylight_savings_adjustment);
                writer.write_i8(*&self.radio_interface);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.universal_time = UniversalTime::decode_from(reader)?;
                out.timezone_offset = reader.read_i8()?;
                out.daylight_savings_adjustment = reader.read_u8()?;
                out.radio_interface = reader.read_i8()?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct UniversalTime2 {
            pub year: u16,
            pub month: u8,
            pub day: u8,
            pub hour: u8,
            pub minute: u8,
            pub second: u8,
            pub day_of_week: u8,
        }
        impl UniversalTime2 {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u16(*&self.year, Endian::Little);
                writer.write_u8(*&self.month);
                writer.write_u8(*&self.day);
                writer.write_u8(*&self.hour);
                writer.write_u8(*&self.minute);
                writer.write_u8(*&self.second);
                writer.write_u8(*&self.day_of_week);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.year = reader.read_u16(Endian::Little)?;
                out.month = reader.read_u8()?;
                out.day = reader.read_u8()?;
                out.hour = reader.read_u8()?;
                out.minute = reader.read_u8()?;
                out.second = reader.read_u8()?;
                out.day_of_week = reader.read_u8()?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct N3gppTime {
            pub universal_time: UniversalTime2,
            pub timezone_offset: i8,
            pub daylight_savings_adjustment: u8,
            pub radio_interface: i8,
        }
        impl N3gppTime {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                (&self.universal_time).encode_to(writer)?;
                writer.write_i8(*&self.timezone_offset);
                writer.write_u8(*&self.daylight_savings_adjustment);
                writer.write_i8(*&self.radio_interface);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.universal_time = UniversalTime2::decode_from(reader)?;
                out.timezone_offset = reader.read_i8()?;
                out.daylight_savings_adjustment = reader.read_u8()?;
                out.radio_interface = reader.read_i8()?;
                Ok(out)
            }
        }
    }
    #[doc = "Incremental Network Scan"]
    pub mod incremental_network_scan {
        use crate::{Endian, Error, TlvBuf, TlvReader, TlvWriter};
        use alloc::string::String;
        use alloc::vec::Vec;
        pub const MESSAGE_ID: u16 = 133;
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Request {
            pub network_type: Option<u8>,
        }
        impl Request {
            pub const TLV_NETWORK_TYPE: u8 = 16;
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = Vec::new();
                if let Some(value) = &self.network_type {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(*value);
                    tlvs.push(writer.into_tlv(16));
                }
                Ok(tlvs)
            }
            pub fn from_tlvs(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                let mut out = Self::default();
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 16) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u8()?;
                    out.network_type = Some(value);
                }
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Response {}
        impl Response {
            pub const TLV_RESULT: u8 = 2;
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                Ok(Vec::new())
            }
            pub fn from_tlvs(_tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Ok(Self::default())
            }
        }
        impl crate::Response for Response {
            type Request = Request;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = crate::result::success_tlvs();
                tlvs.extend(self.to_tlvs()?);
                Ok(tlvs)
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                crate::result::check_result(tlvs)?;
                Response::from_tlvs(tlvs)
            }
        }
        impl crate::Request for Request {
            const SERVICE: u16 = super::super::SERVICE_ID;
            const MESSAGE_ID: u16 = 133;
            type Response = Response;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                self.to_tlvs()
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Self::from_tlvs(tlvs)
            }
        }
    }
    #[doc = "Get DRX"]
    pub mod get_drx {
        use crate::{Endian, Error, TlvBuf, TlvReader, TlvWriter};
        use alloc::string::String;
        use alloc::vec::Vec;
        pub const MESSAGE_ID: u16 = 137;
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Request {}
        impl Request {
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                Ok(Vec::new())
            }
            pub fn from_tlvs(_tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Ok(Self::default())
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Response {
            pub info: Option<u32>,
        }
        impl Response {
            pub const TLV_RESULT: u8 = 2;
            pub const TLV_INFO: u8 = 16;
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = Vec::new();
                if let Some(value) = &self.info {
                    let mut writer = TlvWriter::new();
                    writer.write_u32(*value, Endian::Little);
                    tlvs.push(writer.into_tlv(16));
                }
                Ok(tlvs)
            }
            pub fn from_tlvs(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                let mut out = Self::default();
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 16) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u32(Endian::Little)?;
                    out.info = Some(value);
                }
                Ok(out)
            }
        }
        impl crate::Response for Response {
            type Request = Request;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = crate::result::success_tlvs();
                tlvs.extend(self.to_tlvs()?);
                Ok(tlvs)
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                crate::result::check_result(tlvs)?;
                Response::from_tlvs(tlvs)
            }
        }
        impl crate::Request for Request {
            const SERVICE: u16 = super::super::SERVICE_ID;
            const MESSAGE_ID: u16 = 137;
            type Response = Response;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                self.to_tlvs()
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Self::from_tlvs(tlvs)
            }
        }
    }
    #[doc = "Get LTE Cphy CA Info"]
    pub mod get_lte_cphy_ca_info {
        use crate::{Endian, Error, TlvBuf, TlvReader, TlvWriter};
        use alloc::string::String;
        use alloc::vec::Vec;
        pub const MESSAGE_ID: u16 = 172;
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Request {}
        impl Request {
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                Ok(Vec::new())
            }
            pub fn from_tlvs(_tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Ok(Self::default())
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Response {
            pub dl_bandwidth: Option<u32>,
            pub phy_ca_agg_scell_info: Option<PhyCaAggScellInfo>,
            pub phy_ca_agg_pcell_info: Option<PhyCaAggPcellInfo>,
            pub scell_index: Option<u8>,
            pub phy_ca_agg_secondary_cells: Option<Vec<Ssc>>,
        }
        impl Response {
            pub const TLV_RESULT: u8 = 2;
            pub const TLV_DL_BANDWIDTH: u8 = 17;
            pub const TLV_PHY_CA_AGG_SCELL_INFO: u8 = 18;
            pub const TLV_PHY_CA_AGG_PCELL_INFO: u8 = 19;
            pub const TLV_SCELL_INDEX: u8 = 20;
            pub const TLV_PHY_CA_AGG_SECONDARY_CELLS: u8 = 21;
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = Vec::new();
                if let Some(value) = &self.dl_bandwidth {
                    let mut writer = TlvWriter::new();
                    writer.write_u32(*value, Endian::Little);
                    tlvs.push(writer.into_tlv(17));
                }
                if let Some(value) = &self.phy_ca_agg_scell_info {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(18));
                }
                if let Some(value) = &self.phy_ca_agg_pcell_info {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(19));
                }
                if let Some(value) = &self.scell_index {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(*value);
                    tlvs.push(writer.into_tlv(20));
                }
                if let Some(value) = &self.phy_ca_agg_secondary_cells {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::try_from((value).len()).map_err(|_| Error::TooLong)?);
                    for item in value {
                        (item).encode_to(&mut writer)?;
                    }
                    tlvs.push(writer.into_tlv(21));
                }
                Ok(tlvs)
            }
            pub fn from_tlvs(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                let mut out = Self::default();
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 17) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u32(Endian::Little)?;
                    out.dl_bandwidth = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 18) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = PhyCaAggScellInfo::decode_from(&mut reader)?;
                    out.phy_ca_agg_scell_info = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 19) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = PhyCaAggPcellInfo::decode_from(&mut reader)?;
                    out.phy_ca_agg_pcell_info = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 20) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u8()?;
                    out.scell_index = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 21) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let count = reader.read_u8()? as usize;
                    let mut items = Vec::with_capacity(count.min(256));
                    for _ in 0..count {
                        items.push(Ssc::decode_from(&mut reader)?);
                    }
                    let value = items;
                    out.phy_ca_agg_secondary_cells = Some(value);
                }
                Ok(out)
            }
        }
        impl crate::Response for Response {
            type Request = Request;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = crate::result::success_tlvs();
                tlvs.extend(self.to_tlvs()?);
                Ok(tlvs)
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                crate::result::check_result(tlvs)?;
                Response::from_tlvs(tlvs)
            }
        }
        impl crate::Request for Request {
            const SERVICE: u16 = super::super::SERVICE_ID;
            const MESSAGE_ID: u16 = 172;
            type Response = Response;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                self.to_tlvs()
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Self::from_tlvs(tlvs)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct PhyCaAggScellInfo {
            pub physical_cell_id: u16,
            pub rx_channel: u16,
            pub dl_bandwidth: u32,
            pub lte_band: u16,
            pub state: u32,
        }
        impl PhyCaAggScellInfo {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u16(*&self.physical_cell_id, Endian::Little);
                writer.write_u16(*&self.rx_channel, Endian::Little);
                writer.write_u32(*&self.dl_bandwidth, Endian::Little);
                writer.write_u16(*&self.lte_band, Endian::Little);
                writer.write_u32(*&self.state, Endian::Little);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.physical_cell_id = reader.read_u16(Endian::Little)?;
                out.rx_channel = reader.read_u16(Endian::Little)?;
                out.dl_bandwidth = reader.read_u32(Endian::Little)?;
                out.lte_band = reader.read_u16(Endian::Little)?;
                out.state = reader.read_u32(Endian::Little)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct PhyCaAggPcellInfo {
            pub physical_cell_id: u16,
            pub rx_channel: u16,
            pub dl_bandwidth: u32,
            pub lte_band: u16,
        }
        impl PhyCaAggPcellInfo {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u16(*&self.physical_cell_id, Endian::Little);
                writer.write_u16(*&self.rx_channel, Endian::Little);
                writer.write_u32(*&self.dl_bandwidth, Endian::Little);
                writer.write_u16(*&self.lte_band, Endian::Little);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.physical_cell_id = reader.read_u16(Endian::Little)?;
                out.rx_channel = reader.read_u16(Endian::Little)?;
                out.dl_bandwidth = reader.read_u32(Endian::Little)?;
                out.lte_band = reader.read_u16(Endian::Little)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Ssc {
            pub physical_cell_id: u16,
            pub rx_channel: u16,
            pub dl_bandwidth: u32,
            pub lte_band: u16,
            pub state: u32,
            pub cell_index: u8,
        }
        impl Ssc {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u16(*&self.physical_cell_id, Endian::Little);
                writer.write_u16(*&self.rx_channel, Endian::Little);
                writer.write_u32(*&self.dl_bandwidth, Endian::Little);
                writer.write_u16(*&self.lte_band, Endian::Little);
                writer.write_u32(*&self.state, Endian::Little);
                writer.write_u8(*&self.cell_index);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.physical_cell_id = reader.read_u16(Endian::Little)?;
                out.rx_channel = reader.read_u16(Endian::Little)?;
                out.dl_bandwidth = reader.read_u32(Endian::Little)?;
                out.lte_band = reader.read_u16(Endian::Little)?;
                out.state = reader.read_u32(Endian::Little)?;
                out.cell_index = reader.read_u8()?;
                Ok(out)
            }
        }
    }
    #[doc = "Get ENDC Config"]
    pub mod get_endc_config {
        use crate::{Endian, Error, TlvBuf, TlvReader, TlvWriter};
        use alloc::string::String;
        use alloc::vec::Vec;
        pub const MESSAGE_ID: u16 = 232;
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Request {}
        impl Request {
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                Ok(Vec::new())
            }
            pub fn from_tlvs(_tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Ok(Self::default())
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Response {
            pub enabled: Option<bool>,
            pub immediate_scg_release: Option<bool>,
        }
        impl Response {
            pub const TLV_RESULT: u8 = 2;
            pub const TLV_ENABLED: u8 = 16;
            pub const TLV_IMMEDIATE_SCG_RELEASE: u8 = 17;
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = Vec::new();
                if let Some(value) = &self.enabled {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::from(*value));
                    tlvs.push(writer.into_tlv(16));
                }
                if let Some(value) = &self.immediate_scg_release {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::from(*value));
                    tlvs.push(writer.into_tlv(17));
                }
                Ok(tlvs)
            }
            pub fn from_tlvs(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                let mut out = Self::default();
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 16) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u8()? != 0;
                    out.enabled = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 17) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u8()? != 0;
                    out.immediate_scg_release = Some(value);
                }
                Ok(out)
            }
        }
        impl crate::Response for Response {
            type Request = Request;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = crate::result::success_tlvs();
                tlvs.extend(self.to_tlvs()?);
                Ok(tlvs)
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                crate::result::check_result(tlvs)?;
                Response::from_tlvs(tlvs)
            }
        }
        impl crate::Request for Request {
            const SERVICE: u16 = super::super::SERVICE_ID;
            const MESSAGE_ID: u16 = 232;
            type Response = Response;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                self.to_tlvs()
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Self::from_tlvs(tlvs)
            }
        }
    }
    #[doc = "Swi Get Status"]
    pub mod swi_get_status {
        use crate::{Endian, Error, TlvBuf, TlvReader, TlvWriter};
        use alloc::string::String;
        use alloc::vec::Vec;
        pub const MESSAGE_ID: u16 = 21846;
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Request {}
        impl Request {
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                Ok(Vec::new())
            }
            pub fn from_tlvs(_tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Ok(Self::default())
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Response {
            pub common_info_v2: CommonInfoV2,
            pub lte_info: Option<LteInfo>,
        }
        impl Response {
            pub const TLV_RESULT: u8 = 2;
            pub const TLV_COMMON_INFO_V2: u8 = 1;
            pub const TLV_LTE_INFO: u8 = 16;
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = Vec::new();
                {
                    let mut writer = TlvWriter::new();
                    (&self.common_info_v2).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(1));
                }
                if let Some(value) = &self.lte_info {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(16));
                }
                Ok(tlvs)
            }
            pub fn from_tlvs(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                let mut out = Self::default();
                let tlv = tlvs
                    .iter()
                    .find(|tlv| tlv.id == 1)
                    .ok_or(Error::MissingTlv { id: 1 })?;
                let mut reader = TlvReader::new(&tlv.value);
                out.common_info_v2 = CommonInfoV2::decode_from(&mut reader)?;
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 16) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = LteInfo::decode_from(&mut reader)?;
                    out.lte_info = Some(value);
                }
                Ok(out)
            }
        }
        impl crate::Response for Response {
            type Request = Request;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = crate::result::success_tlvs();
                tlvs.extend(self.to_tlvs()?);
                Ok(tlvs)
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                crate::result::check_result(tlvs)?;
                Response::from_tlvs(tlvs)
            }
        }
        impl crate::Request for Request {
            const SERVICE: u16 = super::super::SERVICE_ID;
            const MESSAGE_ID: u16 = 21846;
            type Response = Response;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                self.to_tlvs()
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Self::from_tlvs(tlvs)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct CommonInfoV2 {
            pub temperature: i8,
            pub modem_mode: u8,
            pub system_mode: u8,
            pub ims_registration_state: u8,
            pub packet_service_state: u8,
        }
        impl CommonInfoV2 {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_i8(*&self.temperature);
                writer.write_u8(*&self.modem_mode);
                writer.write_u8(*&self.system_mode);
                writer.write_u8(*&self.ims_registration_state);
                writer.write_u8(*&self.packet_service_state);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.temperature = reader.read_i8()?;
                out.modem_mode = reader.read_u8()?;
                out.system_mode = reader.read_u8()?;
                out.ims_registration_state = reader.read_u8()?;
                out.packet_service_state = reader.read_u8()?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct LteInfo {
            pub band: u8,
            pub bandwidth: u8,
            pub rx_channel: u16,
            pub tx_channel: u16,
            pub emm_state: u8,
            pub emm_sub_state: u8,
            pub emm_connection_state: u8,
        }
        impl LteInfo {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(*&self.band);
                writer.write_u8(*&self.bandwidth);
                writer.write_u16(*&self.rx_channel, Endian::Little);
                writer.write_u16(*&self.tx_channel, Endian::Little);
                writer.write_u8(*&self.emm_state);
                writer.write_u8(*&self.emm_sub_state);
                writer.write_u8(*&self.emm_connection_state);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.band = reader.read_u8()?;
                out.bandwidth = reader.read_u8()?;
                out.rx_channel = reader.read_u16(Endian::Little)?;
                out.tx_channel = reader.read_u16(Endian::Little)?;
                out.emm_state = reader.read_u8()?;
                out.emm_sub_state = reader.read_u8()?;
                out.emm_connection_state = reader.read_u8()?;
                Ok(out)
            }
        }
    }
}
pub mod indications {
    #[doc = "Event Report"]
    pub mod event_report {
        use crate::{Endian, Error, TlvBuf, TlvReader, TlvWriter};
        use alloc::string::String;
        use alloc::vec::Vec;
        pub const MESSAGE_ID: u16 = 2;
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Indication {}
        impl Indication {
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                Ok(Vec::new())
            }
            pub fn from_tlvs(_tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Ok(Self::default())
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct SignalStrength {
            pub strength: i8,
            pub radio_interface: i8,
        }
        impl SignalStrength {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_i8(*&self.strength);
                writer.write_i8(*&self.radio_interface);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.strength = reader.read_i8()?;
                out.radio_interface = reader.read_i8()?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Element {
            pub radio_interface: i8,
            pub active_band_class: u16,
            pub active_channel: u16,
        }
        impl Element {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_i8(*&self.radio_interface);
                writer.write_u16(*&self.active_band_class, Endian::Little);
                writer.write_u16(*&self.active_channel, Endian::Little);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.radio_interface = reader.read_i8()?;
                out.active_band_class = reader.read_u16(Endian::Little)?;
                out.active_channel = reader.read_u16(Endian::Little)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct RegistrationRejectReason {
            pub service_domain: u8,
            pub reject_cause: u16,
        }
        impl RegistrationRejectReason {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(*&self.service_domain);
                writer.write_u16(*&self.reject_cause, Endian::Little);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.service_domain = reader.read_u8()?;
                out.reject_cause = reader.read_u16(Endian::Little)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Rssi {
            pub rssi: u8,
            pub radio_interface: i8,
        }
        impl Rssi {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(*&self.rssi);
                writer.write_i8(*&self.radio_interface);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.rssi = reader.read_u8()?;
                out.radio_interface = reader.read_i8()?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Ecio {
            pub ecio: u8,
            pub radio_interface: i8,
        }
        impl Ecio {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(*&self.ecio);
                writer.write_i8(*&self.radio_interface);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.ecio = reader.read_u8()?;
                out.radio_interface = reader.read_i8()?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct ErrorRate {
            pub rate: u16,
            pub radio_interface: i8,
        }
        impl ErrorRate {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u16(*&self.rate, Endian::Little);
                writer.write_i8(*&self.radio_interface);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.rate = reader.read_u16(Endian::Little)?;
                out.radio_interface = reader.read_i8()?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Rsrq {
            pub rsrq: i8,
            pub radio_interface: i8,
        }
        impl Rsrq {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_i8(*&self.rsrq);
                writer.write_i8(*&self.radio_interface);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.rsrq = reader.read_i8()?;
                out.radio_interface = reader.read_i8()?;
                Ok(out)
            }
        }
    }
    #[doc = "Serving System"]
    pub mod serving_system {
        use crate::{Endian, Error, TlvBuf, TlvReader, TlvWriter};
        use alloc::string::String;
        use alloc::vec::Vec;
        pub const MESSAGE_ID: u16 = 36;
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Indication {}
        impl Indication {
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                Ok(Vec::new())
            }
            pub fn from_tlvs(_tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Ok(Self::default())
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct ServingSystem {
            pub registration_state: u8,
            pub cs_attach_state: u8,
            pub ps_attach_state: u8,
            pub selected_network: u8,
            pub radio_interfaces: Vec<i8>,
        }
        impl ServingSystem {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(*&self.registration_state);
                writer.write_u8(*&self.cs_attach_state);
                writer.write_u8(*&self.ps_attach_state);
                writer.write_u8(*&self.selected_network);
                writer.write_u8(
                    u8::try_from((&self.radio_interfaces).len()).map_err(|_| Error::TooLong)?,
                );
                for item in &self.radio_interfaces {
                    writer.write_i8(*item);
                }
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.registration_state = reader.read_u8()?;
                out.cs_attach_state = reader.read_u8()?;
                out.ps_attach_state = reader.read_u8()?;
                out.selected_network = reader.read_u8()?;
                let count = reader.read_u8()? as usize;
                let mut items = Vec::with_capacity(count.min(256));
                for _ in 0..count {
                    items.push(reader.read_i8()?);
                }
                out.radio_interfaces = items;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct CurrentPlmn {
            pub mcc: u16,
            pub mnc: u16,
            pub description: String,
        }
        impl CurrentPlmn {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u16(*&self.mcc, Endian::Little);
                writer.write_u16(*&self.mnc, Endian::Little);
                writer.write_string(&self.description, 1, None)?;
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.mcc = reader.read_u16(Endian::Little)?;
                out.mnc = reader.read_u16(Endian::Little)?;
                out.description = reader.read_string(1, 0)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct CdmaSystemId {
            pub sid: u16,
            pub nid: u16,
        }
        impl CdmaSystemId {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u16(*&self.sid, Endian::Little);
                writer.write_u16(*&self.nid, Endian::Little);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.sid = reader.read_u16(Endian::Little)?;
                out.nid = reader.read_u16(Endian::Little)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct CdmaBaseStationInfo {
            pub base_station_id: u16,
            pub base_station_latitude: i32,
            pub base_station_longitude: i32,
        }
        impl CdmaBaseStationInfo {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u16(*&self.base_station_id, Endian::Little);
                writer.write_i32(*&self.base_station_latitude, Endian::Little);
                writer.write_i32(*&self.base_station_longitude, Endian::Little);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.base_station_id = reader.read_u16(Endian::Little)?;
                out.base_station_latitude = reader.read_i32(Endian::Little)?;
                out.base_station_longitude = reader.read_i32(Endian::Little)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Element {
            pub radio_interface: u8,
            pub roaming_indicator: u8,
        }
        impl Element {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(*&self.radio_interface);
                writer.write_u8(*&self.roaming_indicator);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.radio_interface = reader.read_u8()?;
                out.roaming_indicator = reader.read_u8()?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct TimeZone3gpp2 {
            pub leap_seconds: u8,
            pub local_time_offset: i8,
            pub daylight_saving_time: bool,
        }
        impl TimeZone3gpp2 {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(*&self.leap_seconds);
                writer.write_i8(*&self.local_time_offset);
                writer.write_u8(u8::from(*&self.daylight_saving_time));
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.leap_seconds = reader.read_u8()?;
                out.local_time_offset = reader.read_i8()?;
                out.daylight_saving_time = reader.read_u8()? != 0;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct UniversalTimeAndLocalTimeZone3gpp {
            pub year: u16,
            pub month: u8,
            pub day: u8,
            pub hour: u8,
            pub minute: u8,
            pub second: u8,
            pub time_zone: u8,
        }
        impl UniversalTimeAndLocalTimeZone3gpp {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u16(*&self.year, Endian::Little);
                writer.write_u8(*&self.month);
                writer.write_u8(*&self.day);
                writer.write_u8(*&self.hour);
                writer.write_u8(*&self.minute);
                writer.write_u8(*&self.second);
                writer.write_u8(*&self.time_zone);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.year = reader.read_u16(Endian::Little)?;
                out.month = reader.read_u8()?;
                out.day = reader.read_u8()?;
                out.hour = reader.read_u8()?;
                out.minute = reader.read_u8()?;
                out.second = reader.read_u8()?;
                out.time_zone = reader.read_u8()?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct DetailedServiceStatus {
            pub status: u8,
            pub capability: u8,
            pub hdr_status: u8,
            pub hdr_hybrid: bool,
            pub forbidden: bool,
        }
        impl DetailedServiceStatus {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(*&self.status);
                writer.write_u8(*&self.capability);
                writer.write_u8(*&self.hdr_status);
                writer.write_u8(u8::from(*&self.hdr_hybrid));
                writer.write_u8(u8::from(*&self.forbidden));
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.status = reader.read_u8()?;
                out.capability = reader.read_u8()?;
                out.hdr_status = reader.read_u8()?;
                out.hdr_hybrid = reader.read_u8()? != 0;
                out.forbidden = reader.read_u8()? != 0;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct CdmaSystemInfo {
            pub mcc: u16,
            pub imsi_11_12: u8,
        }
        impl CdmaSystemInfo {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u16(*&self.mcc, Endian::Little);
                writer.write_u8(*&self.imsi_11_12);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.mcc = reader.read_u16(Endian::Little)?;
                out.imsi_11_12 = reader.read_u8()?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct CallBarringStatus {
            pub cs_status: i32,
            pub ps_status: i32,
        }
        impl CallBarringStatus {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_i32(*&self.cs_status, Endian::Little);
                writer.write_i32(*&self.ps_status, Endian::Little);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.cs_status = reader.read_i32(Endian::Little)?;
                out.ps_status = reader.read_i32(Endian::Little)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct MncPcsDigitIncludeStatus {
            pub mcc: u16,
            pub mnc: u16,
            pub includes_pcs_digit: bool,
        }
        impl MncPcsDigitIncludeStatus {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u16(*&self.mcc, Endian::Little);
                writer.write_u16(*&self.mnc, Endian::Little);
                writer.write_u8(u8::from(*&self.includes_pcs_digit));
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.mcc = reader.read_u16(Endian::Little)?;
                out.mnc = reader.read_u16(Endian::Little)?;
                out.includes_pcs_digit = reader.read_u8()? != 0;
                Ok(out)
            }
        }
    }
    #[doc = "Operator Name"]
    pub mod operator_name {
        use crate::{Endian, Error, TlvBuf, TlvReader, TlvWriter};
        use alloc::string::String;
        use alloc::vec::Vec;
        pub const MESSAGE_ID: u16 = 58;
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Indication {}
        impl Indication {
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                Ok(Vec::new())
            }
            pub fn from_tlvs(_tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Ok(Self::default())
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct ServiceProviderName {
            pub name_display_condition: u8,
            pub name: String,
        }
        impl ServiceProviderName {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(*&self.name_display_condition);
                writer.write_string(&self.name, 1, None)?;
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.name_display_condition = reader.read_u8()?;
                out.name = reader.read_string(1, 0)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Element {
            pub mcc: String,
            pub mnc: String,
            pub lac1: u16,
            pub lac2: u16,
            pub plmn_name_record_identifier: u8,
        }
        impl Element {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_string(&self.mcc, 0, Some(3))?;
                writer.write_string(&self.mnc, 0, Some(3))?;
                writer.write_u16(*&self.lac1, Endian::Little);
                writer.write_u16(*&self.lac2, Endian::Little);
                writer.write_u8(*&self.plmn_name_record_identifier);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.mcc = reader.read_fixed_string(3)?;
                out.mnc = reader.read_fixed_string(3)?;
                out.lac1 = reader.read_u16(Endian::Little)?;
                out.lac2 = reader.read_u16(Endian::Little)?;
                out.plmn_name_record_identifier = reader.read_u8()?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Element2 {
            pub name_encoding: u8,
            pub short_country_initials: u8,
            pub long_name_spare_bits: u8,
            pub short_name_spare_bits: u8,
            pub long_name: Vec<u8>,
            pub short_name: Vec<u8>,
        }
        impl Element2 {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(*&self.name_encoding);
                writer.write_u8(*&self.short_country_initials);
                writer.write_u8(*&self.long_name_spare_bits);
                writer.write_u8(*&self.short_name_spare_bits);
                writer.write_u8(u8::try_from((&self.long_name).len()).map_err(|_| Error::TooLong)?);
                for item in &self.long_name {
                    writer.write_u8(*item);
                }
                writer
                    .write_u8(u8::try_from((&self.short_name).len()).map_err(|_| Error::TooLong)?);
                for item in &self.short_name {
                    writer.write_u8(*item);
                }
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.name_encoding = reader.read_u8()?;
                out.short_country_initials = reader.read_u8()?;
                out.long_name_spare_bits = reader.read_u8()?;
                out.short_name_spare_bits = reader.read_u8()?;
                let count = reader.read_u8()? as usize;
                let mut items = Vec::with_capacity(count.min(256));
                for _ in 0..count {
                    items.push(reader.read_u8()?);
                }
                out.long_name = items;
                let count = reader.read_u8()? as usize;
                let mut items = Vec::with_capacity(count.min(256));
                for _ in 0..count {
                    items.push(reader.read_u8()?);
                }
                out.short_name = items;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct NitzInformation {
            pub name_encoding: u8,
            pub short_country_initials: u8,
            pub long_name_spare_bits: u8,
            pub short_name_spare_bits: u8,
            pub long_name: Vec<u8>,
            pub short_name: Vec<u8>,
        }
        impl NitzInformation {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(*&self.name_encoding);
                writer.write_u8(*&self.short_country_initials);
                writer.write_u8(*&self.long_name_spare_bits);
                writer.write_u8(*&self.short_name_spare_bits);
                writer.write_u8(u8::try_from((&self.long_name).len()).map_err(|_| Error::TooLong)?);
                for item in &self.long_name {
                    writer.write_u8(*item);
                }
                writer
                    .write_u8(u8::try_from((&self.short_name).len()).map_err(|_| Error::TooLong)?);
                for item in &self.short_name {
                    writer.write_u8(*item);
                }
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.name_encoding = reader.read_u8()?;
                out.short_country_initials = reader.read_u8()?;
                out.long_name_spare_bits = reader.read_u8()?;
                out.short_name_spare_bits = reader.read_u8()?;
                let count = reader.read_u8()? as usize;
                let mut items = Vec::with_capacity(count.min(256));
                for _ in 0..count {
                    items.push(reader.read_u8()?);
                }
                out.long_name = items;
                let count = reader.read_u8()? as usize;
                let mut items = Vec::with_capacity(count.min(256));
                for _ in 0..count {
                    items.push(reader.read_u8()?);
                }
                out.short_name = items;
                Ok(out)
            }
        }
    }
    #[doc = "Network Time"]
    pub mod network_time {
        use crate::{Endian, Error, TlvBuf, TlvReader, TlvWriter};
        use alloc::string::String;
        use alloc::vec::Vec;
        pub const MESSAGE_ID: u16 = 76;
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Indication {}
        impl Indication {
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                Ok(Vec::new())
            }
            pub fn from_tlvs(_tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Ok(Self::default())
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct UniversalTime {
            pub year: u16,
            pub month: u8,
            pub day: u8,
            pub hour: u8,
            pub minute: u8,
            pub second: u8,
            pub day_of_week: u8,
        }
        impl UniversalTime {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u16(*&self.year, Endian::Little);
                writer.write_u8(*&self.month);
                writer.write_u8(*&self.day);
                writer.write_u8(*&self.hour);
                writer.write_u8(*&self.minute);
                writer.write_u8(*&self.second);
                writer.write_u8(*&self.day_of_week);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.year = reader.read_u16(Endian::Little)?;
                out.month = reader.read_u8()?;
                out.day = reader.read_u8()?;
                out.hour = reader.read_u8()?;
                out.minute = reader.read_u8()?;
                out.second = reader.read_u8()?;
                out.day_of_week = reader.read_u8()?;
                Ok(out)
            }
        }
    }
    #[doc = "System Info"]
    pub mod system_info {
        use crate::{Endian, Error, TlvBuf, TlvReader, TlvWriter};
        use alloc::string::String;
        use alloc::vec::Vec;
        pub const MESSAGE_ID: u16 = 78;
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Indication {}
        impl Indication {
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                Ok(Vec::new())
            }
            pub fn from_tlvs(_tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Ok(Self::default())
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct CdmaServiceStatus {
            pub service_status: u8,
            pub preferred_data_path: bool,
        }
        impl CdmaServiceStatus {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(*&self.service_status);
                writer.write_u8(u8::from(*&self.preferred_data_path));
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.service_status = reader.read_u8()?;
                out.preferred_data_path = reader.read_u8()? != 0;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct HdrServiceStatus {
            pub service_status: u8,
            pub preferred_data_path: bool,
        }
        impl HdrServiceStatus {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(*&self.service_status);
                writer.write_u8(u8::from(*&self.preferred_data_path));
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.service_status = reader.read_u8()?;
                out.preferred_data_path = reader.read_u8()? != 0;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct GsmServiceStatus {
            pub service_status: u8,
            pub true_service_status: u8,
            pub preferred_data_path: bool,
        }
        impl GsmServiceStatus {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(*&self.service_status);
                writer.write_u8(*&self.true_service_status);
                writer.write_u8(u8::from(*&self.preferred_data_path));
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.service_status = reader.read_u8()?;
                out.true_service_status = reader.read_u8()?;
                out.preferred_data_path = reader.read_u8()? != 0;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct WcdmaServiceStatus {
            pub service_status: u8,
            pub true_service_status: u8,
            pub preferred_data_path: bool,
        }
        impl WcdmaServiceStatus {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(*&self.service_status);
                writer.write_u8(*&self.true_service_status);
                writer.write_u8(u8::from(*&self.preferred_data_path));
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.service_status = reader.read_u8()?;
                out.true_service_status = reader.read_u8()?;
                out.preferred_data_path = reader.read_u8()? != 0;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct LteServiceStatus {
            pub service_status: u8,
            pub true_service_status: u8,
            pub preferred_data_path: bool,
        }
        impl LteServiceStatus {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(*&self.service_status);
                writer.write_u8(*&self.true_service_status);
                writer.write_u8(u8::from(*&self.preferred_data_path));
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.service_status = reader.read_u8()?;
                out.true_service_status = reader.read_u8()?;
                out.preferred_data_path = reader.read_u8()? != 0;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct CdmaSystemInfo {
            pub domain_valid: bool,
            pub domain: u8,
            pub service_capability_valid: bool,
            pub service_capability: u8,
            pub roaming_status_valid: bool,
            pub roaming_status: u8,
            pub forbidden_valid: bool,
            pub forbidden: bool,
            pub prl_match_valid: bool,
            pub prl_match: bool,
            pub p_rev_valid: bool,
            pub p_rev: u8,
            pub base_station_p_rev_valid: bool,
            pub base_station_p_rev: u8,
            pub concurrent_service_support_valid: bool,
            pub concurrent_service_support: bool,
            pub cdma_system_id_valid: bool,
            pub sid: u16,
            pub nid: u16,
            pub base_station_info_valid: bool,
            pub base_station_id: u16,
            pub base_station_latitude: i32,
            pub base_station_longitude: i32,
            pub packet_zone_valid: bool,
            pub packet_zone: u16,
            pub network_id_valid: bool,
            pub mcc: String,
            pub mnc: String,
        }
        impl CdmaSystemInfo {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(u8::from(*&self.domain_valid));
                writer.write_u8(*&self.domain);
                writer.write_u8(u8::from(*&self.service_capability_valid));
                writer.write_u8(*&self.service_capability);
                writer.write_u8(u8::from(*&self.roaming_status_valid));
                writer.write_u8(*&self.roaming_status);
                writer.write_u8(u8::from(*&self.forbidden_valid));
                writer.write_u8(u8::from(*&self.forbidden));
                writer.write_u8(u8::from(*&self.prl_match_valid));
                writer.write_u8(u8::from(*&self.prl_match));
                writer.write_u8(u8::from(*&self.p_rev_valid));
                writer.write_u8(*&self.p_rev);
                writer.write_u8(u8::from(*&self.base_station_p_rev_valid));
                writer.write_u8(*&self.base_station_p_rev);
                writer.write_u8(u8::from(*&self.concurrent_service_support_valid));
                writer.write_u8(u8::from(*&self.concurrent_service_support));
                writer.write_u8(u8::from(*&self.cdma_system_id_valid));
                writer.write_u16(*&self.sid, Endian::Little);
                writer.write_u16(*&self.nid, Endian::Little);
                writer.write_u8(u8::from(*&self.base_station_info_valid));
                writer.write_u16(*&self.base_station_id, Endian::Little);
                writer.write_i32(*&self.base_station_latitude, Endian::Little);
                writer.write_i32(*&self.base_station_longitude, Endian::Little);
                writer.write_u8(u8::from(*&self.packet_zone_valid));
                writer.write_u16(*&self.packet_zone, Endian::Little);
                writer.write_u8(u8::from(*&self.network_id_valid));
                writer.write_string(&self.mcc, 0, Some(3))?;
                writer.write_string(&self.mnc, 0, Some(3))?;
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.domain_valid = reader.read_u8()? != 0;
                out.domain = reader.read_u8()?;
                out.service_capability_valid = reader.read_u8()? != 0;
                out.service_capability = reader.read_u8()?;
                out.roaming_status_valid = reader.read_u8()? != 0;
                out.roaming_status = reader.read_u8()?;
                out.forbidden_valid = reader.read_u8()? != 0;
                out.forbidden = reader.read_u8()? != 0;
                out.prl_match_valid = reader.read_u8()? != 0;
                out.prl_match = reader.read_u8()? != 0;
                out.p_rev_valid = reader.read_u8()? != 0;
                out.p_rev = reader.read_u8()?;
                out.base_station_p_rev_valid = reader.read_u8()? != 0;
                out.base_station_p_rev = reader.read_u8()?;
                out.concurrent_service_support_valid = reader.read_u8()? != 0;
                out.concurrent_service_support = reader.read_u8()? != 0;
                out.cdma_system_id_valid = reader.read_u8()? != 0;
                out.sid = reader.read_u16(Endian::Little)?;
                out.nid = reader.read_u16(Endian::Little)?;
                out.base_station_info_valid = reader.read_u8()? != 0;
                out.base_station_id = reader.read_u16(Endian::Little)?;
                out.base_station_latitude = reader.read_i32(Endian::Little)?;
                out.base_station_longitude = reader.read_i32(Endian::Little)?;
                out.packet_zone_valid = reader.read_u8()? != 0;
                out.packet_zone = reader.read_u16(Endian::Little)?;
                out.network_id_valid = reader.read_u8()? != 0;
                out.mcc = reader.read_fixed_string(3)?;
                out.mnc = reader.read_fixed_string(3)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct HdrSystemInfo {
            pub domain_valid: bool,
            pub domain: u8,
            pub service_capability_valid: bool,
            pub service_capability: u8,
            pub roaming_status_valid: bool,
            pub roaming_status: u8,
            pub forbidden_valid: bool,
            pub forbidden: bool,
            pub prl_match_valid: bool,
            pub prl_match: bool,
            pub personality_valid: bool,
            pub personality: u8,
            pub protocol_revision_valid: bool,
            pub protocol_revision: u8,
            pub is_856_system_id_valid: bool,
            pub is_856_system_id: String,
        }
        impl HdrSystemInfo {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(u8::from(*&self.domain_valid));
                writer.write_u8(*&self.domain);
                writer.write_u8(u8::from(*&self.service_capability_valid));
                writer.write_u8(*&self.service_capability);
                writer.write_u8(u8::from(*&self.roaming_status_valid));
                writer.write_u8(*&self.roaming_status);
                writer.write_u8(u8::from(*&self.forbidden_valid));
                writer.write_u8(u8::from(*&self.forbidden));
                writer.write_u8(u8::from(*&self.prl_match_valid));
                writer.write_u8(u8::from(*&self.prl_match));
                writer.write_u8(u8::from(*&self.personality_valid));
                writer.write_u8(*&self.personality);
                writer.write_u8(u8::from(*&self.protocol_revision_valid));
                writer.write_u8(*&self.protocol_revision);
                writer.write_u8(u8::from(*&self.is_856_system_id_valid));
                writer.write_string(&self.is_856_system_id, 0, Some(16))?;
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.domain_valid = reader.read_u8()? != 0;
                out.domain = reader.read_u8()?;
                out.service_capability_valid = reader.read_u8()? != 0;
                out.service_capability = reader.read_u8()?;
                out.roaming_status_valid = reader.read_u8()? != 0;
                out.roaming_status = reader.read_u8()?;
                out.forbidden_valid = reader.read_u8()? != 0;
                out.forbidden = reader.read_u8()? != 0;
                out.prl_match_valid = reader.read_u8()? != 0;
                out.prl_match = reader.read_u8()? != 0;
                out.personality_valid = reader.read_u8()? != 0;
                out.personality = reader.read_u8()?;
                out.protocol_revision_valid = reader.read_u8()? != 0;
                out.protocol_revision = reader.read_u8()?;
                out.is_856_system_id_valid = reader.read_u8()? != 0;
                out.is_856_system_id = reader.read_fixed_string(16)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct GsmSystemInfoV2 {
            pub domain_valid: bool,
            pub domain: u8,
            pub service_capability_valid: bool,
            pub service_capability: u8,
            pub roaming_status_valid: bool,
            pub roaming_status: u8,
            pub forbidden_valid: bool,
            pub forbidden: bool,
            pub lac_valid: bool,
            pub lac: u16,
            pub cid_valid: bool,
            pub cid: u32,
            pub registration_reject_info_valid: bool,
            pub registration_reject_domain: u8,
            pub registration_reject_cause: u8,
            pub network_id_valid: bool,
            pub mcc: String,
            pub mnc: String,
            pub egprs_support_valid: bool,
            pub egprs_support: bool,
            pub dtm_support_valid: bool,
            pub dtm_support: bool,
        }
        impl GsmSystemInfoV2 {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(u8::from(*&self.domain_valid));
                writer.write_u8(*&self.domain);
                writer.write_u8(u8::from(*&self.service_capability_valid));
                writer.write_u8(*&self.service_capability);
                writer.write_u8(u8::from(*&self.roaming_status_valid));
                writer.write_u8(*&self.roaming_status);
                writer.write_u8(u8::from(*&self.forbidden_valid));
                writer.write_u8(u8::from(*&self.forbidden));
                writer.write_u8(u8::from(*&self.lac_valid));
                writer.write_u16(*&self.lac, Endian::Little);
                writer.write_u8(u8::from(*&self.cid_valid));
                writer.write_u32(*&self.cid, Endian::Little);
                writer.write_u8(u8::from(*&self.registration_reject_info_valid));
                writer.write_u8(*&self.registration_reject_domain);
                writer.write_u8(*&self.registration_reject_cause);
                writer.write_u8(u8::from(*&self.network_id_valid));
                writer.write_string(&self.mcc, 0, Some(3))?;
                writer.write_string(&self.mnc, 0, Some(3))?;
                writer.write_u8(u8::from(*&self.egprs_support_valid));
                writer.write_u8(u8::from(*&self.egprs_support));
                writer.write_u8(u8::from(*&self.dtm_support_valid));
                writer.write_u8(u8::from(*&self.dtm_support));
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.domain_valid = reader.read_u8()? != 0;
                out.domain = reader.read_u8()?;
                out.service_capability_valid = reader.read_u8()? != 0;
                out.service_capability = reader.read_u8()?;
                out.roaming_status_valid = reader.read_u8()? != 0;
                out.roaming_status = reader.read_u8()?;
                out.forbidden_valid = reader.read_u8()? != 0;
                out.forbidden = reader.read_u8()? != 0;
                out.lac_valid = reader.read_u8()? != 0;
                out.lac = reader.read_u16(Endian::Little)?;
                out.cid_valid = reader.read_u8()? != 0;
                out.cid = reader.read_u32(Endian::Little)?;
                out.registration_reject_info_valid = reader.read_u8()? != 0;
                out.registration_reject_domain = reader.read_u8()?;
                out.registration_reject_cause = reader.read_u8()?;
                out.network_id_valid = reader.read_u8()? != 0;
                out.mcc = reader.read_fixed_string(3)?;
                out.mnc = reader.read_fixed_string(3)?;
                out.egprs_support_valid = reader.read_u8()? != 0;
                out.egprs_support = reader.read_u8()? != 0;
                out.dtm_support_valid = reader.read_u8()? != 0;
                out.dtm_support = reader.read_u8()? != 0;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct WcdmaSystemInfoV2 {
            pub domain_valid: bool,
            pub domain: u8,
            pub service_capability_valid: bool,
            pub service_capability: u8,
            pub roaming_status_valid: bool,
            pub roaming_status: u8,
            pub forbidden_valid: bool,
            pub forbidden: bool,
            pub lac_valid: bool,
            pub lac: u16,
            pub cid_valid: bool,
            pub cid: u32,
            pub registration_reject_info_valid: bool,
            pub registration_reject_domain: u8,
            pub registration_reject_cause: u8,
            pub network_id_valid: bool,
            pub mcc: String,
            pub mnc: String,
            pub hs_call_status_valid: bool,
            pub hs_call_status: u8,
            pub hs_service_valid: bool,
            pub hs_service: u8,
            pub primary_scrambling_code_valid: bool,
            pub primary_scrambling_code: u16,
        }
        impl WcdmaSystemInfoV2 {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(u8::from(*&self.domain_valid));
                writer.write_u8(*&self.domain);
                writer.write_u8(u8::from(*&self.service_capability_valid));
                writer.write_u8(*&self.service_capability);
                writer.write_u8(u8::from(*&self.roaming_status_valid));
                writer.write_u8(*&self.roaming_status);
                writer.write_u8(u8::from(*&self.forbidden_valid));
                writer.write_u8(u8::from(*&self.forbidden));
                writer.write_u8(u8::from(*&self.lac_valid));
                writer.write_u16(*&self.lac, Endian::Little);
                writer.write_u8(u8::from(*&self.cid_valid));
                writer.write_u32(*&self.cid, Endian::Little);
                writer.write_u8(u8::from(*&self.registration_reject_info_valid));
                writer.write_u8(*&self.registration_reject_domain);
                writer.write_u8(*&self.registration_reject_cause);
                writer.write_u8(u8::from(*&self.network_id_valid));
                writer.write_string(&self.mcc, 0, Some(3))?;
                writer.write_string(&self.mnc, 0, Some(3))?;
                writer.write_u8(u8::from(*&self.hs_call_status_valid));
                writer.write_u8(*&self.hs_call_status);
                writer.write_u8(u8::from(*&self.hs_service_valid));
                writer.write_u8(*&self.hs_service);
                writer.write_u8(u8::from(*&self.primary_scrambling_code_valid));
                writer.write_u16(*&self.primary_scrambling_code, Endian::Little);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.domain_valid = reader.read_u8()? != 0;
                out.domain = reader.read_u8()?;
                out.service_capability_valid = reader.read_u8()? != 0;
                out.service_capability = reader.read_u8()?;
                out.roaming_status_valid = reader.read_u8()? != 0;
                out.roaming_status = reader.read_u8()?;
                out.forbidden_valid = reader.read_u8()? != 0;
                out.forbidden = reader.read_u8()? != 0;
                out.lac_valid = reader.read_u8()? != 0;
                out.lac = reader.read_u16(Endian::Little)?;
                out.cid_valid = reader.read_u8()? != 0;
                out.cid = reader.read_u32(Endian::Little)?;
                out.registration_reject_info_valid = reader.read_u8()? != 0;
                out.registration_reject_domain = reader.read_u8()?;
                out.registration_reject_cause = reader.read_u8()?;
                out.network_id_valid = reader.read_u8()? != 0;
                out.mcc = reader.read_fixed_string(3)?;
                out.mnc = reader.read_fixed_string(3)?;
                out.hs_call_status_valid = reader.read_u8()? != 0;
                out.hs_call_status = reader.read_u8()?;
                out.hs_service_valid = reader.read_u8()? != 0;
                out.hs_service = reader.read_u8()?;
                out.primary_scrambling_code_valid = reader.read_u8()? != 0;
                out.primary_scrambling_code = reader.read_u16(Endian::Little)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct LteSystemInfoV2 {
            pub domain_valid: bool,
            pub domain: u8,
            pub service_capability_valid: bool,
            pub service_capability: u8,
            pub roaming_status_valid: bool,
            pub roaming_status: u8,
            pub forbidden_valid: bool,
            pub forbidden: bool,
            pub lac_valid: bool,
            pub lac: u16,
            pub cid_valid: bool,
            pub cid: u32,
            pub registration_reject_info_valid: bool,
            pub registration_reject_domain: u8,
            pub registration_reject_cause: u8,
            pub network_id_valid: bool,
            pub mcc: String,
            pub mnc: String,
            pub tac_valid: bool,
            pub tac: u16,
        }
        impl LteSystemInfoV2 {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(u8::from(*&self.domain_valid));
                writer.write_u8(*&self.domain);
                writer.write_u8(u8::from(*&self.service_capability_valid));
                writer.write_u8(*&self.service_capability);
                writer.write_u8(u8::from(*&self.roaming_status_valid));
                writer.write_u8(*&self.roaming_status);
                writer.write_u8(u8::from(*&self.forbidden_valid));
                writer.write_u8(u8::from(*&self.forbidden));
                writer.write_u8(u8::from(*&self.lac_valid));
                writer.write_u16(*&self.lac, Endian::Little);
                writer.write_u8(u8::from(*&self.cid_valid));
                writer.write_u32(*&self.cid, Endian::Little);
                writer.write_u8(u8::from(*&self.registration_reject_info_valid));
                writer.write_u8(*&self.registration_reject_domain);
                writer.write_u8(*&self.registration_reject_cause);
                writer.write_u8(u8::from(*&self.network_id_valid));
                writer.write_string(&self.mcc, 0, Some(3))?;
                writer.write_string(&self.mnc, 0, Some(3))?;
                writer.write_u8(u8::from(*&self.tac_valid));
                writer.write_u16(*&self.tac, Endian::Little);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.domain_valid = reader.read_u8()? != 0;
                out.domain = reader.read_u8()?;
                out.service_capability_valid = reader.read_u8()? != 0;
                out.service_capability = reader.read_u8()?;
                out.roaming_status_valid = reader.read_u8()? != 0;
                out.roaming_status = reader.read_u8()?;
                out.forbidden_valid = reader.read_u8()? != 0;
                out.forbidden = reader.read_u8()? != 0;
                out.lac_valid = reader.read_u8()? != 0;
                out.lac = reader.read_u16(Endian::Little)?;
                out.cid_valid = reader.read_u8()? != 0;
                out.cid = reader.read_u32(Endian::Little)?;
                out.registration_reject_info_valid = reader.read_u8()? != 0;
                out.registration_reject_domain = reader.read_u8()?;
                out.registration_reject_cause = reader.read_u8()?;
                out.network_id_valid = reader.read_u8()? != 0;
                out.mcc = reader.read_fixed_string(3)?;
                out.mnc = reader.read_fixed_string(3)?;
                out.tac_valid = reader.read_u8()? != 0;
                out.tac = reader.read_u16(Endian::Little)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct AdditionalCdmaSystemInfo {
            pub geo_system_index: u16,
            pub registration_period: u16,
        }
        impl AdditionalCdmaSystemInfo {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u16(*&self.geo_system_index, Endian::Little);
                writer.write_u16(*&self.registration_period, Endian::Little);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.geo_system_index = reader.read_u16(Endian::Little)?;
                out.registration_period = reader.read_u16(Endian::Little)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct AdditionalHdrSystemInfo {
            pub geo_system_index: u16,
        }
        impl AdditionalHdrSystemInfo {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u16(*&self.geo_system_index, Endian::Little);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.geo_system_index = reader.read_u16(Endian::Little)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct AdditionalGsmSystemInfo {
            pub geo_system_index: u16,
            pub cell_broadcast_support: u32,
        }
        impl AdditionalGsmSystemInfo {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u16(*&self.geo_system_index, Endian::Little);
                writer.write_u32(*&self.cell_broadcast_support, Endian::Little);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.geo_system_index = reader.read_u16(Endian::Little)?;
                out.cell_broadcast_support = reader.read_u32(Endian::Little)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct AdditionalWcdmaSystemInfo {
            pub geo_system_index: u16,
            pub cell_broadcast_support: u32,
        }
        impl AdditionalWcdmaSystemInfo {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u16(*&self.geo_system_index, Endian::Little);
                writer.write_u32(*&self.cell_broadcast_support, Endian::Little);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.geo_system_index = reader.read_u16(Endian::Little)?;
                out.cell_broadcast_support = reader.read_u32(Endian::Little)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct AdditionalLteSystemInfo {
            pub geo_system_index: u16,
        }
        impl AdditionalLteSystemInfo {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u16(*&self.geo_system_index, Endian::Little);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.geo_system_index = reader.read_u16(Endian::Little)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct GsmCallBarringStatus {
            pub cs_status: i32,
            pub ps_status: i32,
        }
        impl GsmCallBarringStatus {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_i32(*&self.cs_status, Endian::Little);
                writer.write_i32(*&self.ps_status, Endian::Little);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.cs_status = reader.read_i32(Endian::Little)?;
                out.ps_status = reader.read_i32(Endian::Little)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct WcdmaCallBarringStatus {
            pub cs_status: i32,
            pub ps_status: i32,
        }
        impl WcdmaCallBarringStatus {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_i32(*&self.cs_status, Endian::Little);
                writer.write_i32(*&self.ps_status, Endian::Little);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.cs_status = reader.read_i32(Endian::Little)?;
                out.ps_status = reader.read_i32(Endian::Little)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct TdScdmaServiceStatus {
            pub service_status: u8,
            pub true_service_status: u8,
            pub preferred_data_path: bool,
        }
        impl TdScdmaServiceStatus {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(*&self.service_status);
                writer.write_u8(*&self.true_service_status);
                writer.write_u8(u8::from(*&self.preferred_data_path));
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.service_status = reader.read_u8()?;
                out.true_service_status = reader.read_u8()?;
                out.preferred_data_path = reader.read_u8()? != 0;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct TdScmaSystemInfoV2 {
            pub domain_valid: bool,
            pub domain: u8,
            pub service_capability_valid: bool,
            pub service_capability: u8,
            pub roaming_status_valid: bool,
            pub roaming_status: u8,
            pub forbidden_valid: bool,
            pub forbidden: bool,
            pub lac_valid: bool,
            pub lac: u16,
            pub cid_valid: bool,
            pub cid: u32,
            pub registration_reject_info_valid: bool,
            pub registration_reject_domain: u8,
            pub registration_reject_cause: u8,
            pub network_id_valid: bool,
            pub mcc: String,
            pub mnc: String,
            pub hs_call_status_valid: bool,
            pub hs_call_status: u8,
            pub hs_service_valid: bool,
            pub hs_service: u8,
            pub cell_parameter_id_valid: bool,
            pub cell_parameter_id: u16,
            pub cell_broadcast_support_valid: bool,
            pub cell_broadcast_support: u32,
            pub cs_call_barring_status_valid: bool,
            pub cs_call_barring_status: i32,
            pub ps_call_barring_status_valid: bool,
            pub ps_call_barring_status: i32,
            pub cipher_domain_valid: bool,
            pub cipher_domain: u8,
        }
        impl TdScmaSystemInfoV2 {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(u8::from(*&self.domain_valid));
                writer.write_u8(*&self.domain);
                writer.write_u8(u8::from(*&self.service_capability_valid));
                writer.write_u8(*&self.service_capability);
                writer.write_u8(u8::from(*&self.roaming_status_valid));
                writer.write_u8(*&self.roaming_status);
                writer.write_u8(u8::from(*&self.forbidden_valid));
                writer.write_u8(u8::from(*&self.forbidden));
                writer.write_u8(u8::from(*&self.lac_valid));
                writer.write_u16(*&self.lac, Endian::Little);
                writer.write_u8(u8::from(*&self.cid_valid));
                writer.write_u32(*&self.cid, Endian::Little);
                writer.write_u8(u8::from(*&self.registration_reject_info_valid));
                writer.write_u8(*&self.registration_reject_domain);
                writer.write_u8(*&self.registration_reject_cause);
                writer.write_u8(u8::from(*&self.network_id_valid));
                writer.write_string(&self.mcc, 0, Some(3))?;
                writer.write_string(&self.mnc, 0, Some(3))?;
                writer.write_u8(u8::from(*&self.hs_call_status_valid));
                writer.write_u8(*&self.hs_call_status);
                writer.write_u8(u8::from(*&self.hs_service_valid));
                writer.write_u8(*&self.hs_service);
                writer.write_u8(u8::from(*&self.cell_parameter_id_valid));
                writer.write_u16(*&self.cell_parameter_id, Endian::Little);
                writer.write_u8(u8::from(*&self.cell_broadcast_support_valid));
                writer.write_u32(*&self.cell_broadcast_support, Endian::Little);
                writer.write_u8(u8::from(*&self.cs_call_barring_status_valid));
                writer.write_i32(*&self.cs_call_barring_status, Endian::Little);
                writer.write_u8(u8::from(*&self.ps_call_barring_status_valid));
                writer.write_i32(*&self.ps_call_barring_status, Endian::Little);
                writer.write_u8(u8::from(*&self.cipher_domain_valid));
                writer.write_u8(*&self.cipher_domain);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.domain_valid = reader.read_u8()? != 0;
                out.domain = reader.read_u8()?;
                out.service_capability_valid = reader.read_u8()? != 0;
                out.service_capability = reader.read_u8()?;
                out.roaming_status_valid = reader.read_u8()? != 0;
                out.roaming_status = reader.read_u8()?;
                out.forbidden_valid = reader.read_u8()? != 0;
                out.forbidden = reader.read_u8()? != 0;
                out.lac_valid = reader.read_u8()? != 0;
                out.lac = reader.read_u16(Endian::Little)?;
                out.cid_valid = reader.read_u8()? != 0;
                out.cid = reader.read_u32(Endian::Little)?;
                out.registration_reject_info_valid = reader.read_u8()? != 0;
                out.registration_reject_domain = reader.read_u8()?;
                out.registration_reject_cause = reader.read_u8()?;
                out.network_id_valid = reader.read_u8()? != 0;
                out.mcc = reader.read_fixed_string(3)?;
                out.mnc = reader.read_fixed_string(3)?;
                out.hs_call_status_valid = reader.read_u8()? != 0;
                out.hs_call_status = reader.read_u8()?;
                out.hs_service_valid = reader.read_u8()? != 0;
                out.hs_service = reader.read_u8()?;
                out.cell_parameter_id_valid = reader.read_u8()? != 0;
                out.cell_parameter_id = reader.read_u16(Endian::Little)?;
                out.cell_broadcast_support_valid = reader.read_u8()? != 0;
                out.cell_broadcast_support = reader.read_u32(Endian::Little)?;
                out.cs_call_barring_status_valid = reader.read_u8()? != 0;
                out.cs_call_barring_status = reader.read_i32(Endian::Little)?;
                out.ps_call_barring_status_valid = reader.read_u8()? != 0;
                out.ps_call_barring_status = reader.read_i32(Endian::Little)?;
                out.cipher_domain_valid = reader.read_u8()? != 0;
                out.cipher_domain = reader.read_u8()?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Nr5gServiceStatusInfo {
            pub service_status: u8,
            pub true_service_status: u8,
            pub preferred_data_path: bool,
        }
        impl Nr5gServiceStatusInfo {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(*&self.service_status);
                writer.write_u8(*&self.true_service_status);
                writer.write_u8(u8::from(*&self.preferred_data_path));
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.service_status = reader.read_u8()?;
                out.true_service_status = reader.read_u8()?;
                out.preferred_data_path = reader.read_u8()? != 0;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Nr5gSystemInfo {
            pub domain_valid: bool,
            pub domain: u8,
            pub service_capability_valid: bool,
            pub service_capability: u8,
            pub roaming_status_valid: bool,
            pub roaming_status: u8,
            pub forbidden_valid: bool,
            pub forbidden: bool,
            pub lac_valid: bool,
            pub lac: u16,
            pub cid_valid: bool,
            pub cid: u32,
            pub registration_reject_info_valid: bool,
            pub registration_reject_domain: u8,
            pub registration_reject_cause: u8,
            pub network_id_valid: bool,
            pub mcc: String,
            pub mnc: String,
            pub tac_valid: bool,
            pub tac: u16,
        }
        impl Nr5gSystemInfo {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(u8::from(*&self.domain_valid));
                writer.write_u8(*&self.domain);
                writer.write_u8(u8::from(*&self.service_capability_valid));
                writer.write_u8(*&self.service_capability);
                writer.write_u8(u8::from(*&self.roaming_status_valid));
                writer.write_u8(*&self.roaming_status);
                writer.write_u8(u8::from(*&self.forbidden_valid));
                writer.write_u8(u8::from(*&self.forbidden));
                writer.write_u8(u8::from(*&self.lac_valid));
                writer.write_u16(*&self.lac, Endian::Little);
                writer.write_u8(u8::from(*&self.cid_valid));
                writer.write_u32(*&self.cid, Endian::Little);
                writer.write_u8(u8::from(*&self.registration_reject_info_valid));
                writer.write_u8(*&self.registration_reject_domain);
                writer.write_u8(*&self.registration_reject_cause);
                writer.write_u8(u8::from(*&self.network_id_valid));
                writer.write_string(&self.mcc, 0, Some(3))?;
                writer.write_string(&self.mnc, 0, Some(3))?;
                writer.write_u8(u8::from(*&self.tac_valid));
                writer.write_u16(*&self.tac, Endian::Little);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.domain_valid = reader.read_u8()? != 0;
                out.domain = reader.read_u8()?;
                out.service_capability_valid = reader.read_u8()? != 0;
                out.service_capability = reader.read_u8()?;
                out.roaming_status_valid = reader.read_u8()? != 0;
                out.roaming_status = reader.read_u8()?;
                out.forbidden_valid = reader.read_u8()? != 0;
                out.forbidden = reader.read_u8()? != 0;
                out.lac_valid = reader.read_u8()? != 0;
                out.lac = reader.read_u16(Endian::Little)?;
                out.cid_valid = reader.read_u8()? != 0;
                out.cid = reader.read_u32(Endian::Little)?;
                out.registration_reject_info_valid = reader.read_u8()? != 0;
                out.registration_reject_domain = reader.read_u8()?;
                out.registration_reject_cause = reader.read_u8()?;
                out.network_id_valid = reader.read_u8()? != 0;
                out.mcc = reader.read_fixed_string(3)?;
                out.mnc = reader.read_fixed_string(3)?;
                out.tac_valid = reader.read_u8()? != 0;
                out.tac = reader.read_u16(Endian::Little)?;
                Ok(out)
            }
        }
    }
    #[doc = "Signal Info"]
    pub mod signal_info {
        use crate::{Endian, Error, TlvBuf, TlvReader, TlvWriter};
        use alloc::string::String;
        use alloc::vec::Vec;
        pub const MESSAGE_ID: u16 = 81;
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Indication {}
        impl Indication {
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                Ok(Vec::new())
            }
            pub fn from_tlvs(_tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Ok(Self::default())
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct CdmaSignalStrength {
            pub rssi: i8,
            pub ecio: i16,
        }
        impl CdmaSignalStrength {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_i8(*&self.rssi);
                writer.write_i16(*&self.ecio, Endian::Little);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.rssi = reader.read_i8()?;
                out.ecio = reader.read_i16(Endian::Little)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct HdrSignalStrength {
            pub rssi: i8,
            pub ecio: i16,
            pub sinr: u8,
            pub io: i32,
        }
        impl HdrSignalStrength {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_i8(*&self.rssi);
                writer.write_i16(*&self.ecio, Endian::Little);
                writer.write_u8(*&self.sinr);
                writer.write_i32(*&self.io, Endian::Little);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.rssi = reader.read_i8()?;
                out.ecio = reader.read_i16(Endian::Little)?;
                out.sinr = reader.read_u8()?;
                out.io = reader.read_i32(Endian::Little)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct WcdmaSignalStrength {
            pub rssi: i8,
            pub ecio: i16,
        }
        impl WcdmaSignalStrength {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_i8(*&self.rssi);
                writer.write_i16(*&self.ecio, Endian::Little);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.rssi = reader.read_i8()?;
                out.ecio = reader.read_i16(Endian::Little)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct LteSignalStrength {
            pub rssi: i8,
            pub rsrq: i8,
            pub rsrp: i16,
            pub snr: i16,
        }
        impl LteSignalStrength {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_i8(*&self.rssi);
                writer.write_i8(*&self.rsrq);
                writer.write_i16(*&self.rsrp, Endian::Little);
                writer.write_i16(*&self.snr, Endian::Little);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.rssi = reader.read_i8()?;
                out.rsrq = reader.read_i8()?;
                out.rsrp = reader.read_i16(Endian::Little)?;
                out.snr = reader.read_i16(Endian::Little)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct N5gSignalStrength {
            pub rsrp: i16,
            pub snr: i16,
        }
        impl N5gSignalStrength {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_i16(*&self.rsrp, Endian::Little);
                writer.write_i16(*&self.snr, Endian::Little);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.rsrp = reader.read_i16(Endian::Little)?;
                out.snr = reader.read_i16(Endian::Little)?;
                Ok(out)
            }
        }
    }
    #[doc = "Network Reject"]
    pub mod network_reject {
        use crate::{Endian, Error, TlvBuf, TlvReader, TlvWriter};
        use alloc::string::String;
        use alloc::vec::Vec;
        pub const MESSAGE_ID: u16 = 104;
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Indication {}
        impl Indication {
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                Ok(Vec::new())
            }
            pub fn from_tlvs(_tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Ok(Self::default())
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Plmn {
            pub mcc: u16,
            pub mnc: u16,
            pub includes_pcs_digit: bool,
        }
        impl Plmn {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u16(*&self.mcc, Endian::Little);
                writer.write_u16(*&self.mnc, Endian::Little);
                writer.write_u8(u8::from(*&self.includes_pcs_digit));
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.mcc = reader.read_u16(Endian::Little)?;
                out.mnc = reader.read_u16(Endian::Little)?;
                out.includes_pcs_digit = reader.read_u8()? != 0;
                Ok(out)
            }
        }
    }
    #[doc = "Incremental Network Scan"]
    pub mod incremental_network_scan {
        use crate::{Endian, Error, TlvBuf, TlvReader, TlvWriter};
        use alloc::string::String;
        use alloc::vec::Vec;
        pub const MESSAGE_ID: u16 = 133;
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Indication {}
        impl Indication {
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                Ok(Vec::new())
            }
            pub fn from_tlvs(_tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Ok(Self::default())
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Element {
            pub mcc: u16,
            pub mnc: u16,
            pub network_status: u8,
            pub radio_interface: i8,
            pub includes_pcs_digit: bool,
            pub description: String,
        }
        impl Element {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u16(*&self.mcc, Endian::Little);
                writer.write_u16(*&self.mnc, Endian::Little);
                writer.write_u8(*&self.network_status);
                writer.write_i8(*&self.radio_interface);
                writer.write_u8(u8::from(*&self.includes_pcs_digit));
                writer.write_string(&self.description, 1, None)?;
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.mcc = reader.read_u16(Endian::Little)?;
                out.mnc = reader.read_u16(Endian::Little)?;
                out.network_status = reader.read_u8()?;
                out.radio_interface = reader.read_i8()?;
                out.includes_pcs_digit = reader.read_u8()? != 0;
                out.description = reader.read_string(1, 0)?;
                Ok(out)
            }
        }
    }
}
