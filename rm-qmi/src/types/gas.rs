//! Generated by `cargo xtask qmi-codegen`; do not edit.
//!
//! Source: `qmi-service-gas.json`.

#![allow(unused_imports)]
pub const SERVICE_ID: u16 = 232;
pub mod messages {
    #[doc = "DMS Set USB Composition"]
    pub mod dms_set_usb_composition {
        use crate::{Endian, Error, TlvBuf, TlvReader, TlvWriter};
        use alloc::string::String;
        use alloc::vec::Vec;
        pub const MESSAGE_ID: u16 = 515;
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Request {
            pub usb_composition: u32,
            pub endpoint_type: u8,
            pub composition_persistence: bool,
            pub immediate_setting: bool,
            pub reboot_after_setting: bool,
        }
        impl Request {
            pub const TLV_USB_COMPOSITION: u8 = 1;
            pub const TLV_ENDPOINT_TYPE: u8 = 2;
            pub const TLV_COMPOSITION_PERSISTENCE: u8 = 3;
            pub const TLV_IMMEDIATE_SETTING: u8 = 4;
            pub const TLV_REBOOT_AFTER_SETTING: u8 = 5;
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = Vec::new();
                {
                    let mut writer = TlvWriter::new();
                    writer.write_u32(*&self.usb_composition, Endian::Little);
                    tlvs.push(writer.into_tlv(1));
                }
                {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(*&self.endpoint_type);
                    tlvs.push(writer.into_tlv(2));
                }
                {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::from(*&self.composition_persistence));
                    tlvs.push(writer.into_tlv(3));
                }
                {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::from(*&self.immediate_setting));
                    tlvs.push(writer.into_tlv(4));
                }
                {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::from(*&self.reboot_after_setting));
                    tlvs.push(writer.into_tlv(5));
                }
                Ok(tlvs)
            }
            pub fn from_tlvs(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                let mut out = Self::default();
                let tlv = tlvs
                    .iter()
                    .find(|tlv| tlv.id == 1)
                    .ok_or(Error::MissingTlv { id: 1 })?;
                let mut reader = TlvReader::new(&tlv.value);
                out.usb_composition = reader.read_u32(Endian::Little)?;
                let tlv = tlvs
                    .iter()
                    .find(|tlv| tlv.id == 2)
                    .ok_or(Error::MissingTlv { id: 2 })?;
                let mut reader = TlvReader::new(&tlv.value);
                out.endpoint_type = reader.read_u8()?;
                let tlv = tlvs
                    .iter()
                    .find(|tlv| tlv.id == 3)
                    .ok_or(Error::MissingTlv { id: 3 })?;
                let mut reader = TlvReader::new(&tlv.value);
                out.composition_persistence = reader.read_u8()? != 0;
                let tlv = tlvs
                    .iter()
                    .find(|tlv| tlv.id == 4)
                    .ok_or(Error::MissingTlv { id: 4 })?;
                let mut reader = TlvReader::new(&tlv.value);
                out.immediate_setting = reader.read_u8()? != 0;
                let tlv = tlvs
                    .iter()
                    .find(|tlv| tlv.id == 5)
                    .ok_or(Error::MissingTlv { id: 5 })?;
                let mut reader = TlvReader::new(&tlv.value);
                out.reboot_after_setting = reader.read_u8()? != 0;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Response {}
        impl Response {
            pub const TLV_RESULT: u8 = 2;
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                Ok(Vec::new())
            }
            pub fn from_tlvs(_tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Ok(Self::default())
            }
        }
        impl crate::Response for Response {
            type Request = Request;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = crate::result::success_tlvs();
                tlvs.extend(self.to_tlvs()?);
                Ok(tlvs)
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                crate::result::check_result(tlvs)?;
                Response::from_tlvs(tlvs)
            }
        }
        impl crate::Request for Request {
            const SERVICE: u16 = super::super::SERVICE_ID;
            const MESSAGE_ID: u16 = 515;
            type Response = Response;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                self.to_tlvs()
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Self::from_tlvs(tlvs)
            }
        }
    }
    #[doc = "DMS Get USB Composition"]
    pub mod dms_get_usb_composition {
        use crate::{Endian, Error, TlvBuf, TlvReader, TlvWriter};
        use alloc::string::String;
        use alloc::vec::Vec;
        pub const MESSAGE_ID: u16 = 516;
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Request {}
        impl Request {
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                Ok(Vec::new())
            }
            pub fn from_tlvs(_tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Ok(Self::default())
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Response {
            pub usb_composition: u32,
            pub endpoint_type: u8,
            pub composition_persistence: bool,
            pub immediate_setting: bool,
            pub reboot_after_setting: bool,
        }
        impl Response {
            pub const TLV_RESULT: u8 = 2;
            pub const TLV_USB_COMPOSITION: u8 = 3;
            pub const TLV_ENDPOINT_TYPE: u8 = 4;
            pub const TLV_COMPOSITION_PERSISTENCE: u8 = 5;
            pub const TLV_IMMEDIATE_SETTING: u8 = 6;
            pub const TLV_REBOOT_AFTER_SETTING: u8 = 7;
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = Vec::new();
                {
                    let mut writer = TlvWriter::new();
                    writer.write_u32(*&self.usb_composition, Endian::Little);
                    tlvs.push(writer.into_tlv(3));
                }
                {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(*&self.endpoint_type);
                    tlvs.push(writer.into_tlv(4));
                }
                {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::from(*&self.composition_persistence));
                    tlvs.push(writer.into_tlv(5));
                }
                {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::from(*&self.immediate_setting));
                    tlvs.push(writer.into_tlv(6));
                }
                {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::from(*&self.reboot_after_setting));
                    tlvs.push(writer.into_tlv(7));
                }
                Ok(tlvs)
            }
            pub fn from_tlvs(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                let mut out = Self::default();
                let tlv = tlvs
                    .iter()
                    .find(|tlv| tlv.id == 3)
                    .ok_or(Error::MissingTlv { id: 3 })?;
                let mut reader = TlvReader::new(&tlv.value);
                out.usb_composition = reader.read_u32(Endian::Little)?;
                let tlv = tlvs
                    .iter()
                    .find(|tlv| tlv.id == 4)
                    .ok_or(Error::MissingTlv { id: 4 })?;
                let mut reader = TlvReader::new(&tlv.value);
                out.endpoint_type = reader.read_u8()?;
                let tlv = tlvs
                    .iter()
                    .find(|tlv| tlv.id == 5)
                    .ok_or(Error::MissingTlv { id: 5 })?;
                let mut reader = TlvReader::new(&tlv.value);
                out.composition_persistence = reader.read_u8()? != 0;
                let tlv = tlvs
                    .iter()
                    .find(|tlv| tlv.id == 6)
                    .ok_or(Error::MissingTlv { id: 6 })?;
                let mut reader = TlvReader::new(&tlv.value);
                out.immediate_setting = reader.read_u8()? != 0;
                let tlv = tlvs
                    .iter()
                    .find(|tlv| tlv.id == 7)
                    .ok_or(Error::MissingTlv { id: 7 })?;
                let mut reader = TlvReader::new(&tlv.value);
                out.reboot_after_setting = reader.read_u8()? != 0;
                Ok(out)
            }
        }
        impl crate::Response for Response {
            type Request = Request;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = crate::result::success_tlvs();
                tlvs.extend(self.to_tlvs()?);
                Ok(tlvs)
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                crate::result::check_result(tlvs)?;
                Response::from_tlvs(tlvs)
            }
        }
        impl crate::Request for Request {
            const SERVICE: u16 = super::super::SERVICE_ID;
            const MESSAGE_ID: u16 = 516;
            type Response = Response;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                self.to_tlvs()
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Self::from_tlvs(tlvs)
            }
        }
    }
    #[doc = "DMS Set Active Firmware"]
    pub mod dms_set_active_firmware {
        use crate::{Endian, Error, TlvBuf, TlvReader, TlvWriter};
        use alloc::string::String;
        use alloc::vec::Vec;
        pub const MESSAGE_ID: u16 = 518;
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Request {
            pub carrier_name: Option<String>,
            pub slot_index: Option<u8>,
            pub version: Option<String>,
        }
        impl Request {
            pub const TLV_CARRIER_NAME: u8 = 16;
            pub const TLV_SLOT_INDEX: u8 = 17;
            pub const TLV_VERSION: u8 = 18;
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = Vec::new();
                if let Some(value) = &self.carrier_name {
                    let mut writer = TlvWriter::new();
                    writer.write_string(value, 0, None)?;
                    tlvs.push(writer.into_tlv(16));
                }
                if let Some(value) = &self.slot_index {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(*value);
                    tlvs.push(writer.into_tlv(17));
                }
                if let Some(value) = &self.version {
                    let mut writer = TlvWriter::new();
                    writer.write_string(value, 0, None)?;
                    tlvs.push(writer.into_tlv(18));
                }
                Ok(tlvs)
            }
            pub fn from_tlvs(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                let mut out = Self::default();
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 16) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_string(0, 0)?;
                    out.carrier_name = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 17) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u8()?;
                    out.slot_index = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 18) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_string(0, 0)?;
                    out.version = Some(value);
                }
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Response {}
        impl Response {
            pub const TLV_RESULT: u8 = 2;
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                Ok(Vec::new())
            }
            pub fn from_tlvs(_tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Ok(Self::default())
            }
        }
        impl crate::Response for Response {
            type Request = Request;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = crate::result::success_tlvs();
                tlvs.extend(self.to_tlvs()?);
                Ok(tlvs)
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                crate::result::check_result(tlvs)?;
                Response::from_tlvs(tlvs)
            }
        }
        impl crate::Request for Request {
            const SERVICE: u16 = super::super::SERVICE_ID;
            const MESSAGE_ID: u16 = 518;
            type Response = Response;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                self.to_tlvs()
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Self::from_tlvs(tlvs)
            }
        }
    }
    #[doc = "DMS Get Firmware List"]
    pub mod dms_get_firmware_list {
        use crate::{Endian, Error, TlvBuf, TlvReader, TlvWriter};
        use alloc::string::String;
        use alloc::vec::Vec;
        pub const MESSAGE_ID: u16 = 520;
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Request {
            pub mode: u8,
            pub slot_index: Option<u8>,
            pub name: Option<String>,
            pub version: Option<String>,
        }
        impl Request {
            pub const TLV_MODE: u8 = 1;
            pub const TLV_SLOT_INDEX: u8 = 16;
            pub const TLV_NAME: u8 = 17;
            pub const TLV_VERSION: u8 = 18;
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = Vec::new();
                {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(*&self.mode);
                    tlvs.push(writer.into_tlv(1));
                }
                if let Some(value) = &self.slot_index {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(*value);
                    tlvs.push(writer.into_tlv(16));
                }
                if let Some(value) = &self.name {
                    let mut writer = TlvWriter::new();
                    writer.write_string(value, 0, None)?;
                    tlvs.push(writer.into_tlv(17));
                }
                if let Some(value) = &self.version {
                    let mut writer = TlvWriter::new();
                    writer.write_string(value, 0, None)?;
                    tlvs.push(writer.into_tlv(18));
                }
                Ok(tlvs)
            }
            pub fn from_tlvs(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                let mut out = Self::default();
                let tlv = tlvs
                    .iter()
                    .find(|tlv| tlv.id == 1)
                    .ok_or(Error::MissingTlv { id: 1 })?;
                let mut reader = TlvReader::new(&tlv.value);
                out.mode = reader.read_u8()?;
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 16) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u8()?;
                    out.slot_index = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 17) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_string(0, 0)?;
                    out.name = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 18) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_string(0, 0)?;
                    out.version = Some(value);
                }
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Response {
            pub mode: u8,
            pub stored_firmware_1: Option<StoredFirmware1>,
            pub stored_firmware_2: Option<StoredFirmware2>,
            pub stored_firmware_3: Option<StoredFirmware3>,
            pub stored_firmware_4: Option<StoredFirmware4>,
        }
        impl Response {
            pub const TLV_RESULT: u8 = 2;
            pub const TLV_MODE: u8 = 1;
            pub const TLV_STORED_FIRMWARE_1: u8 = 16;
            pub const TLV_STORED_FIRMWARE_2: u8 = 17;
            pub const TLV_STORED_FIRMWARE_3: u8 = 18;
            pub const TLV_STORED_FIRMWARE_4: u8 = 19;
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = Vec::new();
                {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(*&self.mode);
                    tlvs.push(writer.into_tlv(1));
                }
                if let Some(value) = &self.stored_firmware_1 {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(16));
                }
                if let Some(value) = &self.stored_firmware_2 {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(17));
                }
                if let Some(value) = &self.stored_firmware_3 {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(18));
                }
                if let Some(value) = &self.stored_firmware_4 {
                    let mut writer = TlvWriter::new();
                    (value).encode_to(&mut writer)?;
                    tlvs.push(writer.into_tlv(19));
                }
                Ok(tlvs)
            }
            pub fn from_tlvs(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                let mut out = Self::default();
                let tlv = tlvs
                    .iter()
                    .find(|tlv| tlv.id == 1)
                    .ok_or(Error::MissingTlv { id: 1 })?;
                let mut reader = TlvReader::new(&tlv.value);
                out.mode = reader.read_u8()?;
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 16) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = StoredFirmware1::decode_from(&mut reader)?;
                    out.stored_firmware_1 = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 17) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = StoredFirmware2::decode_from(&mut reader)?;
                    out.stored_firmware_2 = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 18) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = StoredFirmware3::decode_from(&mut reader)?;
                    out.stored_firmware_3 = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 19) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = StoredFirmware4::decode_from(&mut reader)?;
                    out.stored_firmware_4 = Some(value);
                }
                Ok(out)
            }
        }
        impl crate::Response for Response {
            type Request = Request;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = crate::result::success_tlvs();
                tlvs.extend(self.to_tlvs()?);
                Ok(tlvs)
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                crate::result::check_result(tlvs)?;
                Response::from_tlvs(tlvs)
            }
        }
        impl crate::Request for Request {
            const SERVICE: u16 = super::super::SERVICE_ID;
            const MESSAGE_ID: u16 = 520;
            type Response = Response;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                self.to_tlvs()
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Self::from_tlvs(tlvs)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct StoredFirmware1 {
            pub index: u8,
            pub name: String,
            pub version: String,
            pub pri_revision: String,
        }
        impl StoredFirmware1 {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(*&self.index);
                writer.write_string(&self.name, 1, None)?;
                writer.write_string(&self.version, 1, None)?;
                writer.write_string(&self.pri_revision, 1, None)?;
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.index = reader.read_u8()?;
                out.name = reader.read_string(1, 0)?;
                out.version = reader.read_string(1, 0)?;
                out.pri_revision = reader.read_string(1, 0)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct StoredFirmware2 {
            pub index: u8,
            pub name: String,
            pub version: String,
            pub pri_revision: String,
        }
        impl StoredFirmware2 {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(*&self.index);
                writer.write_string(&self.name, 1, None)?;
                writer.write_string(&self.version, 1, None)?;
                writer.write_string(&self.pri_revision, 1, None)?;
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.index = reader.read_u8()?;
                out.name = reader.read_string(1, 0)?;
                out.version = reader.read_string(1, 0)?;
                out.pri_revision = reader.read_string(1, 0)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct StoredFirmware3 {
            pub index: u8,
            pub name: String,
            pub version: String,
            pub pri_revision: String,
        }
        impl StoredFirmware3 {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(*&self.index);
                writer.write_string(&self.name, 1, None)?;
                writer.write_string(&self.version, 1, None)?;
                writer.write_string(&self.pri_revision, 1, None)?;
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.index = reader.read_u8()?;
                out.name = reader.read_string(1, 0)?;
                out.version = reader.read_string(1, 0)?;
                out.pri_revision = reader.read_string(1, 0)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct StoredFirmware4 {
            pub index: u8,
            pub name: String,
            pub version: String,
            pub pri_revision: String,
        }
        impl StoredFirmware4 {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u8(*&self.index);
                writer.write_string(&self.name, 1, None)?;
                writer.write_string(&self.version, 1, None)?;
                writer.write_string(&self.pri_revision, 1, None)?;
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.index = reader.read_u8()?;
                out.name = reader.read_string(1, 0)?;
                out.version = reader.read_string(1, 0)?;
                out.pri_revision = reader.read_string(1, 0)?;
                Ok(out)
            }
        }
    }
    #[doc = "DMS Set Firmware Auto Sim"]
    pub mod dms_set_firmware_auto_sim {
        use crate::{Endian, Error, TlvBuf, TlvReader, TlvWriter};
        use alloc::string::String;
        use alloc::vec::Vec;
        pub const MESSAGE_ID: u16 = 546;
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Request {
            pub auto_sim_mode: u8,
            pub config_id: Option<u8>,
        }
        impl Request {
            pub const TLV_AUTO_SIM_MODE: u8 = 1;
            pub const TLV_CONFIG_ID: u8 = 16;
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = Vec::new();
                {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(*&self.auto_sim_mode);
                    tlvs.push(writer.into_tlv(1));
                }
                if let Some(value) = &self.config_id {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(*value);
                    tlvs.push(writer.into_tlv(16));
                }
                Ok(tlvs)
            }
            pub fn from_tlvs(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                let mut out = Self::default();
                let tlv = tlvs
                    .iter()
                    .find(|tlv| tlv.id == 1)
                    .ok_or(Error::MissingTlv { id: 1 })?;
                let mut reader = TlvReader::new(&tlv.value);
                out.auto_sim_mode = reader.read_u8()?;
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 16) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u8()?;
                    out.config_id = Some(value);
                }
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Response {}
        impl Response {
            pub const TLV_RESULT: u8 = 2;
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                Ok(Vec::new())
            }
            pub fn from_tlvs(_tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Ok(Self::default())
            }
        }
        impl crate::Response for Response {
            type Request = Request;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = crate::result::success_tlvs();
                tlvs.extend(self.to_tlvs()?);
                Ok(tlvs)
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                crate::result::check_result(tlvs)?;
                Response::from_tlvs(tlvs)
            }
        }
        impl crate::Request for Request {
            const SERVICE: u16 = super::super::SERVICE_ID;
            const MESSAGE_ID: u16 = 546;
            type Response = Response;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                self.to_tlvs()
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Self::from_tlvs(tlvs)
            }
        }
    }
    #[doc = "DMS Get Firmware Auto Sim"]
    pub mod dms_get_firmware_auto_sim {
        use crate::{Endian, Error, TlvBuf, TlvReader, TlvWriter};
        use alloc::string::String;
        use alloc::vec::Vec;
        pub const MESSAGE_ID: u16 = 547;
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Request {}
        impl Request {
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                Ok(Vec::new())
            }
            pub fn from_tlvs(_tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Ok(Self::default())
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Response {
            pub auto_sim_mode: u8,
            pub config_id: Option<u8>,
        }
        impl Response {
            pub const TLV_RESULT: u8 = 2;
            pub const TLV_AUTO_SIM_MODE: u8 = 3;
            pub const TLV_CONFIG_ID: u8 = 16;
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = Vec::new();
                {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(*&self.auto_sim_mode);
                    tlvs.push(writer.into_tlv(3));
                }
                if let Some(value) = &self.config_id {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(*value);
                    tlvs.push(writer.into_tlv(16));
                }
                Ok(tlvs)
            }
            pub fn from_tlvs(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                let mut out = Self::default();
                let tlv = tlvs
                    .iter()
                    .find(|tlv| tlv.id == 3)
                    .ok_or(Error::MissingTlv { id: 3 })?;
                let mut reader = TlvReader::new(&tlv.value);
                out.auto_sim_mode = reader.read_u8()?;
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 16) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let value = reader.read_u8()?;
                    out.config_id = Some(value);
                }
                Ok(out)
            }
        }
        impl crate::Response for Response {
            type Request = Request;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = crate::result::success_tlvs();
                tlvs.extend(self.to_tlvs()?);
                Ok(tlvs)
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                crate::result::check_result(tlvs)?;
                Response::from_tlvs(tlvs)
            }
        }
        impl crate::Request for Request {
            const SERVICE: u16 = super::super::SERVICE_ID;
            const MESSAGE_ID: u16 = 547;
            type Response = Response;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                self.to_tlvs()
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Self::from_tlvs(tlvs)
            }
        }
    }
    #[doc = "DMS Get Ethernet PDU MAC Address"]
    pub mod dms_get_ethernet_pdu_mac_address {
        use crate::{Endian, Error, TlvBuf, TlvReader, TlvWriter};
        use alloc::string::String;
        use alloc::vec::Vec;
        pub const MESSAGE_ID: u16 = 557;
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Request {}
        impl Request {
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                Ok(Vec::new())
            }
            pub fn from_tlvs(_tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Ok(Self::default())
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Response {
            pub mac_address_0: Vec<u8>,
            pub mac_address_1: Option<Vec<u8>>,
        }
        impl Response {
            pub const TLV_RESULT: u8 = 2;
            pub const TLV_MAC_ADDRESS_0: u8 = 3;
            pub const TLV_MAC_ADDRESS_1: u8 = 16;
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = Vec::new();
                {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(
                        u8::try_from((&self.mac_address_0).len()).map_err(|_| Error::TooLong)?,
                    );
                    for item in &self.mac_address_0 {
                        writer.write_u8(*item);
                    }
                    tlvs.push(writer.into_tlv(3));
                }
                if let Some(value) = &self.mac_address_1 {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::try_from((value).len()).map_err(|_| Error::TooLong)?);
                    for item in value {
                        writer.write_u8(*item);
                    }
                    tlvs.push(writer.into_tlv(16));
                }
                Ok(tlvs)
            }
            pub fn from_tlvs(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                let mut out = Self::default();
                let tlv = tlvs
                    .iter()
                    .find(|tlv| tlv.id == 3)
                    .ok_or(Error::MissingTlv { id: 3 })?;
                let mut reader = TlvReader::new(&tlv.value);
                let count = reader.read_u8()? as usize;
                let mut items = Vec::with_capacity(count.min(256));
                for _ in 0..count {
                    items.push(reader.read_u8()?);
                }
                out.mac_address_0 = items;
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 16) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let count = reader.read_u8()? as usize;
                    let mut items = Vec::with_capacity(count.min(256));
                    for _ in 0..count {
                        items.push(reader.read_u8()?);
                    }
                    let value = items;
                    out.mac_address_1 = Some(value);
                }
                Ok(out)
            }
        }
        impl crate::Response for Response {
            type Request = Request;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = crate::result::success_tlvs();
                tlvs.extend(self.to_tlvs()?);
                Ok(tlvs)
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                crate::result::check_result(tlvs)?;
                Response::from_tlvs(tlvs)
            }
        }
        impl crate::Request for Request {
            const SERVICE: u16 = super::super::SERVICE_ID;
            const MESSAGE_ID: u16 = 557;
            type Response = Response;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                self.to_tlvs()
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Self::from_tlvs(tlvs)
            }
        }
    }
}
