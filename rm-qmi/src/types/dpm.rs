//! Generated by `cargo xtask qmi-codegen`; do not edit.
//!
//! Source: `qmi-service-dpm.json`.

#![allow(unused_imports)]
pub const SERVICE_ID: u16 = 47;
pub mod messages {
    #[doc = "Open Port"]
    pub mod open_port {
        use crate::{Endian, Error, TlvBuf, TlvReader, TlvWriter};
        use alloc::string::String;
        use alloc::vec::Vec;
        pub const MESSAGE_ID: u16 = 32;
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Request {
            pub control_ports: Option<Vec<Element>>,
            pub hardware_data_ports: Option<Vec<Element2>>,
            pub software_data_ports: Option<Vec<Element3>>,
        }
        impl Request {
            pub const TLV_CONTROL_PORTS: u8 = 16;
            pub const TLV_HARDWARE_DATA_PORTS: u8 = 17;
            pub const TLV_SOFTWARE_DATA_PORTS: u8 = 18;
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = Vec::new();
                if let Some(value) = &self.control_ports {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::try_from((value).len()).map_err(|_| Error::TooLong)?);
                    for item in value {
                        (item).encode_to(&mut writer)?;
                    }
                    tlvs.push(writer.into_tlv(16));
                }
                if let Some(value) = &self.hardware_data_ports {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::try_from((value).len()).map_err(|_| Error::TooLong)?);
                    for item in value {
                        (item).encode_to(&mut writer)?;
                    }
                    tlvs.push(writer.into_tlv(17));
                }
                if let Some(value) = &self.software_data_ports {
                    let mut writer = TlvWriter::new();
                    writer.write_u8(u8::try_from((value).len()).map_err(|_| Error::TooLong)?);
                    for item in value {
                        (item).encode_to(&mut writer)?;
                    }
                    tlvs.push(writer.into_tlv(18));
                }
                Ok(tlvs)
            }
            pub fn from_tlvs(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                let mut out = Self::default();
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 16) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let count = reader.read_u8()? as usize;
                    let mut items = Vec::with_capacity(count.min(256));
                    for _ in 0..count {
                        items.push(Element::decode_from(&mut reader)?);
                    }
                    let value = items;
                    out.control_ports = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 17) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let count = reader.read_u8()? as usize;
                    let mut items = Vec::with_capacity(count.min(256));
                    for _ in 0..count {
                        items.push(Element2::decode_from(&mut reader)?);
                    }
                    let value = items;
                    out.hardware_data_ports = Some(value);
                }
                if let Some(tlv) = tlvs.iter().find(|tlv| tlv.id == 18) {
                    let mut reader = TlvReader::new(&tlv.value);
                    let count = reader.read_u8()? as usize;
                    let mut items = Vec::with_capacity(count.min(256));
                    for _ in 0..count {
                        items.push(Element3::decode_from(&mut reader)?);
                    }
                    let value = items;
                    out.software_data_ports = Some(value);
                }
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Response {}
        impl Response {
            pub const TLV_RESULT: u8 = 2;
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                Ok(Vec::new())
            }
            pub fn from_tlvs(_tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Ok(Self::default())
            }
        }
        impl crate::Response for Response {
            type Request = Request;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = crate::result::success_tlvs();
                tlvs.extend(self.to_tlvs()?);
                Ok(tlvs)
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                crate::result::check_result(tlvs)?;
                Response::from_tlvs(tlvs)
            }
        }
        impl crate::Request for Request {
            const SERVICE: u16 = super::super::SERVICE_ID;
            const MESSAGE_ID: u16 = 32;
            type Response = Response;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                self.to_tlvs()
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Self::from_tlvs(tlvs)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Element {
            pub port_name: String,
            pub endpoint_type: u32,
            pub interface_number: u32,
        }
        impl Element {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_string(&self.port_name, 1, None)?;
                writer.write_u32(*&self.endpoint_type, Endian::Little);
                writer.write_u32(*&self.interface_number, Endian::Little);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.port_name = reader.read_string(1, 0)?;
                out.endpoint_type = reader.read_u32(Endian::Little)?;
                out.interface_number = reader.read_u32(Endian::Little)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Element2 {
            pub endpoint_type: u32,
            pub interface_number: u32,
            pub rx_endpoint_number: u32,
            pub tx_endpoint_number: u32,
        }
        impl Element2 {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u32(*&self.endpoint_type, Endian::Little);
                writer.write_u32(*&self.interface_number, Endian::Little);
                writer.write_u32(*&self.rx_endpoint_number, Endian::Little);
                writer.write_u32(*&self.tx_endpoint_number, Endian::Little);
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.endpoint_type = reader.read_u32(Endian::Little)?;
                out.interface_number = reader.read_u32(Endian::Little)?;
                out.rx_endpoint_number = reader.read_u32(Endian::Little)?;
                out.tx_endpoint_number = reader.read_u32(Endian::Little)?;
                Ok(out)
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Element3 {
            pub endpoint_type: u32,
            pub interface_number: u32,
            pub port_name: String,
        }
        impl Element3 {
            pub fn encode_to(&self, writer: &mut TlvWriter) -> Result<(), Error> {
                writer.write_u32(*&self.endpoint_type, Endian::Little);
                writer.write_u32(*&self.interface_number, Endian::Little);
                writer.write_string(&self.port_name, 1, None)?;
                Ok(())
            }
            pub fn decode_from(reader: &mut TlvReader<'_>) -> Result<Self, Error> {
                let mut out = Self::default();
                out.endpoint_type = reader.read_u32(Endian::Little)?;
                out.interface_number = reader.read_u32(Endian::Little)?;
                out.port_name = reader.read_string(1, 0)?;
                Ok(out)
            }
        }
    }
    #[doc = "Close Port"]
    pub mod close_port {
        use crate::{Endian, Error, TlvBuf, TlvReader, TlvWriter};
        use alloc::string::String;
        use alloc::vec::Vec;
        pub const MESSAGE_ID: u16 = 33;
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Request {}
        impl Request {
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                Ok(Vec::new())
            }
            pub fn from_tlvs(_tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Ok(Self::default())
            }
        }
        #[derive(Debug, Clone, PartialEq, Default)]
        pub struct Response {}
        impl Response {
            pub const TLV_RESULT: u8 = 2;
            pub fn to_tlvs(&self) -> Result<Vec<TlvBuf>, Error> {
                Ok(Vec::new())
            }
            pub fn from_tlvs(_tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Ok(Self::default())
            }
        }
        impl crate::Response for Response {
            type Request = Request;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                let mut tlvs = crate::result::success_tlvs();
                tlvs.extend(self.to_tlvs()?);
                Ok(tlvs)
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                crate::result::check_result(tlvs)?;
                Response::from_tlvs(tlvs)
            }
        }
        impl crate::Request for Request {
            const SERVICE: u16 = super::super::SERVICE_ID;
            const MESSAGE_ID: u16 = 33;
            type Response = Response;
            fn encode(&self) -> Result<Vec<TlvBuf>, Error> {
                self.to_tlvs()
            }
            fn decode(tlvs: &[TlvBuf]) -> Result<Self, Error> {
                Self::from_tlvs(tlvs)
            }
        }
    }
}
